# Day 8 — Prompt Engineering 与上下文工程 + 渠道铺设

> 目标：掌握高级 Prompt 技巧和上下文工程的面试要点，同步启动求职渠道铺设
> 上午学技术，下午铺渠道，晚上自测

---

## 🔗 与主文件《3 周版》的联动（避免重复，先读这里）

本文件是 Day8 的**深化执行手册**（技术 + 渠道双主题）：

| 你要做的 | 去哪找 | 本文件补充什么 |
|---------|--------|---------------|
| 查 Prompt/上下文深度 | 主文件 **附录 A**（RAG/Agent 原理） | ✅ 补 CoT/ToT/结构化输出深化 |
| 查渠道铺设打法 | 主文件 **附录 D.1**（公司分层）+ **D.4**（技术影响力） | ✅ 补猎头/内推/平台实操 |
| 查投递策略 | 主文件 **第二周 Day 1-2**（渠道半日表） | ✅ 本文件做"渠道深化" |
| 做半日时段练习 | 主文件 **第二周 Day 1**（已含渠道半日表） | ✅ 本文件做"主题深化" |

**阅读顺序建议**：上午专注 Prompt 技术，下午专注渠道实操——两个主题不要混。

---

## 今日安排

| 时段 | 内容 | 产出 |
|------|------|------|
| 上午 9:00-12:00 | Prompt Engineering + 上下文工程面试问答 | 技术笔记 |
| 下午 14:00-15:00 | 结构化输出 + 可观测性 + 缓存策略 | 技术笔记 |
| 下午 15:00-17:00 | **渠道铺设**：联系猎头、更新简历平台、发内推 | 渠道清单 |
| 晚上 19:00-20:00 | 自测 + 面试投递 2-3 家练手公司 | 投递记录 |

> **图标约定**：🎯 面试官追问 / ⚠️ 踩坑提示

---

## Part A：Prompt Engineering 高频面试题

### Q1. 系统对比 CoT、Few-shot CoT、Self-Consistency 和 Tree-of-Thought

**答案：**

| 维度 | CoT | Self-Consistency | Tree-of-Thought |
|------|-----|------------------|-----------------|
| 推理结构 | 线性链 | 多条独立链 | 树状搜索 |
| 计算成本 | 1x | Nx (采样次数) | 10-100x |
| 是否可回溯 | 否 | 否 | 是 |
| 适用问题 | 中等复杂度推理 | 客观题多数投票 | 需要探索的复杂问题 |
| 生产可行性 | 高 | 中高 | 低（特定场景可行） |

**CoT 工程要点**：
- Few-shot 示例的推理步骤质量直接决定输出质量
- 推理链会增加 token 消耗 2-5 倍，需在延迟和准确率间权衡
- 对简单任务 CoT 可能反而降低性能（"过度思考"）

**Self-Consistency 工程要点**：
- 成本是普通 CoT 的 N 倍，可并行调用 API 降低延迟
- 开放式问题需改用语义相似度聚类代替投票
- 可结合 confidence score：不仅统计频次，还考虑 log-probability

**ToT 工程要点**：
- 计算开销极大，适合离线或准确率要求极高的场景
- 评估器设计是关键：可用同一 LLM 做 self-evaluation
- 生产落地时设置最大搜索深度和剪枝阈值

---

### Q2. ReAct Prompting 是什么？与 CoT 有何不同？

**答案：**

ReAct = Reasoning + Acting，将 LLM 推理能力与外部工具调用结合：

```
Thought: 分析当前状态
Action: 选择并调用工具
Observation: 获取工具返回结果
... (循环)
Final Answer: 最终结论
```

| 维度 | CoT | ReAct |
|------|-----|-------|
| 信息来源 | 仅依赖模型内部知识 | 可调用外部工具获取实时信息 |
| 幻觉风险 | 高（无法验证事实） | 低（可通过工具查证） |
| 工程复杂度 | 低（单次调用） | 高（工具注册、循环控制、错误处理） |

**工程实现关键**：
- 循环控制：设置最大迭代次数（5-10 次）
- Action 解析鲁棒性：需要 robust parser + fallback
- 工具错误处理：失败信息反馈给模型让其调整策略
- 安全边界：限制工具权限，避免危险操作

---

### Q3. 什么是 Meta-Prompting？

**答案：**

Meta-Prompting 是"用 LLM 生成/优化 Prompt"的技术范式：

1. **Prompt 自动生成**：用户描述任务目标，Meta-Prompt 让 LLM 生成完整的 System Prompt
2. **Prompt 优化迭代**：基于评估反馈自动改进 Prompt（代表框架：DSPy、OPRO、TextGrad）
3. **动态 Prompt 编排**：根据用户输入和上下文动态组装 Prompt 各模块

**企业级应用**：
- 多租户 Prompt 管理（版本化 + CI/CD）
- A/B 测试自动化（自动生成候选 → 线上测试 → 选最优）
- 自适应 Prompt（根据用户画像动态调整风格）

---

### Q4. 上下文工程（Context Engineering）深入解析

**答案：**

上下文工程是系统性地设计和管理 LLM 输入上下文的工程实践。

**1. 上下文压缩**

| 方法 | 原理 | 适用场景 |
|------|------|----------|
| LLM 摘要压缩 | 小模型生成摘要 | 对话历史压缩 |
| 选择性保留 | 相关性评分 top-K | RAG 检索裁剪 |
| 增量摘要 | 滑动窗口式摘要 | 长对话记忆 |
| 结构化抽取 | 抽取 key-value 对 | 合同/报告解析 |

**2. 信息排序（应对 Lost in the Middle）**

- 高优先级信息放开头（System Prompt、核心约束）
- 任务指令放末尾（利用 Recency Effect）
- 参考资料按相关性排序，最相关的放首尾
- 三明治结构：关键指令开头重复 + 末尾强调

**3. Token 预算分配（8K 上下文示例）**

```
System Prompt:     ~500 tokens  (6%)
Tool Descriptions: ~500 tokens  (6%)
Few-shot Examples: ~1000 tokens (12%)
Retrieved Docs:    ~3000 tokens (38%)
Conversation History: ~2000 tokens (25%)
User Query:        ~500 tokens  (6%)
Reserved for Output: ~500 tokens (6%)
```

**4. Lost in the Middle 问题**

核心发现：信息在开头/结尾时准确率 >75%，在中间时可低至 20-30%。

**缓解策略**：
- 重新排序：最相关文档放首尾
- 分段处理：Map-Reduce 模式
- 压缩上下文：减少无关内容
- 结构化标记：XML 标签或 Markdown 标题辅助定位

---

### Q5. LLM 结构化输出方案对比

**答案：**

| 方案 | 可靠性 | 灵活性 | 适用场景 |
|------|--------|--------|----------|
| JSON Mode | 中 | 高 | 简单 JSON 输出 |
| Function Calling | 高 | 中 | 工具调用、API 集成 |
| Pydantic 约束 | 最高 | 中 | 严格 schema 验证 |
| Output Parser | 中低 | 最高 | 自定义格式 |

**最佳实践**：Pydantic + OpenAI Structured Output 是当前生产环境中最可靠的方案，配合 `strict: true` 参数强制输出符合 Schema。

---

### Q6. LLM 应用的可观测性工具对比

**答案：**

| 工具 | 定位 | 核心优势 | 适用场景 |
|------|------|---------|---------|
| **Langfuse** | 开源可观测性平台 | 自托管、Trace + Eval + Dataset | 需要数据不出域的企业 |
| **LangSmith** | LangChain 官方平台 | 与 LangChain 深度集成 | LangChain 生态用户 |
| **TruLens** | 深度可观测性 | Feedback Functions 内置 | RAG 系统深度评估 |
| **Arize Phoenix** | 开发调试 | 本地运行、零配置 | 早期开发调试 |

**选型建议**：生产环境首选 Langfuse（开源 + 自托管），LangChain 深度用户可用 LangSmith 补充。

---

### Q7. LLM 应用的缓存策略

**答案：**

**语义缓存 vs 精确缓存**：
- **精确缓存**：完全相同的 Query → 直接返回缓存
- **语义缓存**：语义相似的 Query → 返回缓存（基于 Embedding 相似度）

**语义缓存工程要点**：
- 相似度阈值：通常 0.92-0.95，太低会返回不相关结果
- 缓存失效策略：TTL + 知识库更新触发失效
- 命中率：取决于业务场景，通常 15-30%
- 实现方案：Redis + Embedding 向量索引

**十大优化技巧**（来自 Redis LangCache）：
1. 对 System Prompt 单独缓存
2. 分层缓存（精确匹配 → 语义匹配）
3. 基于用户/租户的缓存隔离
4. 热度感知的缓存淘汰
5. 异步缓存写入，不阻塞主流程

---

### Q8. 多模型路由策略

**答案：**

```
用户请求 → 分类器 → 简单查询 → 小模型（7B, 低成本）
                   → 中等查询 → 中模型（GPT-4o-mini）
                   → 复杂查询 → 大模型（GPT-4o/Claude）
```

**路由策略**：
- **基于任务类型**：FAQ → 小模型，推理 → 大模型
- **基于成本**：低优先 → Spot 实例，高优先 → 预留实例
- **基于延迟**：实时 → 就近节点，批量 → 成本最优节点
- **动态权重调整**：根据后端模型延迟、成功率实时调整流量

**负载均衡与传统 Web 的差异**：
- LLM 请求是有状态的（KV Cache），不能简单轮询
- 冷启动分钟级（vs Web 的秒级）
- 请求成本差异巨大（10 token vs 10K token）

---

## Part A 补充：Prompt Engineering → Prompt Management 的工程化演进

> **核心认知**：2026 年面试中，如果你还在大谈 CoT 技巧怎么写，面试官会觉得你停留在 2023 年。当前的趋势是——Prompt 本身在贬值，**Prompt 的版本管理、自动化评测、A/B 测试 Pipeline** 才是工程重点。从"谁能写出最好的 Prompt"变成"谁能搭建让 Prompt 持续变好的系统"。

### Q.新增："为什么说 Prompt Engineering 在贬值，Prompt Management 在升值？"

> "Prompt Engineering 解决的是'一个 prompt 怎么写好'——这是一个人的手艺。Prompt Management 解决的是'N 个 prompt 在 N 个场景下怎么持续变好、不出错、可回滚'——这是一个系统的问题。
>
> 三个变化驱动了这个转变：
> 1. **模型变聪明了**：GPT-4 级别的模型对 Prompt 的敏感度远低于 GPT-3.5 时代。以前要精心构造 Few-shot 才能做好的任务，现在 Zero-shot 就能搞定 80%。你的 Prompt 调优空间被模型本身压缩了。
> 2. **Prompt 数量爆炸**：一个生产级 RAG 系统可能有 20+ 个 Prompt（系统 prompt、query 改写 prompt、路由 prompt、生成 prompt、后处理 prompt……）。手改任何一个都要提心吊胆。没有版本管理和回归测试，Prompt 上线 = 赌博。
> 3. **评测体系成了瓶颈**：Prompt 改得好不好，不能靠"我感觉"。必须有 Golden Set + 自动评分，每次 Prompt 变更都跑一遍，分降了就回滚。这套体系比任何一个具体的 Prompt 写法更有价值。
>
> 所以我在面试中会主动说：我最擅长的不是写 Prompt，而是搭 Prompt 管理体系——让团队的 Prompt 变更从'凭感觉'变成'有数据、可回滚、可审计'。"

### Q.新增："具体怎么搭 Prompt 管理体系？"

> "四个组件，按优先级：
>
> **1. Prompt 版本化**（基础设施）：所有 Prompt 存入 Git / LangSmith Hub，每次改动用 PR + Code Review。不是粘贴在代码里一个字符串，而是配置文件或模板文件，带版本号和最后修改人。
>
> **2. 自动化评测回归**（质量闸门）：建 Golden Set（至少 50 条覆盖各类场景的测试用例），每次 Prompt 变更触发评测 Pipeline——跑所有 Case → 对比新旧 Prompt 的 LLM-as-Judge 评分。质量分下降 > 5% → 阻断合并。
>
> **3. A/B 测试 Pipeline**（线上验证）：离线评测过了只是第一步，必须在线上做 10% vs 90% A/B 对比用户满意度、点踩率、任务完成率。离线分涨了但线上变差的情况太常见了——因为离线 Case 永远覆盖不了真实世界的长尾。
>
> **4. Prompt 变更文档**（知识沉淀）：每次 Prompt 变更附带'为什么改'和'预期效果'。三个月后你能回溯每个 Prompt 的进化历史，而不是看着一段复杂的 Few-shot 模板一头雾水。
>
> 面试中可以加一句：'这套体系听起来麻烦，但实际值多少钱？假设一个客服系统的 Prompt，改坏了导致满意率从 85% 掉到 80%，一天就是几百个用户不满意。有自动回归测试，10 分钟内发现 → 回滚 → 损失可控。没有，等于放了一个定时炸弹。'"

### Q.新增："DSPy 这类'编程式 Prompt 优化'和手写 Prompt 的关系是什么？"

> "DSPy 的思路是对的，但 2026 年的工程实践是**两者共存**：
>
> - **手写 Prompt**：适合系统级别的 instruction（角色定义、输出格式、安全边界）。这些需要人的判断力——什么该说、什么不该说、什么语气更合适。代码优化不了价值观。
> - **DSPy/自动优化**：适合可量化评估的子任务（比如 RAG 中的'判断这段文本是否回答了用户的问题'）。这种任务有明确的正确/错误标签，模型可以自己迭代找更好的组合。
>
> 面试中表达：'我不用 DSPy 优化系统 Prompt——那是产品定义的问题。但我会用程序化方法优化检索后的 relevance 判断 Prompt，因为那是一个纯工程指标，有 golden label 可测。知道什么时候该自动化、什么时候该人工判断，是技术负责人的基本功。'
>
> **行业趋势判断**：Prompt 不会消失，但会从'核心竞争力'降级为'需要管理的工程资产'。就像 10 年前 CSS 要手写、现在用 Tailwind，Prompt 未来也会被工具层自动化大部分。但定义系统行为的顶层 Prompt（系统角色、安全策略、输出约束）永远需要人的判断。"

---

## Part B：渠道铺设行动清单

### 下午必做（2 小时）

**1. 联系猎头（30 分钟）**

准备一段自我介绍发给 2-3 家 AI 方向猎头：

```
你好，我是 XX，目前在 XX 公司担任技术负责人，
10年技术经验（算法→架构→团队管理），
近两年重点做 LLM 应用落地（RAG/Agent/知识图谱），
现在看 AI 方向 Tech Lead 的机会，期望城市 XX，薪资范围 XX。
方便的话可以聊聊。
```

**2. Boss 直聘操作（30 分钟）**
- 更新在线简历，关键词覆盖：LLM、RAG、Agent、技术负责人、Tech Lead
- 设置"在职-考虑机会"状态
- 主动打招呼 5-10 个目标岗位的 CTO/VP

**3. 脉脉操作（20 分钟）**
- 更新个人简介
- 设置"在看机会"状态
- 主动联系 3-5 个在目标公司工作的前同事/校友

**4. 内推发动（40 分钟）**
- 列出你认识的所有在 AI 公司/大厂 AI 部门工作的人
- 逐一发消息询问内推机会
- 准备好简历 PDF 方便直接转发

---

## 深度问题补充（Prompt/上下文工程难题）

### Q12. 【底层原理】Few-shot 示例怎么选？为什么"相似示例"不一定比"随机示例"好？

**① 原理**：Few-shot 是用示例引导模型行为。直觉是"选和当前 query 最相似的示例"，但研究表明这有时反而更差——因为①相似示例可能让模型过度模仿而非泛化②多样性不足导致模式坍缩。

**② 示例选择策略对比**：
- **随机选**：baseline，简单但可能选到差的
- **相似度选（embedding 最近邻）**：常用，但需配合多样性
- **多样性选（k-means 聚类后每簇选）**：覆盖各种 case 类型，泛化好
- **难度选**：优先选"边界 case"（容易错的），强化模型弱项
- **自洽选**：选能让模型输出稳定的示例（投票一致性高）

**③ 生产经验**：
- 示例数量：3-5 个性价比最高，> 8 个边际递减且贵
- 示例顺序：影响大（recency bias），把最相关的放最后
- 示例质量 > 数量：1 个高质量示例胜过 10 个平庸的
- 示例要覆盖"正例 + 反例"（什么是正确行为 + 什么不是）

**④ Tradeoff**：
- 相似 vs 多样：相似聚焦当前 case 但可能过拟合；多样泛化好但可能不聚焦
- 静态 vs 动态：静态 few-shot 固定简单；动态（检索式）准但复杂

**⑤ 示例**：
> "我们做过实验：分类任务用'相似度选示例'比'随机'准确率高 3%，但用'多样性选（k-means）'+ '相似度'组合高 7%。原因：纯相似度会让模型对某类 case 过度自信，多样性保证覆盖。另外示例顺序影响 2-3%——把最相关的放最后（利用 recency bias）。关键认知：few-shot 不是'选最像的'，是'选最能引导正确行为的'。"

---

### Q13. 【生产实战】Prompt 改了一行效果就崩，怎么管理 Prompt 版本和回归？

**① 原理**：Prompt 是"软代码"——改一行可能让模型行为剧变，但没有编译器检查。生产必须有**版本管理 + 回归测试**，否则改 prompt = 玩雷。

**② Prompt as Code 实践**：
- **版本控制**：Prompt 存 Git（不是代码里 hardcode），每次改有 diff + commit message
- **回归测试**：每次改 prompt 必跑 golden set（100-300 case）+ LLM-as-Judge 评分，分数掉就阻断合并
- **A/B 发布**：新 prompt 灰度 10% → 50% → 100%，看业务指标
- **回滚机制**：保留历史版本，效果崩了 1 键回滚

**③ 生产经验**：
- **Prompt 模板化**：用 Jinja2/string template，变量和逻辑分离
- **变更分类**：小改（调措辞）快通道；大改（换策略）必须 full regression
- **Prompt 评审**：像 code review 一样 review prompt，两人通过才能合并
- **监控漂移**：上线后监控输出分布，prompt 可能因模型升级而失效

**④ Tradeoff**：
- 严谨 vs 速度：严格回归慢但稳；快速迭代快但风险高。分级管理
- 人工 vs 自动：LLM-as-Judge 自动但可能漏；人工抽检准但慢。组合用

**⑤ 示例**：
> "我们踩过坑：改了一个 system prompt 的措辞（'请简洁回答'→'请简短回答'），结果模型输出风格全变，用户投诉。之后上了 Prompt as Code：prompt 存 Git，每次 PR 自动跑 200 条 golden set + LLM-as-Judge，分数掉 > 3% 阻断合并；灰度 10% → 50% → 100% 三档；保留 5 个历史版本可一键回滚。现在 prompt 改动事故归零。关键是把 prompt 当代码管理，不是当配置随意改。"

---

### Q14. 【权衡决策】什么时候该用结构化输出（JSON/Schema），什么时候纯文本更好？

**① 原理**：结构化输出（JSON、function calling、structured output）让模型输出可解析，但可能**牺牲表达力 + 增加成本**。纯文本灵活但解析难。

**② 决策框架**：

| 场景 | 推荐格式 | 理由 |
|------|---------|------|
| 下游系统消费（API/数据库） | 结构化（JSON Schema） | 可解析性刚需 |
| 需要多字段提取（如简历解析） | 结构化 | 清晰边界 |
| 创意生成（文案/故事） | 纯文本 | 表达力优先 |
| Agent 工具调用 | function calling | 框架约定 |
| 长篇推理（CoT） | 纯文本 | 流畅性优先 |
| 混合（推理 + 结构化结果） | 文本 reasoning + JSON result | 兼顾 |

**③ 生产经验**：
- **structured output 模式**（OpenAI/Gemini）强制 schema，解析率 99%+，但可能让模型"硬凑"
- **function calling** 适合工具调用，不适合纯数据提取
- **JSON 模式 + 描述性 schema**：给每个字段写 description（像 prompt），引导模型填对
- **解析容错**：即使结构化输出，也要有 fallback 解析（正则/JSON repair）

**④ Tradeoff**：
- 可靠性 vs 表达力：结构化可靠但可能限制模型；纯文本灵活但解析难
- 成本 vs 准确：schema 越复杂 token 越多；简单 schema 省钱但可能信息丢失

**⑤ 示例**：
> "我们简历解析用 JSON Schema 结构化输出——提取姓名/学历/工作经历等多字段，下游直接入数据库。关键是 schema 的 description 当 prompt 写（如'工作经历：按时间倒序，每段含公司/职位/起止时间/主要成就'）。早期用纯文本 + 正则解析，错误率 15%；换 structured output + schema description，错误率降到 2%。但创意文案场景我们坚持纯文本——结构化会让文案生硬。判断：数据提取用结构化，内容生成用纯文本。"

---

## 2026 进阶高频题（分水岭级）

> 以下 2 题考的是"你是否理解 2026 年 Prompt 工程向 Context Engineering 的范式转移，以及生产级 Prompt 管理"。这是区分"会写 prompt"和"能管理 prompt 生命周期"的分水岭。

### Q15. 【Context Engineering · 2026 共识】Context Engineering 和 Prompt Engineering 的本质区别是什么？

**① 原理**：2026 年 LangChain / Anthropic / 社区形成共识——Prompt Engineering 正在演进为 Context Engineering。区别不在于"换了个名字"，而在于关注点转移：
- **Prompt Engineering**：关注"怎么写好指令"（system prompt 的措辞、few-shot 示例、CoT 引导）——把模型当"执行者"，优化指令
- **Context Engineering**：关注"在正确的时间把正确的信息放进正确的上下文层"——把模型当"推理引擎"，管理输入它的全部信息（system prompt + 工具定义 + 检索内容 + 记忆 + 环境信息），优化"上下文结构"

**② 本质区别**：

| 维度 | Prompt Engineering | Context Engineering |
|------|-------------------|---------------------|
| 关注点 | 指令怎么写 | 上下文里放什么、怎么放 |
| 优化对象 | system prompt / few-shot | 五层上下文栈（system/tool/retrieved/memory/ambient） |
| 核心问题 | "怎么让模型听懂" | "该给模型什么信息、放哪一层" |
| 典型动作 | 改 prompt 措辞、加 CoT、加示例 | 信息分层（RAG vs Memory vs Ambient）、上下文压缩、token 预算分配 |

**③ 上下文栈五层（记牢这张图）**：

| 层 | 内容 | 生命周期 | 技术 |
|----|------|---------|------|
| L1 System Prompt | 角色、规则、约束 | 会话级 | 静态/版本化 |
| L2 Tool Definitions | 工具签名、description | 会话级 | Function Calling / MCP |
| L3 Retrieved Context（RAG） | 外部知识库召回 | 单次请求 | 向量检索 / BM25 |
| L4 Memory / History | 用户画像、跨会话事实 | 跨会话 | mem0 / Zep |
| L5 Ambient | 时间、位置、实时数据 | 实时 | MCP 按需拉取 |

**④ 生产经验**：
- **最常见的错误：什么都往 RAG 塞**。很多团队把用户偏好、实时库存、历史订单全塞进 RAG 知识库，导致上下文膨胀 + 召回差。正确做法：通用知识→L3（RAG）、个人偏好→L4（Memory）、实时数据→L5（Ambient，Agent 调工具按需拉）
- **Context Engineering 的核心动作是"该放哪层"**：判断标准 = 更新频率 + 个性化程度。实时变化→L5、跨会话个人→L4、知识库事实→L3
- **Token 预算分配**：上下文不是越多越好——总 token 预算要分配给各层（如 system 500 + tool 1000 + retrieved 4000 + memory 1000 + user query 500 = 7000），超预算要做压缩/截断

**⑤ 示例**：
> "我们早期把所有信息都塞进 system prompt + RAG——用户画像、历史订单、实时库存、知识库全混在一起。结果是 prompt 8000 字、RAG 召回噪音大、Lost in the Middle 严重。学了 Context Engineering 后重新分层：① system prompt 只放角色和规则（500 token）；② 用户偏好和历史走 Memory（mem0 管理，L4，按需召回）；③ 实时库存/价格走 Ambient（Agent 调 MCP 工具按需拉，L5，不预填）；④ 只有知识库事实走 RAG（L3）。分层后上下文从 8000 token 降到 3000 token，回答准确率反而提升——因为信号噪音比高了。判断：Context Engineering 的核心不是'给更多信息'，是'给对的信息在对的层'。"

🎯 **面试官追问**：
- "Ambient 层和 RAG 层有什么区别？" → RAG 是"从知识库召回"（预存的静态知识），Ambient 是"实时拉取"（当前时间/库存/位置等动态信息）。RAG 预建索引，Ambient 按需调工具
- "Memory 和 RAG 会冲突吗？" → 会。用户偏好放 Memory 还是放 RAG 的判断：个人化 + 跨会话→Memory；通用知识→RAG。别把个人偏好放 RAG（会和其他用户的数据混在一起召回）
- "Token 预算怎么分配？" → 按信息重要性 + 频率。system + tool 固定占比（10-15%）、retrieved 占大头（50-60%）、memory 适度（10-15%）、留余量给 user query + output

⚠️ **踩坑提示**：
- 别把 Context Engineering 讲成"就是 prompt engineering 换个名字"——关注点从"指令"转到"信息架构"，是范式的转移
- "我们把所有信息都放 RAG"是典型反模式——会导致上下文膨胀 + 召回差
- 忽略"该放哪层"的判断框架是高频遗漏——这是 Context Engineering 的核心方法论

---

### Q16. 【Prompt 版本管理 · 2026 高频】生产级 Prompt 怎么做版本管理和 A/B 测试？

**① 原理**：生产 AI 应用的 prompt 不是"写一次就定"——要持续迭代、A/B 测试、灰度发布、回滚。很多团队把 prompt 硬编码在代码里，改 prompt 要发版，A/B 测不了，出事回滚不了。生产级 prompt 要像代码一样版本管理 + 像配置一样动态下发 + 像实验一样 A/B 测。

**② Prompt 管理平台三要素**：

| 要素 | 作用 | 落地 |
|------|------|------|
| 版本管理 | 每次 prompt 变更有版本号 + 变更说明 + 审批 | Prompt 仓库（类似 Git），支持 diff/回滚/分支 |
| 动态下发 | prompt 不硬编码在代码里，运行时从仓库拉取 | Prompt 服务 + 缓存（代码侧只存 prompt_id） |
| A/B 测试 | 灰度对比不同 prompt 版本效果 | 流量分流 + 效果指标对比 + 显著性检验 |

**③ 生产经验**：
- **Prompt 仓库化**：用 Langfuse / Promptflow / 自建平台管理 prompt。每个 prompt 有 ID + 版本号 + 变更记录 + 关联评估集。改 prompt 走 PR + 审批（和代码一样）
- **A/B 测试流程**：① 离线先在 golden set 上跑新 prompt，确认不掉点；② 线上灰度 10% 流量跑新版本，对比业务指标（准确率/满意度/点踩率）；③ 显著性达标后全量；④ 任一护栏指标（延迟/成本/幻觉率）恶化立即回滚
- **Prompt + 模型组合管理**：换模型经常要调 prompt（不同模型对 prompt 格式敏感度不同）。prompt 版本要绑定"适配的模型版本"，换模型时自动选对应 prompt 版本

**④ Tradeoff**：
- 灵活性 vs 稳定性：prompt 动态下发灵活（改 prompt 不发版）但有风险（prompt 仓库挂了应用也挂）；要缓存兜底 + prompt 仓库高可用
- A/B 成本 vs 收益：A/B 测试要流量 + 时间 + 分析成本；不是所有 prompt 改动都值得 A/B（小改直接上 + 监控，大改必 A/B）
- 集中管理 vs 团队自治：集中管理（一个 prompt 平台）规范但慢；团队自治（各团队管自己的）快但易乱。大公司倾向集中 + 分权

**⑤ 示例**：
> "我们早期 prompt 硬编码在代码里，改一个字要发版 + 审批 3 天。上了 Prompt 管理平台（Langfuse）后：① prompt 仓库化，每个 prompt 有 ID + 版本号，改 prompt 走 PR + 审批（和代码一样）；② 动态下发，代码只存 prompt_id，运行时从平台拉取 + 本地缓存 5 分钟（平台挂了用缓存兜底）；③ A/B 测试——新 prompt 先离线跑 golden set 确认不掉点，线上灰度 10% 流量跑 3 天，对比准确率/满意度/点踩率，显著性达标全量。有一次新 prompt 线上准确率掉了 5%，靠 A/B 及时发现 + 一键回滚，没影响全量用户。判断：Prompt 不做版本管理 = 裸奔，改 prompt 出事没法回滚是生产事故。"

🎯 **面试官追问**：
- "Prompt A/B 测试的样本量怎么算？" → 看指标方差 + 最小可检测差异 + 显著性水平（通常 p<0.05）；用 Bayesian 或 frequentist 计算器。准确率类指标通常需要每版本 1000+ 样本
- "Prompt 和模型怎么协同版本管理？" → prompt 版本绑定模型版本（prompt v3 适配 GPT-4o，prompt v4 适配 Claude 3.5）；换模型时自动选对应 prompt 版本 + 跑评估回归
- "非技术同事能改 prompt 吗？" → 能，这是 prompt 仓库化的价值——产品/运营通过平台改 prompt（不用改代码），但要有审批 + 评估门禁

⚠️ **踩坑提示**：
- 别说"我们的 prompt 在代码里管"——这是 2024 做法，2026 生产级要仓库化 + 动态下发
- "我们做了 A/B 测试"如果讲不出流程（离线验证→灰度→显著性→全量/回滚），会被认为没做过
- 忽略"prompt + 模型协同版本"是高频盲点——换模型不调 prompt 会掉效果

---

### Q20. 动态 prompt 和静态 prompt 的区别？生产中怎么组合用？

**答案：**

| 维度 | 静态 prompt | 动态 prompt |
|---|---|---|
| 定义 | 编译期固定，模板 + 变量槽位（Jinja2） | 运行时按上下文组装 |
| 典型内容 | System 角色、角色设定、输出格式约束 | 工具列表、记忆、Few-shot 示例、检索结果 |
| 版本管理 | 进 Prompt 管理平台，带 ID + 版本号 | 代码生成，带代码版本 |
| 可复现性 | 高（同版本同输入必同输出） | 低（依赖运行时上下文）|
| 缓存友好度 | 高（前缀稳定，可做 KV Cache） | 低（每轮都变） |
| A/B 测试 | 容易（版本切换即可） | 难（要控制上下文变量） |

**生产组合 = 骨架静态 + 片段动态**：
```
[System: 静态骨架] ← 固定，版本管理，< 500 token
  你是 Coding Agent，遵循以下规则...
[Tools: 动态片段] ← 按用户权限裁剪工具列表
  可用工具：{filtered_tools}
[Memory: 动态片段] ← 按需召回的长期记忆 Top-K
  相关历史：{recalled_memory}
[Examples: 动态片段] ← 按相似度选 Few-shot
  类似案例：{similar_examples}
[User: 用户输入]
```

**何时必须用动态**：
1. **上下文敏感**：用户画像、历史对话、当前项目状态——每轮都变
2. **权限裁剪**：不同用户能用不同工具，工具列表必须动态拼
3. **Few-shot 检索**：按当前 query 相似度从示例库选 Top-K，不是固定示例

**工程化关键（踩过坑的才知道）**：
- **动态片段要有 token 预算**：每段独立预算（Tools < 500、Memory < 1000、Examples < 800），超预算触发压缩或截断——否则动态片段膨胀爆窗口
- **必须 trace 完整 prompt**：把每轮实际发给 LLM 的完整 prompt（静态+动态拼好的）存到 Langfuse，否则线上故障无法复现
- **动态片段也要带版本**：代码生成的片段走 Git 版本；静态片段走 Prompt 平台版本——两者用 `prompt_version + code_version` 联合标识

🎯 **面试官追问**：
- "动态 prompt 怎么做回归测试？" → Prompt 快照 + 黄金集回归——每次代码或静态 prompt 变更，在 golden set 上跑一遍对比指标
- "动态片段爆窗口怎么办？" → 每段 token 预算 + 优先级（System > Tools > Memory > Examples），超预算从低优先级开始压缩/截断
- "和 Prompt 管理系统（Langfuse）什么关系？" → 静态部分进平台管理（非技术人员可改）；动态片段由代码生成，不进平台——平台管不了代码逻辑

⚠️ **踩坑提示**：
- 别把"动态"等同于"硬编码字符串拼接"——必须有模板引擎（Jinja2）+ 版本管理 + trace
- 漏讲"trace 完整 prompt"——这是线上排查的第一前提，不存完整 prompt 出事没法复现
- 不提"token 预算"——动态片段不加约束必然爆窗口

---

## 今日自测清单

> **升级版自查**：Day8 双主题，技术 + 渠道都要达标。

| 题 | 自查问题 | 达标标准（开口讲） | ✓/✗ |
|----|---------|-------------------|-----|
| CoT/SC/ToT | 三者区别 + 选用 | 能讲清"按需升级，不滥用 ToT" | ⬜ |
| 上下文压缩 | 4 种方法 | 能讲清 Lost in the Middle + 证据前置 | ⬜ |
| 结构化输出 | 选型建议 | 能讲清 JSON Schema vs Function Calling | ⬜ |
| 可观测性 | Langfuse vs LangSmith | 能讲清选型（开源 vs SaaS） | ⬜ |
| 语义缓存 | 阈值 + 失效 | 能讲清"误命中风险" + 业务过滤 | ⬜ |
| 多模型路由 | 三种策略 | 能讲清按难度/成本/延迟路由 | ⬜ |
| 渠道铺设 | ≥ 3 个渠道触达 | 猎头 + 内推 + 平台 全启动 | ⬜ |
| 练手投递 | 2-3 家练手公司 | 已预约或投递 | ⬜ |

**达标线**：8 题中 ≥ 6 题达标 → Day8 通过。
**未达标处理**：
- 技术题答不深 → 主文件附录 A 对应章节重读
- 渠道没动起来 → 本日最大失败，必须当晚补，求职不能等
- 练手公司没投 → 别让心仪公司成首战，排序很关键

**关键提醒**：
- **本日最大坑是"只学技术不动渠道"**——技术永远学不完，渠道必须按时启动
- 渠道铺设参考主文件附录 **D.1**（公司分层）+ **D.4**（技术影响力）
- 练手公司排在心仪公司前 4-5 个，别让最想去的成首战
- Prompt/上下文工程在 RAG/Agent 题里常被追问，是 Day1/Day2 的延伸

## 参考资源

- [Prompt Engineering Guide](https://www.promptingguide.ai/techniques/cot)
- [Lost in the Middle 论文](https://arxiv.org/abs/2307.03172)
- [Redis LangCache 优化技巧](https://redis.io/blog/10-techniques-for-semantic-cache-optimization/)
- [LLM 可观测性工具对比](https://bigdataboutique.com/blog/llm-observability-tools-compared-langfuse-vs-langsmith-vs-opik)
