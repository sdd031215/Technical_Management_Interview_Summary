# QA Agent 记忆管理机制设计 — 面试特化专题

> 定位：本篇是 Day2 Q8（Agent 记忆三层架构）和 Q25（通用记忆量化设计）的 **QA 场景特化版**。
> 通用三层架构（工作记忆/长期记忆/情景记忆）见 Day2 Q8、Q25，本篇不重复，只补 QA Agent 的差异点和踩坑。
> 适用题：「设计一个 QA Agent / 智能客服 / 知识问答系统的记忆机制」「QA Agent 怎么避免答错后越答越错」

---

## 1. 开场定调（30 秒先建立区分）

> 面试官问"QA Agent 记忆怎么设计"，**最差的答法是把通用三层架构背一遍**——面试官会觉得你没思考过场景。

**一句话切入**：

> 通用 Agent 的记忆服务"行动一致性"（让 Agent 记住用户是谁、上次做到哪步）；
> **QA Agent 的记忆服务"答案一致性"** —— 它的核心风险不是忘事，而是 **把错误答案记住后反复自信地答错**。

这一句把方向定下来，后面的设计才有抓手。三个真正的难点：

1. **答案可信度记忆** —— 哪些答案能进缓存，错了怎么纠
2. **多轮追问上下文** —— 连环追问下，怎么不爆窗口又不丢话题
3. **知识 vs 记忆分工** —— RAG 检索到的文档不算记忆，混存会脏

---

## 2. QA Agent 的四类记忆（核心区分表）

通用 Agent 通常分"工作/长期/情景"三层，**QA 场景需要更细的四分类**，因为来源、生命周期、可信度差异巨大，混存必出问题：

| 记忆类型 | 存什么 | 来源 | 生命周期 | 最致命风险 |
|---|---|---|---|---|
| **对话上下文**（短期） | 多轮追问、指代消解（"它/那个"指代谁） | 用户当前输入 | 单会话内 | 窗口爆炸 / 话题漂移 |
| **用户画像**（长期） | 身份、偏好、已告知信息 | LLM 抽取 + 用户显式告知 | 跨会话 | 过期 / 隐私泄露 |
| **答案缓存**（语义缓存） | 高频问题 → 已验证答案 | 历史成功回答 | TTL + 相似度失效 | **错误答案污染** |
| **知识库引用**（外部） | 文档/FAQ 切片（只放指针） | RAG 检索 | 文档更新即失效 | 把"知识"当"记忆"混存 |

🎯 **面试官视角**：能把第 3、4 行单独拆出来讲，说明你做过系统、踩过坑。只讲前两行 = 还停留在教科书。

---

## 3. 三个关键设计决策（带 Tradeoff）

### 决策 A：答案该不该写入长期记忆？

这是 QA Agent **最核心**的决策，通用 Agent 框架不会讲。

| 方案 | 做法 | 问题 |
|---|---|---|
| 朴素做法 | 答过的全存进语义缓存 | 错误答案被反复召回，**越答越错** |
| 推荐做法 | **分级写入**：置信度 ≥ 阈值 + 用户未纠正 + 来源可溯（带 citation）才入库 | 缓存命中率会降，但污染风险可控 |

**必备机制 —— 纠错回路**：
- 用户后续说"你上次答错了" / 给差评 / 修正 → 触发记忆删除或降权
- 答案带 **版本号**，知识库更新后用新版 revalidate 旧缓存
- 定期任务：抽样缓存答案，用最新知识库重新回答，不一致的标记复核

**Tradeoff**：
- 写入门槛高 → 命中率低，但准确率高
- 写入门槛低 → 命中率高，但错误污染风险指数级放大（因为会被反复召回）
- **QA 场景永远偏向高门槛** —— 答错的代价远大于重算一次

### 决策 B：多轮追问怎么管上下文？

QA 用户常连环追问："XX 怎么配置？" → "那默认值是多少？" → "改完要重启吗？"。指代和话题焦点都要保住。

**反模式**：每轮塞全量历史 → 第 5 轮窗口就爆了，且旧话题噪音淹没当前问题。

**推荐三层结构**：
```
[当前问题]              ← 完整保留
+ [话题摘要：近 N 轮压缩] ← 滑窗 + LLM 摘要
+ [长期画像：Top-K 召回] ← 按当前 query 相关性召回
```

**关键技巧 —— 话题切换检测**：
- 用户换话题时（embedding 距离跳变 / 关键词切换），**主动摘要旧话题并归档**
- 释放窗口给新话题，旧话题转入"可按需召回"状态
- 这一步不做 → 窗口永远在前几轮话题上，新问题被稀释

### 决策 C：知识（RAG）和记忆怎么分工？

**最常见的反模式**：把检索到的文档切片当作"记忆"持久化存下来。
- 后果：文档一更新，缓存里的旧切片全是脏数据，Agent 还"自信地"引用过期信息

**正解**：
- 知识库永远是 **source of truth**，每次新查，不持久化检索结果
- 记忆里只放两类东西：
  1. **用户专属信息**（画像、历史偏好）—— 机器学不到的
  2. **已验证答案的索引（指针，不存内容）** —— 命中后回查知识库取最新内容

**一句话口诀（可直接在面试时说）**：

> **"知识每次新查，记忆只放机器学不到的。"**

🎯 这一句是面试场上区分"背过架构"和"做过系统"的分水岭。

---

## 4. 面试官追问预案

### Q: 错误答案污染怎么治？
→ 四件套：**分级写入**（门槛） + **纠错回路**（用户反馈触发删除/降权） + **答案版本号**（知识库更新后 revalidate） + **定期抽样复核**（主动发现陈旧答案）

### Q: 语义缓存怎么避免"相似但不同的问题"被误命中？
→ 不能只靠 embedding 相似度阈值。"如何取消订单" vs "如何取消订阅" embedding 很近但答案完全不同。
→ 加一层 **LLM 判定"是否真的语义等价"**，相似度只做粗筛，LLM 做精筛。成本换准确率，值得。

### Q: QA Agent 要不要情景记忆（执行轨迹）？
→ 看子场景：
  - **客服 QA**：要。成功 case 存下来做 few-shot，下次类似问题直接参考
  - **纯 FAQ 问答**：不需要。每次都是独立检索，轨迹没用
→ 判断标准：**会不会复用历史交互模式**。会 → 要情景记忆；不会 → 不要

### Q: 隐私合规怎么处理？
→ 三步：
  1. **PII 检测前置** —— 写入记忆前过一道敏感信息识别
  2. **敏感信息要么不入记忆，要么加密存储**
  3. **用户可查可删** —— GDPR/个保法的硬要求，记忆系统必须支持按用户 ID 全量删除

### Q: 你怎么评估记忆系统好不好？
→ **别只看缓存命中率**，那是误导性指标。
→ 真正看的指标：
  - **端到端回答准确率**（有记忆 vs 无记忆的对比）
  - **错误记忆占比**（抽样人工标注）
  - **纠错响应时长**（从用户反馈到记忆更新的延迟）
→ 命中率高但准确率没提升 = 记忆系统在帮倒忙

---

## 5. 可直接背的示例答（90 秒版）

> "QA Agent 的记忆设计，我的核心判断是：**它和通用 Agent 不一样，通用 Agent 记忆管'行动一致性'，QA Agent 记忆管'答案一致性'**。最致命的风险不是忘事，而是错误答案进缓存后被反复召回，越答越错。
>
> 所以我会把记忆拆成四类，**关键是把'答案缓存'和'知识库引用'从笼统的长期记忆里单独拆出来**。知识库永远是 source of truth，每次 RAG 新查，绝不把检索结果持久化成记忆——文档一更新脏数据就满天飞。记忆里只放两类：用户画像、和已验证答案的指针（不存内容，命中后回查）。
>
> 答案写入要分级：置信度达标 + 用户没纠正 + 带 citation 才入库。必须有纠错回路——用户说'上次答错了'就触发删除或降权，答案带版本号，知识库更新后批量 revalidate。语义缓存不能只靠 embedding 相似度，'取消订单'和'取消订阅' embedding 很近但答案不同，要加一层 LLM 判等价。
>
> 我们踩过坑：最早把所有成功回答都缓存，结果一个早期版本的错误答案被反复命中，用户投诉才发现。后来改成写入必须有 citation + 三天观察期（期间用户没纠正才正式入库），命中率从 60% 降到 35%，但准确率从 78% 升到 91%——QA 场景这个 tradeoff 永远值。
>
> 多轮追问用三层结构：当前问题 + 近 N 轮话题摘要 + 长期画像 Top-K 召回，加话题切换检测主动归档旧话题释放窗口。评估指标别只看命中率，看端到端准确率和错误记忆占比。"

---

## 6. 与其他材料的联动

| 想查 | 去哪 |
|---|---|
| 通用 Agent 记忆三层架构（工作/长期/情景） | Day2 **Q8** |
| 通用记忆的写入/召回/遗忘量化设计 + Tradeoff | Day2 **Q25** |
| **QA 场景特化决策（本篇）** | 本文件 |
| RAG 知识库本身的设计 | Day1 《RAG 技术体系深度梳理》 |

> 阅读顺序建议：先 Day2 Q8 建立通用框架 → Q25 看量化 → 本篇看 QA 场景怎么特化。
