# Day 4 — 项目复盘与故事打磨

> 目标：准备 2-3 个高质量项目故事，按 STAR+R 框架整理，能在面试中流畅讲述 15 分钟
> 产出：2-3 份完整的项目故事文档（口述稿）

---

## 🔗 与主文件《3 周版》的联动（避免重复，先读这里）

本文件是 Day4 的**深化执行手册**。模板与原则在主文件，本文件聚焦"怎么把模板填成可用的成品"：

| 你要做的 | 去哪找 | 本文件补充什么 |
|---------|--------|---------------|
| 拿 STAR+R 空白模板 | 主文件 **附录 C.2**（含填空模板） | ✅ 补**完整填好的范例**（脱敏示例） |
| 查故事池准备清单 | 主文件 **附录 C.2** 末尾表格 | ✅ 补每个故事的"数字怎么找" |
| 查面试官追问题库 | 主文件 **附录 B.5**（Q20-Q27 管理/行为题） | ✅ 补每个故事的针对性追问 |
| 做半日时段练习 | 主文件 **第一周 Day 5**（已含本主题半日表） | ✅ 本文件做"故事成品化" |

**核心痛点**：原模板全是 `[占位符]`，照着填还是不知道填什么。本文件给每个故事**配一个完整填好的范例**，你照着范例把自己的数据替换进去即可。

---

## 今日安排

| 时段 | 内容 | 产出 |
|------|------|------|
| 上午 9:00-11:00 | 梳理 LLM 应用项目故事（RAG/Agent） | 故事稿 1 |
| 上午 11:00-12:00 | 梳理系统架构演进项目故事 | 故事稿 2 |
| 下午 14:00-15:30 | 梳理团队管理/技术推进项目故事 | 故事稿 3 |
| 下午 15:30-17:00 | 录音自讲 + 回放改进 | 改进记录 |
| 晚上 19:00-20:00 | 准备"面试官追问"的预设答案 | 追问清单 |

> **图标约定**：
> 📝 **空白模板**——你照着填的骨架（保留原内容）
> ✨ **填好的范例**——脱敏虚构示例，展示"填好后长什么样"，照着改你的数据
> 💡 **数字怎么找**——找不到量化数字时的补救方法
> 🎯 **面试官追问**——这个故事讲完，面试官真正会接着问的

---

## STAR+R 框架详解

每个项目故事按以下结构组织，控制在 **10-15 分钟**内讲完：

```
S - Situation（背景，1-2 分钟）
    "当时我们面对的业务场景是……"
    "团队的情况是……"
    "核心挑战/痛点是……"

T - Task（任务，30 秒）
    "我的角色是……"
    "我的目标/OKR 是……"

A - Action（行动，5-7 分钟）← 最重要
    "在技术选型上，我做了……"
    "在架构设计上，我选择了……因为……"
    "在团队组织上，我……"
    "过程中遇到了 XX 困难，我的应对方式是……"

R - Result（结果，1-2 分钟）
    "最终实现了……（量化指标）"
    "对业务的影响是……"
    "团队的成长是……"

R - Reflection（反思，1 分钟）
    "如果重来，我会在 XX 方面……"
    "最大的教训是……"
    "这个经验对我后来的工作有什么影响……"
```

---

## 故事一：LLM 应用项目（RAG/Agent 系统）

### 填写模板

```markdown
## 项目名称：[你的项目名称]

### S - 背景
- 业务场景：[描述你们公司/部门做什么业务]
- 面临的问题：[为什么需要引入 LLM/RAG/Agent]
- 之前的方案：[原来的解决方案是什么，有什么问题]
- 时间背景：[什么时候启动的]

### T - 任务
- 我的角色：[技术负责人/架构师/Tech Lead]
- 团队规模：[几人团队，角色分工]
- 核心目标：[用数字定义成功标准，如准确率>X%、延迟<Ys]

### A - 行动（重点展开）

#### 技术选型决策
- 为什么选择 RAG 而不是微调？
  → "考虑到我们业务的知识更新频率（每X天更新一次），RAG 的知识更新成本远低于微调……"
- Embedding 模型选了什么？为什么？
  → "评估了 BGE、OpenAI、M3E 三个模型，在我们的业务评估集上……"
- 向量数据库选了什么？为什么？
  → "数据量级在 X 万条，考虑到运维复杂度和团队技术栈……"
- 检索方案怎么设计的？
  → "采用了混合检索（BM25+向量），用 RRF 融合，引入 BGE-Reranker 做精排……"

#### 遇到的关键技术挑战
挑战1：[比如：复杂PDF解析]
  → 解决方案 + 效果量化

挑战2：[比如：幻觉问题]
  → "设计了四层防御体系：检索层阈值过滤、Prompt层引用约束、
     生成层Self-RAG、后处理层NLI检测。幻觉率从 X% 降到 Y%"

挑战3：[比如：上下文窗口限制]
  → "采用了父子块机制，小块检索大块返回，
     结合摘要压缩将有效信息密度提升了 X%"

#### 团队组织
- 怎么分工的？
- 如何做技术评审？
- 如何管理项目进度？

### R - 结果
- 核心指标：[准确率、召回率、延迟、成本等]
- 业务影响：[节省了多少人力、提升了多少效率、覆盖了多少场景]
- 团队成长：[培养了哪些人、沉淀了哪些方法论]

### R - 反思
- 如果重来：[有什么会做不同的选择]
- 最大教训：[最有价值的经验]
- 后续演进：[这个项目后来怎么发展的]
```

### 讲述技巧
- 开场 30 秒用一句话概括项目价值："我负责了公司 XX 业务的 RAG 系统建设，将客服自动化率从 X% 提升到 Y%"
- 技术选型部分重点讲"为什么"而非"是什么"
- 用数字说话，避免模糊表述
- 反思部分展示成熟度

---

### ✨ 填好的范例（脱敏虚构，照着改你的数据）

> 以下是一个**完整填好的 LLM 应用项目故事范例**，用于展示"填完后长什么样"。数字均为虚构，请替换成你的真实数据。建议你按这个粒度和叙事节奏来组织自己的故事。

```markdown
## 项目名称：企业知识库 RAG 系统——把客服一次性解决率从 61% 提到 78%

### S - 背景（讲 1.5 分钟）
- 业务场景：公司是 ToB SaaS，企业客户用我们的客服系统处理终端用户咨询。
  日均工单量 ~1.2 万，一线客服 ~80 人，人力成本高、夜间无人值守。
- 面临的问题：原有的关键词 + FAQ 命中率低，复杂问题全转人工，客服
  一次解决率只有 61%；终端用户平均要等 4 小时才收到回复。
- 之前的方案：基于 Elasticsearch 的关键词检索 + 规则机器人，对长尾、
  口语化问题几乎无能为力。
- 时间背景：2024 年 Q2 启动，大模型刚好成熟可用。

### T - 任务（30 秒）
- 我的角色：技术负责人，带 4 人小组（2 算法 + 1 后端 + 1 前端），
  汇报给 CTO。
- 核心目标：3 个月内上线一版，把一次解决率提到 75% 以上，
  P95 首字延迟 < 3 秒，单 query 成本控制在 X 分以内。

### A - 行动（讲 6-7 分钟，核心）

#### 技术选型决策
- 为什么选 RAG 而不是微调？
  → "我们的知识库每周更新（产品迭代快），RAG 只需更新知识库不用重训，
    微调的更新成本和滞后性扛不住。这个决策后面验证是对的——半年内
    产品改版两次，我们只动了索引。"
- Embedding 选了什么？为什么？
  → "对比了 BGE-M3、OpenAI text-embedding-3、M3E 三个，在我们 300 条
    业务评估集上跑 recall@5：BGE-M3 0.82，OpenAI 0.79，M3E 0.71。
    选了 BGE-M3——中文效果最好且能私有部署（客户数据合规要求）。"
- 向量库选了什么？
  → "Milvus。当时数据量预估 50 万 chunk，且要支持多租户隔离，
    Milvus 的 collection + metadata filter 能原生支持，pgvector
    在这个规模下性能不够。"
- 检索方案？
  → "混合检索：BM25（专有名词、产品型号、错误码命中）+ 向量（语义召回），
    RRF 融合，再用 BGE-Reranker 精排 top-50 → top-5。
    只上向量时长尾 query 召回差，加 BM25 后 recall 从 0.74 涨到 0.85。"

#### 关键技术挑战
挑战1：复杂 PDF（产品手册含表格/截图）
  → "用 PyMuPDF 提文本 + PaddleOCR 处理扫描图，表格转 Markdown 整块保留。
    表格不切分这条很关键——早期按行切分，表格行列关系丢失，召回全错。"

挑战2：幻觉问题
  → "四层防御：检索层设相似度阈值（<0.6 不召回）+ Prompt 强约束只用
    上下文 + 后处理用 NLI 模型判 entailment + 低置信走兜底（'我需要转人工'）。
    幻觉率（人工抽检）从初版 12% 降到 3% 以内。"

挑战3：长文档上下文窗口限制
  → "父子分块：小块 200 token 检索，命中后返回 800 token 父块。
    配合上下文压缩，有效信息密度提升明显，token 消耗降 40%。"

#### 团队组织
- 我负责架构设计和技术选型决策，2 个算法同事负责检索链路和评估，
  后端搭 Gateway 和向量库，前端做对话界面。
- 每周一技术评审，用评估集回归数据驱动决策，避免"我觉得"的争论。

### R - 结果（讲 1.5 分钟，全是数字）
- 核心指标：一次解决率 61% → 78%（+17 个百分点），P95 首字延迟 2.3 秒，
  幻觉率 <3%，recall@5 从 0.74 提到 0.85。
- 业务影响：夜间无人值守成为可能，一线客服工单压力降 30%，
  估算年节省人力成本 ~180 万。
- 团队成长：沉淀了一套 RAG 评估 pipeline（含 300 条 golden set），
  后面接新场景（售后、续费咨询）只花 2 周就能复用上线。

### R - 反思（讲 1 分钟，体现成熟度）
- 如果重来：评估体系应该第一天就建，而不是开发到一半才补——
  前期很多选型是凭直觉，后面有 golden set 才发现有些方向是错的。
- 最大教训：文档解析被严重低估。我们花了 40% 精力在解析和清洗上，
  远超预期。"Garbage in garbage out"在 RAG 里特别真实。
- 后续演进：从单轮 RAG 升级到 Agent（加了工具调用，能查订单状态），
  这是下一个阶段的事。
```

> ⚠️ 以下为【示例项目深度追问】，演示如何回答"如果数字做到 91.2% 怎么解释"。非真实数据，面试前替换为你的真实项目细节。

### 【示例项目深度追问】如果 recall@5 做到 91.2%，怎么解释这个数字？

**场景**：这是另一个项目（多模态文档 RAG，非客服 RAG）。客服项目是 0.74→0.85；这个项目从 baseline 0.68 优化到 0.912。两个项目不要混。

**① 数字拆解（按贡献度排序）**：
| 优化项 | recall@5 提升 | 占比 |
|--------|-------------|------|
| Baseline（BGE-M3 + 向量检索） | 0.68 | — |
| + BM25 混合 + RRF 融合 | +0.12 → 0.80 | 最大头，专有名词/型号/代码命中 |
| + BGE-Reranker 精排 top-50→top-5 | +0.05 → 0.85 | 第二大，把语义噪声砍掉 |
| + Contextual Retrieval（chunk 加摘要） | +0.03 → 0.88 | 第三，解决 chunk 脱上下文问题 |
| + Query 改写（HyDE + Multi-Query） | +0.02 → 0.90 | 第四，模糊 query 友好化 |
| + 父子分块（小块检索大块喂 LLM） | +0.01 → 0.91 | 第五，上下文完整性 |
| + 人工标注纠错（修复 golden set 错标） | +0.002 → 0.912 | 收尾，评估集本身的质量 |

**② 关键工程决策**（面试官想听的"为什么"）：
- **为什么先上 BM25 而不是先换 embedding**：业务 query 含大量专有名词/型号/错误码，BM25 精确命中率最高，ROI 最大。换 embedding 要全量重建索引，成本高
- **为什么 Contextual Retrieval 排在第三**：前两项把检索精度拉起来后，chunk 脱上下文问题暴露——单个 chunk 看不懂在讲什么。给每个 chunk 加 LLM 生成的 1-2 句上下文摘要，索引成本高但收益稳定
- **为什么 Query 改写只 +0.02**：大部分 query 本身清晰，只有长尾模糊 query 受益。但 HyDE 对"概念性提问"（如"怎么理解 XX"）提升明显

**③ 金句**：
> "91.2% 不是某一个优化做到的，是 6 个优化叠加。最大头是 BM25+RRF（+0.12），因为业务 query 含大量专有名词。关键是优化顺序——先做 ROI 最大的（BM25），再做成本最高的（Contextual Retrieval），最后收尾（人工纠错 golden set）。如果反过来先做 Contextual Retrieval，收益会被前面没做 BM25 的噪声吃掉。"

---

### 【示例项目深度追问】三级压缩方案是什么？怎么设计的？

> ⚠️ 【示例，非真实数据，面试前替换】

**① 设计哲学**：借鉴 Letta/MemGPT 的 OS 风格分层内存——Core Memory（快、贵、有限）↔ Archival Memory（慢、便宜、海量），通过显式分页把有限窗口虚拟化成无限记忆。**核心原则：被压缩的内容不永久丢弃，写入 archival memory 可被工具召回**。

**② 三级方案**：

| 级别 | 触发阈值（context 占用） | 机制 | 信息损失 | 延迟开销 | 适用场景 |
|------|------------------------|------|---------|---------|---------|
| **L1 轻量** | 50%–80% | 滑动窗口 + 保留 recent N 条原始消息，砍尾部 | **无损失**（只丢时间维度） | ~0（纯截断） | 短会话、用户还在追问上下文 |
| **L2 中度** | 80%–90% | 把超出窗口的历史用 LLM 摘要成 1-2 段 | **有损**（细节丢失，语义保留） | 1 次 LLM 调用 | 长会话中期、上下文已饱和 |
| **L3 重度** | >90% | 结构化提取：只保留实体/决策/待办/关键数字（JSON schema） | **高度有损但保密度高** | 1-2 次 LLM 调用 | 超长任务、跨 session 续跑 |

**③ 关键工程细节**：
- **重要性判断维度**（L2 触发时哪些保留）：① 是否影响当前对话理解 ② 用户是否反复提及 ③ 是否为长期关键事实。保留 Top 60%，其余归档
- **不永久丢弃**：L2/L3 压缩掉的内容写入 archival memory，需要时用 `archival_memory_search` 工具召回——这是「虚拟上下文」而非「信息焚毁」
- **Agent 主动管理**：成熟方案是 Agent 主动调用 `core_memory_replace` / `archival_memory_insert` 工具管理记忆，而非被动后台任务

**④ 评估：怎么判断压缩没伤信息**：
1. **Golden case 回归集**：50-100 条「必须答对」的历史问题，压缩策略迭代后全跑，对比未压缩 baseline
2. **关键事实抽查**：从被压缩段落抽取实体/数字，看压缩后 agent 还能否答出
3. **压缩前后 perplexity 对比**：同一追问的 logprob 差异应在阈值内

**⑤ 金句**：
> "Context 压缩不是砍尾巴，是给 LLM 的注意力做信噪比优化。我的三级方案本质是 MemGPT 的 OS 分页思想：L1 截断是无损分页换出，L2 摘要是 LLM 辅助压缩，L3 结构化提取是『只留索引不留原文』。关键是不能只丢不存——所有被压缩的都进 archival memory 可被工具召回，这才是『虚拟上下文』而非『信息焚毁』。"

---

### 【示例项目深度追问】检索为什么是五轮？不是三轮不是十轮？

> ⚠️ 【示例，非真实数据，面试前替换】

**① 为什么需要多轮检索**：传统单轮 RAG 三大缺陷——① Query 模糊（用户原始提问不是最佳检索 query）② 知识分散（多跳问题需跨文档推理）③ 无自我验证（单轮无法评估召回质量）。多轮检索闭环：query 改写 → 增量检索 → 结果评估 → 决定是否继续。

**② 为什么是 5 轮**（成本-正确率曲线的交点）：

| 轮数 | 评估 | 理由 |
|------|------|------|
| **3 轮** | 太短 | 复杂多跳问题（对比/时间线/因果链）需 query 分解 + 增量检索 + 交叉验证，3 轮只够展开第一跳 |
| **5 轮** | 业界经验值 | 覆盖大多数多跳问题深度（MuSiQue 等 multi-hop benchmark 经验值）。成本曲线和正确率曲线在此相交 |
| **10 轮** | 过长 | ① 成本爆炸（每轮 1 次 LLM 评估 + 1 次检索）② 边际收益递减 ③ **上下文衰减**——Anthropic 警告「塞越多注意力越分散」，10 轮累积片段会稀释关键信息 |

**③ 每轮做什么**（Agentic RAG 标准闭环）：
1. Query 改写（把模糊问题重写成检索友好型）
2. 增量检索（vector / web / api）
3. 结果评估（grader LLM 打相关性分）
4. 决策：达标 → 生成 / 不达标 → 回到 1

**④ 提前终止（不硬跑满 5 轮）**：
- **相关性阈值**：`all(score > 0.7)` 即进入生成阶段
- **Adaptive-RAG 分级**：先用分类器判 query 复杂度（A/B/C 级），不同级别分配不同最大轮数。简单问题 1 轮就出，复杂问题才放开到 5 轮
- **反思模式终止**：事实准确性 + 来源一致性 + 完整性三维度均达标即停

**⑤ 评估：怎么衡量轮数选对了**：
1. **轨迹评估**：不只看最终答案，对每轮 query 改写、检索片段、决策点打分，判断有无「废轮」（检索了但结果未被引用）
2. **成本-正确率 Pareto**：横轴轮数，纵轴正确率，找拐点（典型在 4-5 轮后变平）
3. **Wasted-call 指标**：统计每轮检索结果是否被最终答案引用，未被引用的轮次即浪费

**⑥ 金句**：
> "5 轮不是拍脑袋，是成本曲线和正确率曲线的交点——3 轮覆盖不了多跳，10 轮因上下文衰减反而掉点。但真正的工程智慧不在定数，而在 Adaptive-RAG 的思路：先用分类器判 query 复杂度，动态分配轮数上限，配合相关性阈值提前终止。简单问题 1 轮就出，复杂问题才放开到 5 轮——既不过度检索也不检索不足。"

💡 **数字怎么找**（如果你忘了具体数字）：
- 效果指标：翻当时的周报/月报/OKR 复盘、问当时的业务方/产品经理
- 成本数字：找运维或财务要账单，按 API 调用量或 GPU 时长算
- 团队规模：翻组织架构图、招聘记录、绩效名单
- 实在记不起：用区间（"召回率从 70% 多提到 85% 左右"）比硬编精确数字安全

🎯 **这个故事讲完，面试官会追问**（提前准备）：
1. "78% 的一次解决率怎么定义和测量的？" → 人工标注 + 用户反馈双轨
2. "幻觉率 3% 是怎么测的？样本多大？" → 抽检 + NLI 模型辅助
3. "如果流量涨 10 倍，向量库怎么扩？" → 分片 + 读写分离
4. "为什么不试 GraphRAG？" → 当时不解决关系类问题，传统 RAG 够用（参考附录 B 的 Q 追问）
5. "你们 evaluation pipeline 具体长什么样？" → golden set + RAGAS + 线上点踩

---

## 故事二：系统架构演进项目

### 填写模板

```markdown
## 项目名称：[你的项目名称]

### S - 背景
- 系统现状：[原有架构是什么样的]
- 为什么需要演进：[性能瓶颈/扩展性问题/技术债]
- 业务压力：[业务增长了多少，系统需要支撑什么]

### T - 任务
- 我的角色：[架构师/技术负责人]
- 目标：[性能提升 X%、支持 Y 倍流量增长、降低 Z% 成本]

### A - 行动

#### 架构分析
- 对现有系统做了哪些诊断？
- 瓶颈在哪里？怎么定位的？

#### 架构决策
- 新架构方案是什么？
- 为什么选择这个方案而不是其他方案？（对比分析）
- 如何平衡技术债清理和业务迭代？

#### 推进策略
- 是一次性重构还是渐进式演进？为什么？
- 如何在不影响线上业务的情况下完成迁移？
- 如何做灰度验证？

### R - 结果
- 性能指标变化
- 稳定性提升
- 开发效率提升
- 成本变化

### R - 反思
- 架构决策的得与失
- 技术债管理的经验
```

---

### ✨ 填好的范例（脱敏虚构，照着改你的数据）

```markdown
## 项目名称：推荐系统从规则到实时化的架构演进——QPS 从 2K 涨到 5 万，延迟 P99 < 80ms

### S - 背景
- 系统现状：原有推荐是"规则 + 离线跑批"，每天凌晨算好 top-N 写入 Redis，
  用户请求时直接读。QPS 到 2K 就开始抖动，延迟 P99 300ms+。
- 为什么演进：业务量半年涨 4 倍，且运营要上"实时个性化"（用户刚浏览过的
  商品要立刻影响推荐），离线跑批根本做不到。
- 业务压力：DAU 从 50 万涨到 200 万，推荐位 CTR 直接挂钩广告收入。

### T - 任务
- 我的角色：架构师，带 6 人后端 + 算法混合组。
- 目标：6 个月内完成实时化改造，支撑 QPS 5 万，P99 < 100ms，
  推荐位 CTT 提升 ≥15%，且迁移期间业务零中断。

### A - 行动

#### 架构分析
- 对老系统做了链路追踪，瓶颈定位：Redis 单点 + 规则引擎同步计算
  吃 CPU。70% 的延迟卡在规则引擎。

#### 架构决策
- 新架构：实时特征 + 模型推理分离。
  - 特征层：Flink 实时算用户行为特征，写 HBase。
  - 模型层：召回（向量）+ 排序（精排）两段式，模型服务独立部署。
- 为什么不用纯 Lambda（批+流双链路）？
  → "团队规模 hold 不住两套链路一致性，选了纯 Kappa（全走流），
    用 Flink checkpoint 保证一致性。事后看是对的——运维成本低很多。"
- 如何平衡技术债和业务迭代？
  → "70/30 原则：70% 资源做新架构，30% 维持老系统平稳过渡。
    老系统不重构，只保证能撑到迁移完成。"

#### 推进策略
- 渐进式演进，不是推倒重来：
  - 第 1-2 月：新链路并行跑（影子流量），对比老链路效果
  - 第 3-4 月：10% → 50% 灰度，每档观察 3 天
  - 第 5-6 月：全量切换 + 老系统下线
- 灰度按用户 ID hash 分桶，能随时回退。

### R - 结果
- 性能：QPS 2K → 5 万（25 倍），P99 延迟 300ms → 78ms。
- 业务：推荐位 CTR +22%（超目标），广告收入季度环比 +18%。
- 稳定性：迁移期间 0 故障，老系统优雅下线。
- 复用：特征平台后来给搜索、风控团队复用，成了公司级基础设施。

### R - 反思
- 架构决策的得：选 Kappa 而非 Lambda，省了维护两套链路的复杂度。
- 架构决策的失：特征平台一开始没设计多团队复用接口，后来给搜索复用
  时返工了 3 周。下次会一开始就按平台标准建。
- 技术债管理：30% 资源维稳老系统的决策，事后看是关键——给了团队
  试错空间，不用"边开车边换轮子"。
```

💡 **数字怎么找**：
- QPS/延迟：找 SRE 或看 Grafana 历史监控截图
- 业务指标（CTR/收入）：找产品或数据分析师要报表
- 迁移过程数据：灰度记录、故障单、复盘文档

🎯 **这个故事讲完，面试官会追问**：
1. "灰度时发现新链路效果比老的差，怎么决策？" → 数据驱动 + 快速定位 + 必要时回退
2. "Flink checkpoint 失败导致状态不一致怎么办？" → 监控 + 幂等 + 兜底重算
3. "实时特征和离线特征口径不一致怎么解决？" → 特征平台统一口径 + 离线在线对齐校验
4. "为什么不上更复杂的深度模型？" → ROI，当时特征工程收益更大（参考附录 B.21 技术选型）

---

## 故事三：团队管理/技术推进项目

### 填写模板

```markdown
## 项目名称：[你的项目名称]

### S - 背景
- 团队情况：[团队规模、能力分布、当时的状态]
- 面临的挑战：[技术攻坚/跨团队协作/组织变革]

### T - 任务
- 我的角色：[Tech Lead/技术管理者]
- 管理目标：[团队建设/能力提升/流程优化]

### A - 行动

#### 团队建设
- 如何搭建/优化团队？
- 人才画像是什么？怎么招聘？
- 如何培养团队成员？

#### 技术管理
- 建立了哪些技术规范和流程？
- 如何做技术评审和 Code Review？
- 如何管理技术债？

#### 跨团队协作
- 如何与产品/业务/其他技术团队对齐？
- 如何推动技术决策达成共识？
- 如何处理技术方案分歧？

### R - 结果
- 团队成长指标
- 交付效率提升
- 技术影响力

### R - 反思
- 管理风格的调整
- 最有成就感的一件事
- 最想改进的一件事
```

---

### ✨ 填好的范例（脱敏虚构，照着改你的数据）

```markdown
## 项目名称：从 0 搭建 8 人 AI 小组——半年内交付 3 个线上场景

### S - 背景
- 团队情况：公司原有算法团队 2 人（偏研究），业务要做推荐、搜索、
  智能客服三件事，人力严重不足，CTO 让我从 0 组建 AI 小组。
- 面临的挑战：① 没人——AI 人才市场抢手；② 没平台——零基础设施；
  ③ 业务方对 AI 期望虚高，以为一个月就能做出来。

### T - 任务
- 我的角色：Tech Lead，直接汇报 CTO。
- 管理目标：6 个月内招到 8 人，搭建基础平台，交付 3 个线上场景
  （推荐、搜索、客服），同时把业务方预期拉到合理水位。

### A - 行动

#### 团队建设
- 人才画像：明确要"能把模型跑上线"的工程师，不是顶会论文作者。
  四类角色：2 ML 工程师 + 2 平台工程师 + 2 数据工程 + 1 算法 Lead + 1 应届。
- 怎么招：
  - 核心岗（算法 Lead、平台架构师）外部挖，给期权 + 技术自由度；
  - 2 个后端内部转岗做 MLOps，他们的基础设施经验比纯算法背景更值钱；
  - 应届从合作高校实验室直接锁定。
- 培养：每人入职 2 周配 mentor，第 1 个月跑通一个完整小项目再上核心模块。

#### 技术管理
- 建了三套规范：技术方案评审（>3 人天必评）、代码 review（双人 + 自动化）、
  上线 checklist（含回滚预案）。
- 技术债：每迭代固定 20% 时间还债，高息债务（影响稳定性的）优先。
- AI 项目特殊管理：用 Spike 机制——不确定的探索固定 2 周时间盒，
  到点 Go/No-Go 评审，避免"无限优化"。

#### 跨团队协作
- 拉齐预期：和业务方分级定义目标——L1 FAQ 先上（1 月），L2 复杂意图二期（2 月），
  L3 多轮对话三期。不说"做不到"，说"这个要 3 个月，先上 MVP 看效果"。
- AI 工程师去客服团队旁听一周，理解真实痛点再设计——这个动作后来被证明
  是项目成功的关键。

### R - 结果
- 团队：8 人到位（1 人内部转岗失败补招），6 个月内稳定运行。
- 交付：3 个场景全部上线，推荐 CTR +18%，搜索相关性 +25%，客服一次解决率 +17%。
- 沉淀：特征平台 + 模型部署平台成了公司基础设施，给后续团队复用。
- 影响力：流程规范被其他技术组采纳，我晋升一级。

### R - 反思
- 管理风格调整：早期我太"亲力亲为"，关键技术决策都自己拍，
  后期学会把决策权下放给算法 Lead，我只 review 重大决策。
- 最有成就感：2 个内部转岗的同事成长为 MLOps 骨干，
  比外部挖人成本低、忠诚度高、业务理解深。
- 最想改进：技术规划应该更早和业务对齐，中期返工了一次
  因为业务方改了优先级。下次用 OKR + 双周对齐机制。
```

💡 **数字怎么找**：
- 团队规模/招聘周期：翻 HR 系统记录、组织架构变更
- 交付指标：项目复盘文档、OKR 总结
- 影响力：晋升记录、流程被采纳的证据（邮件/文档）

🎯 **这个故事讲完，面试官会追问**：
1. "8 个人里如果只能保留 3 个，你留谁？" → 考察人才判断（参考附录 B.22）
2. "内部转岗失败那个，你怎么处理的？" → 真实管理经验，体现处理人的能力
3. "你怎么和业务方处理优先级冲突？" → 具体例子，参考附录 B.20
4. "你怎么培养应届生？" → 培养机制，体现长期主义

---

## 面试官高频追问清单

针对 LLM 项目，面试官可能追问的问题——提前准备答案：

### 技术深度追问
1. "你们 RAG 系统的准确率是多少？怎么定义的准确率？"
2. "Embedding 模型有没有做过微调？效果提升多少？"
3. "上线后遇到过最严重的 bad case 是什么？怎么解决的？"
4. "你们的 evaluation pipeline 具体是怎么设计的？"
5. "成本是多少？做过哪些成本优化？"

### 架构决策追问
6. "为什么不用 XX 方案？（如 Fine-tuning、GraphRAG）"
7. "如果流量增长 10 倍，你的方案怎么扩展？"
8. "你们的技术债主要在哪里？计划怎么处理？"

### 管理相关追问
9. "团队中有人技术方案和你不一致时怎么处理？"
10. "如何向非技术高管汇报 AI 项目的价值？"
11. "你怎么看 AI 项目的 ROI？怎么衡量？"
12. "如果给你 5 个 HC，你会怎么搭建 AI 团队？"

---

## 项目深度追问（Q1-Q5）

> 这 5 道题是面试官对"你作为 Agent 项目负责人"的动机、判断力、自我认知的深度考察。技术深度题前面已经覆盖，这里聚焦"人"的维度——为什么是你、为什么做这个、学到了什么。

### Q1. 你自己平时用不用 coding agent？为什么？

**答案骨架**：
- **每天用**：Claude Code 写代码、Cursor 做重构、WorkBuddy 管理工作流——自己是重度用户才会理解长程任务的痛点
- **dogfooding 是做 Agent 项目的必要条件**：自己不用就感受不到"第 30 步失败怎么恢复""上下文爆了怎么办"这些真实痛点——这些痛点正是产品迭代的输入
- **不用 Agent 的场景**：① 简单脚本（< 50 行直接写更快）② 需要精确控制的性能关键代码（Agent 生成后还要逐行 review，不如自己写）③ 学习新语言/框架时（自己写才能建立 muscle memory）

**面试金句**：
> "做 Agent 不 dogfooding 就是闭门造车。我自己每天用 Claude Code 写代码，每次它犯的错我都记下来进评估集——我踩的坑就是产品的迭代方向。"

🎯 **面试官追问**：
- "用哪个最多？" → Claude Code（代码生成质量 + 长程任务最稳）；Cursor 补充（重构/跳转体验好）
- "不用 Agent 时是什么场景？" → 简单脚本、性能关键代码、学新东西时——Agent 不是万能的，知道什么时候不用更重要

---

### Q2. 做这个项目个人出发点是什么？为什么是现在？

**答案骨架**：
- **痛点驱动**：自己被低效工具折磨——重复写样板代码、跨项目上下文丢失、团队知识无法复用，这些痛点是立项的原动力
- **技术判断**：Agent 是未来 5 年的主战场——基模能力到了临界点（GPT-4o/Claude 3.5），但"让 LLM 可靠干活"的工程层（Harness/可靠性/评估）还远未成熟，这是机会窗口
- **能力闭环**：想做"微调 + RAG + Agent 三件套协同"的完整实践——单一技术（只微调/只 RAG）的天花板已经看到，三件套协同是下一个能力台阶
- **为什么是现在**：基模成熟（2024-2026）+ 开源生态成熟（vLLM/PEFT/UnSloth）+ 市场需求起势（企业都在找 Agent 落地方案）——三个条件同时满足才有窗口期

**面试金句**：
> "出发点是自己的痛点 + 对趋势的判断。痛点是被低效工具折磨烦了；判断是基模到了临界点但工程层未成熟——这是'让 LLM 可靠干活'的最佳窗口期，晚一年就晚了。"

🎯 **面试官追问**：
- "为什么是 Qwen2.5 不是别的？" → 中英双语+代码双强 + 128K 长上下文 + Apache 2.0 零限制 + 迭代最快（详见 Day3 Q40）
- "做了多久了？" → 按实际回答；强调"从 demo 到生产"的跨度（不是只有 demo）

---

### Q3. 相比别人/已有方案（Cursor/Copilot/Claude Code），你的优势是什么？

**答案骨架**：
- **护城河不是"更便宜/更快"——是对手短期追不上的三个深度**：
  1. **垂直深度**：针对内部代码库/工作流微调，比通用 Agent（Cursor/Copilot）更懂业务上下文——通用模型不知道内部系统名、私有 API、团队约定
  2. **数据飞轮**：每次失败 case 进评估集 → 迭代数据 → 重训——用得越多数据越好，形成正循环；竞品拿不到我们的私有数据
  3. **工程化深度**：可观测（全链路 trace）+ 可回滚（Skill/Prompt 版本）+ 可评估（golden set 回归）——不是 demo，是能上生产的系统
- **诚实的边界**：通用代码能力（写公开库/标准算法）不如 Claude Code/Cursor——我们的优势在垂直领域，不是全场景

**面试金句**：
> "优势不是更便宜或更快，是三个深度：垂直微调懂业务、数据飞轮越用越准、工程化能上生产。如果大厂做同样的垂直场景，他们缺的是我们的私有数据和领域 know-how——就像 AWS 做了 Lambda，但垂直 SaaS 不会死。"

🎯 **面试官追问**：
- "护城河到底是什么？" → 私有数据 + 领域 know-how + 数据飞轮正循环——不是模型本身（模型会迭代）
- "如果大厂做同样的事你怎么办？" → 大厂做通用 Agent，垂直领域的领域知识 + 合规 + 私有部署是护城河；参考垂直 SaaS vs 云厂商的共存逻辑

---

### Q4. 希望从这个项目学到什么？

**答案骨架**：
- **技术维度**：① 长程任务的可靠性工程（50 步链路怎么保持 95%+ 成功率）② 微调 + RAG + Agent 三件套的协同（不是各自为战）③ Agent 产品的评估方法论（轨迹评估 + 端态评估）
- **工程维度**：数据飞轮怎么闭环——从线上失败 case 到评估集到重训的自动化流水线
- **认知维度**：搞清楚"模型能力 vs Harness 工程"的边界——哪些问题靠更强的模型解决，哪些必须靠工程兜底
- **下一步想学**：Multi-Agent 协作（多个 Agent 分工 + 通信 + 冲突解决）——这是 Agent 的下一站

**面试金句**：
> "最想学的是长程任务的可靠性工程——单步 95% 不难，50 步链路 95%+ 极难。这逼着我搞清楚模型能力和工程兜底的边界——哪些该扔给模型，哪些必须工程解决。这个判断力是 Agent 工程师的核心能力。"

🎯 **面试官追问**：
- "学到最难的是什么？" → 可靠性工程——不是模型不够强，是"怎么让 50 步不出错"的系统性设计（重试/校验/回滚/状态机）
- "下一步想学什么？" → Multi-Agent 协作——分工 + 通信 + 冲突解决，这是 Agent 的下一站

---

### Q5. 项目最大失败/教训是什么？

**答案骨架**：
- **早期迷信"模型够强就行"**：用 GPT-4o 起步，觉得模型强就能搞定一切，忽略了 Harness 工程（可观测/校验/回滚）——结果上线第一周因为死循环烧了 5 万 token 才发现
- **具体事故**：Agent 进入"工具调用失败 → 重试 → 再失败 → 再重试"的死循环，没有最大重试次数限制，一晚上烧光当天 token 预算
- **教训（三层）**：
  1. **先做可观测，再做能力**——没有 trace 就发现不了死循环；先上 Langfuse 全链路 trace，再谈功能迭代
  2. **每个工具调用必须有护栏**——最大重试次数、超时、成本上限，缺一不可
  3. **模型 95% 和工程 95% 是乘法关系**——模型错 5% × 工程没兜底 = 100% 事故；必须工程补模型的错

**面试金句**：
> "最大的教训是早期迷信模型——用 GPT-4o 起步觉得够强了，结果上线第一周死循环烧了 5 万 token。教训是：先做可观测再做能力，每个工具调用必须有护栏。模型 95% 和工程 95% 是乘法不是加法——工程不补位，模型再强也崩。"

🎯 **面试官追问**：
- "怎么发现的？" → 第二天看成本告警才发现——这正好暴露"没有实时可观测"的问题，事后立刻上了 Langfuse 全链路 trace
- "现在怎么防？" → 三层护栏：① 每个工具最大重试 3 次 ② 单任务 token 上限 ③ 全局成本告警；超出任一阈值自动停止 + 人工介入

---

## 练习方法

1. **录音自讲**：用手机录音，完整讲述每个故事（15分钟），然后回听
2. **检查点**：
   - 是否有具体的数字？
   - 是否讲清了"为什么"而不只是"做了什么"？
   - 是否展示了技术判断力和管理能力？
   - 反思部分是否有真诚度？
3. **找朋友模拟面试**：让朋友扮演面试官，讲完后让 TA 追问
4. **精简版准备**：每个故事再准备一个 3 分钟的精简版（用于初面或时间紧张的场景）

---

## 今日产出检查

> **升级版验收**：不是"填完了"，而是"能脱稿讲 + 接住追问"。

| 产出 | 达标标准 | 验收方式 | ✓/✗ |
|------|---------|---------|-----|
| 故事1：LLM 应用 | 完整版（15 min）+ 精简版（3 min） | 录音回放，无卡壳 | ⬜ |
| 故事2：架构演进 | 完整版 + 精简版 | 录音回放，无卡壳 | ⬜ |
| 故事3：团队管理 | 完整版 + 精简版 | 录音回放，无卡壳 | ⬜ |
| 数字准确性 | 每个故事 ≥ 5 个量化数字 | 翻记录核对，无硬编 | ⬜ |
| 追问应对 | 每个故事 ≥ 4 个追问有预案 | 朋友/AI 抽问 | ⬜ |
| 开口练习 | 至少 1 个故事真人模拟讲完 | 朋友反馈"听得懂不枯燥" | ⬜ |

**达标线**：6 项中 ≥ 5 项达标 → Day4 通过。
**未达标处理**：① 卡壳 → 故事结构没理顺，重画 Action 决策树；② 数字记不住 → 写便签，面试前临阵看；③ 太长超时 → 砍掉技术细节，只留 2-3 个关键决策。

**关键提醒**：
- 故事的灵魂是 **Action 里的"为什么"**，不是"做了什么"。面试官听过无数 RAG 项目，记住你的是**你的决策判断**。
- Reflection 不是凑数，**真实教训**最加分。编造的反思面试官一眼看穿。
- 三个故事要**人设一致**——你展现的技术风格、管理风格、价值观不能互相矛盾（终面尤其看这个）。
- 数字宁可给区间（"70% 多到 85% 左右"）也不要硬编精确值，被追问来源时硬编会露馅。
