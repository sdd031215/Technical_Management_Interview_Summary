# Day 13 — 行业场景题 + 行为面试 + 高级系统设计

> 目标：准备行业垂直场景（旅游/酒店+AI）、行为面试题（HR 面）、以及更高难度的系统设计题
> 今天的目标是"补全最后 10%"——让你在任何面试场景下都能从容应对

---

## 🔗 与主文件《3 周版》的联动（避免重复，先读这里）

本文件是 Day13 的**深化执行手册**（行业 + 行为 + 系统设计三主题）：

| 你要做的 | 去哪找 | 本文件补充什么 |
|---------|--------|---------------|
| 查系统设计 8 步模板 | 主文件 **附录 C.3** | ✅ 补 Agent+RAG 融合平台实战 |
| 查行为面答题框架 | 主文件 **附录 B.5**（Q20-Q27） | ✅ 补"为什么离开 10 年公司"等 HR 题 |
| 查反问面试官 | 主文件 **附录 B.6**（5 个反问） | ✅ 补按轮次选反问 |
| 做半日时段练习 | 主文件 **第二周 Day 4-5**（练手面半日表） | ✅ 本文件做"补全最后 10%" |

**核心心法**：行业场景题考的是"把通用 AI 能力落到垂直场景"，不是背方案。

---

## 今日安排

| 时段 | 内容 | 产出 |
|------|------|------|
| 上午 9:00-11:00 | 旅游/酒店行业 AI 场景题 | 行业方案 |
| 上午 11:00-12:00 | 行为面试（HR 面）高频题 | 行为答案 |
| 下午 14:00-15:30 | 高级系统设计（Agent + RAG 融合系统） | 架构设计 |
| 下午 15:30-17:00 | 练手面试 #3 或面试复盘 | 面试记录 |
| 晚上 19:00-20:00 | 综合自测 | 自测记录 |

> **图标约定**：🎯 面试官追问 / ⚠️ 踩坑提示

---

## Part A：旅游/酒店行业 AI 场景题

结合你的行业背景，以下是面试中可能被问到的场景题：

### Q1. 如何为 OTA（在线旅游平台）设计一个智能客服系统？

**答案框架：**

```
用户请求（多渠道：App/Web/微信/电话）
  → 会话管理层（Redis，多轮上下文）
  → 意图路由层
    ├── FAQ Agent → RAG 知识库检索（酒店政策/退改规则/常见问题）
    ├── Order Agent → Function Calling（查订单/改签/退款）
    ├── Recommend Agent → 推荐引擎（酒店/线路/机票个性化推荐）
    └── Complaint Agent → Plan-and-Execute（复杂投诉多步处理）
  → 升级判断（情绪检测 + 复杂度评估 → 人工客服）
  → LLM 生成 + 引用标注 + 幻觉检测
```

**关键技术决策**：
1. **知识库构建**：酒店信息结构化存储（设施/房型/价格）+ 政策文档 RAG
2. **多轮对话管理**：Slot Filling（收集入住日期/城市/人数等必填字段）
3. **人机协作**：AI 处理 60-70% 常规问题，复杂投诉/退款审批升级人工
4. **个性化**：结合用户历史订单和偏好做推荐（"您上次住了 XX，这次推荐类似的 YY"）
5. **多语言**：国际酒店场景需要多语言能力（Qwen2.5 支持 29 种语言）

**量化目标**：
- AI 自动解决率：>60%
- 用户满意度：>4.0/5.0
- 平均响应时间：<3 秒
- 人工客服效率提升：>30%

---

### Q2. 如何用 LLM 提升酒店/旅游的内容生产效率？

**答案：**

**应用场景**：
1. **酒店描述生成**：基于结构化数据（设施、位置、房型）自动生成吸引力文案
2. **用户评价摘要**：对海量用户评论做 Aspect-based 摘要（位置好/服务佳/早餐丰富）
3. **推荐理由生成**：个性化生成"为什么推荐这家酒店"的文案
4. **多语言翻译+本地化**：酒店信息自动翻译为目标市场语言
5. **短视频脚本生成**：基于酒店信息生成探店视频脚本

**技术架构**：
- 模板化 + LLM 生成结合（模板保证信息准确性，LLM 保证文案吸引力）
- A/B 测试框架：对比 AI 生成 vs 人工编辑的点击率和转化率
- 质量把关：AI 生成内容需人工审核后发布（或置信度 > 阈值自动发布）

---

### Q3. 如何设计一个旅游场景的智能搜索系统？

**答案：**

**传统搜索痛点**：
- "找个适合带娃的海边酒店" → 关键词搜索无法理解
- 需要多条件组合（城市+日期+星级+设施+价格）

**LLM 增强搜索**：
```
用户自然语言查询
  → Query 理解（LLM 提取结构化条件）
    提取：{城市:三亚, 日期:7月, 需求:亲子, 设施:泳池+儿童乐园, 星级:4-5星}
  → 混合检索
    ├── 结构化查询（SQL/ES 过滤）
    ├── 向量检索（语义匹配"适合带娃"）
    └── 知识图谱检索（实体关联：三亚→亚龙湾→亲子酒店）
  → Rerank + 推荐理由生成
  → 展示结果 + "为什么推荐" 的 AI 解释
```

---

## Part B：行为面试（HR 面）高频题

### Q1. 为什么离开工作了 10 年的公司？

**答案框架：感恩 + 成长 + 新挑战**

> "首先，我对现在的公司非常感恩——10 年里我从算法工程师成长为技术负责人，经历了公司从小到大的完整过程。但正因为待了足够久，我已经把能学的都学了，能贡献的也到了一个平台期。我想在新的环境中面对不同的技术挑战和业务场景，特别是 LLM 应用这个方向，我希望能在一个 AI 是第一优先级而非辅助角色的公司里，把我的经验发挥到最大价值。"

**关键原则**：
- 不说不满，只说成长和新追求
- 不批评老东家
- 把"10 年"框定为"深度成长"而非"待太久"

---

### Q2. 你的优势和劣势是什么？

**优势（选 2-3 个，配合案例）**：
> "我的核心优势是'技术判断力+落地能力'的组合。很多技术管理者要么只懂方向不懂细节，要么只懂代码不懂业务。我能从架构层面做技术决策，同时也能深入到代码和系统设计中确保落地。比如上一个 RAG 项目，从技术选型到上线都是我带着团队做的。"

**劣势（选一个真实的但非致命的，并说明改进措施）**：
> "我有时候会过于追求技术方案的完美，在早期花太多时间在细节上。我后来学会了'MVP 思维'——先给一个 80 分的方案快速验证，再根据反馈迭代。现在我给团队定的规则是：先用 20% 的时间做一个能跑的版本，再决定要不要花剩下的 80% 去优化。"

---

### Q3. 你的职业规划是什么？

> "短期（1-2 年）：在一个 AI 为核心的公司担任 Tech Lead，把 LLM 应用从 0 到 1 做出来并规模化。中期（3-5 年）：成长为 AI 方向的技术总监或 VP of Engineering，能影响公司的技术战略方向。长期：希望能在 AI 应用领域留下一些有影响力的工作——不管是产品、开源项目还是技术方法论。"

---

### Q4. 你期望的薪资范围？

**策略**：
- 不要先报价，等对方先给范围
- 如果必须先说，给一个范围而非具体数字
- 强调"整体包"而非月薪：基本薪资 + 年终 + 股票/期权 + 福利

> "薪资方面我更看重整体机会和成长空间。如果一定要说范围，基于我的经验和市场行情，我期望的整体包在 XX-XX 之间。但具体数字可以根据岗位的发展空间和整体 package 来讨论。"

---

### Q5. 你有什么问题想问我们？

**好问题（选 3-5 个）**：
1. "这个 AI 团队目前在公司的定位？未来一年的战略优先级？"
2. "如果我入职，前 90 天最重要的交付是什么？"
3. "目前最大的技术挑战是什么？"
4. "团队的技术栈和 GPU 资源状况？"
5. "AI 项目的决策流程？Tech Lead 在技术选型上有多大自主权？"

**不要问**：加班多不多、年假几天、食堂怎么样

---

## Part C：高级系统设计

### 综合设计题：设计一个 Agent + RAG 融合的企业级平台

**题目**："请设计一个支持 RAG 和 Agent 的企业级 AI 平台，需要支持多业务线接入、统一模型管理、统一知识库管理。"

**架构设计**：

```
┌──────────────────────────────────────────────────┐
│                  应用层                            │
│  客服系统 │ 内部助手 │ 内容生产 │ 数据分析 │ ...   │
├──────────────────────────────────────────────────┤
│              AI Gateway（统一接入）                │
│  认证限流 │ 租户隔离 │ 用量计量 │ 内容审核          │
├──────────────────────────────────────────────────┤
│           Agent Orchestration Layer               │
│  LangGraph State Machine                         │
│  ├── RAG Pipeline（检索增强生成）                  │
│  ├── Tool Use Pipeline（API/DB 调用）             │
│  ├── Multi-Agent Pipeline（协作工作流）            │
│  └── Human-in-the-Loop（人工介入节点）             │
├──────────────────────────────────────────────────┤
│              Model Service Layer                  │
│  vLLM (自部署) │ External API │ 模型路由/降级      │
├──────────────────────────────────────────────────┤
│              Knowledge Service Layer              │
│  统一向量库 │ BM25 索引 │ 知识图谱 │ 元数据管理     │
│  多租户隔离 │ 版本管理 │ 增量更新 │ 权限控制        │
├──────────────────────────────────────────────────┤
│              Infrastructure Layer                 │
│  GPU 集群 │ K8s │ Redis │ PostgreSQL │ 监控告警    │
└──────────────────────────────────────────────────┘
```

**关键设计决策**：
1. **多租户知识库隔离**：元数据标签 + 检索层过滤（不依赖 Prompt 约束权限）
2. **统一模型路由**：根据任务复杂度+成本+延迟自动选择最优模型
3. **Agent 工作流可视化**：基于 LangGraph 的 DAG 编排，支持非技术人员配置
4. **用量计量**：按租户/业务线的 Token 消耗、API 调用、知识库查询次数
5. **灰度发布**：新模型/Prompt 版本 5% 流量灰度 → A/B 评估 → 全量

---

## 深度问题补充（行业落地难题）

### Q1. 【行业落地】传统企业（如银行/制造）上 AI，最大障碍是什么？你怎么推进？

**① 原理**：传统企业 AI 落地瓶颈通常不是技术，是**组织 + 数据 + 合规**。技术团队常高估模型能力，低估这三座大山。

**② 三大障碍**：
1. **数据**：数据分散在 10+ 系统（核心账务/CRM/工单），质量差（脏/缺/不一致），无统一治理
2. **合规**：金融/医疗有强监管（数据出境、可解释、审计），AI 黑盒难合规
3. **组织**：业务方不懂 AI（期望虚高），IT 部门求稳（抗拒新事物），缺 AI 文化

**③ 推进策略（5 步）**：
1. **选低风险高价值场景**：内部知识库（不直接对客）起步，而非直接上客户面
2. **数据治理先行**：建数据飞轮，先把一个场景的数据打通再谈模型
3. **合规前置**：和法务/风控提前对齐，设计可解释 + 审计机制
4. **MVP 跑通**：3 个月出可见成果，建组织信心
5. **培养 AI 文化**：业务方 AI literacy 培训，降低期望虚高

**④ Tradeoff**：
- 速度 vs 合规：传统企业不能互联网式"先上后改"，必须合规先行
- 自研 vs 外采：传统企业缺 AI 人才，早期外采 + 内部转化更现实

**⑤ 示例**：
> "我们帮某银行做 AI 客服，最大障碍不是模型，是数据——客户信息散在 5 个系统，格式不一。我们前 2 个月全花在数据治理（统一客户画像 + 知识库清洗），第 3 个月才上模型。合规上和风控提前对齐：所有回复带引用溯源（可审计）+ 高风险操作（转账）必须人审。结果：3 个月 MVP 跑通，但前 2 个月数据治理是关键。判断：传统企业 AI 落地，数据治理占 60% 工作量，模型只占 20%。"

---

### Q2. 【权衡决策】ToB AI 产品和 ToC AI 产品，技术架构有什么本质区别？

**① 原理**：ToB 和 ToC 不是"用户多少"差别，是**需求确定性 + 定制化 + 商业模式**差别，导致架构侧重不同。

**② 架构差异**：

| 维度 | ToC（如 ChatGPT） | ToB（如企业客服） |
|------|------------------|------------------|
| 需求 | 标准化、海量用户 | 定制化、租户隔离 |
| 数据 | 公开/通用 | 企业私有/敏感 |
| 模型 | 大模型通用 | 小模型 + 领域微调 |
| 架构重点 | 高并发、低成本 | 多租户、可定制、合规 |
| 准确率 | 容忍度高（用户体验） | 容忍度低（业务合同） |
| 迭代 | 快（A/B 测） | 慢（需客户确认） |

**③ 生产经验**：
- ToB 必须做**多租户隔离**（数据/配额/模型独立）+ **可定制**（每租户独立 prompt/知识库）
- ToB 准确率要求高——加 Human-in-the-Loop + 引用溯源 + 审计日志
- ToB 商业模式是订阅/调用计费，架构要支持精确计量
- ToC 重吞吐，ToB 重 SLA（合同约束）

**④ Tradeoff**：
- 通用 vs 定制：ToB 要在"平台通用"和"租户定制"间平衡，抽象层 + 配置项
- 成本 vs SLA：ToB 为 SLA 付溢价（独立资源/多活）

**⑤ 示例**：
> "我们从 ToC 转 ToB 踩过坑：原架构为高并发优化（共享资源池），但 ToB 客户要求'我的数据不和别家混'+'99.9% SLA'。重构：①每租户独立知识库 + namespace 隔离 ②VIP 租户独立推理资源（保 SLA）③加审计日志（合规）④每租户独立 prompt 和模型配置（定制）。成本上升但客单价是 ToC 的 100 倍。判断：ToB 不是 ToC 的'企业版'，是不同架构哲学——重 SLA/隔离/定制，不是吞吐。"

---

## 2026 新增高频题（高级系统设计 + 行为）

> 本节补 3 题。Part C 已有系统设计方法论；这里补 2026 高频新场景（多模态客服、Computer Use/Browser Agent）和行为题里的"趋势判断题"。

### 题3：设计一个多模态智能客服（文 + 图 + 语音）

**① 需求澄清**：用户可发文字、图片（拍故障/发票/商品）、语音；客服要理解多模态、查知识库、调工单/CRM、必要时转人工

**② 顶层架构**：
```
模态路由(入口分类) → 预处理(OCR/ASR/Vision encoder)
  → 统一表示(多模态LLM 或 融合层) → RAG(知识库) + Function Calling(工单/CRM)
  → 人工兜底(转人工带完整对话+附件上下文)
```

**③ 核心深挖**：
- **模态路由**：入口分类（文/图/语音/文档），路由到对应预处理
- **模型分级（ROI 关键）**：简单文字走便宜 SLM，复杂多模态走前沿模型——分级是 ROI 所在
- **工具增强**：RAG 知识库 + function calling 工单/履约 API；转人工带全上下文

**④ 横切**：成本（多模态 token 贵，分级控制）、安全（图片注入）、可用性（转人工兜底）

**⑤ 瓶颈与扩展**：多模态推理贵（分级 + 缓存解）；扩展按模态独立部署

🎯 **关键判断**："**多模态客服的 ROI 在模型分级**——80% 简单文字走 SLM，20% 多模态复杂走前沿，整体成本可控。别全量用最贵的多模态模型。"

---

### 题4：设计一个 Computer Use / Browser Agent 生产系统

**① 需求澄清**：Agent 自动操作网页/GUI 完成任务（抓竞品价、自动填表、数据录入），生产可用、多租户、安全

**② 架构**：
```
Orchestrator Agent → Browser Agent 子 Agent
  → 沙箱浏览器(Playwright/CDP) → 动作执行(点击/输入/滚动)
  → 状态/diff 观察循环 → HITL(危险动作确认)
```

**③ 核心深挖（输入模态选型是核心考点）**：
- **纯视觉/截图**（Claude Computer Use）：通用但慢、贵、UI 变了就脆
- **DOM based**：快、稳，但 canvas/SPA 应用会坏
- **accessibility tree**：介于两者之间
- **生产做法**：按页面类型路由（结构化页用 DOM，复杂页用视觉）

**④ 关键挑战**：
- **状态 grounding**："我的点击生效了吗"——观察前后状态 diff
- **错误恢复**：UI 变了/被反爬——重试 + 降级 + HITL
- **会话隔离（多租户）**：每租户独立沙箱浏览器
- **凭证管理**：密码/cookie 安全 vaulting
- **HITL**：删除/付款等危险动作人工确认

**⑤ 横切**：反爬/CAPTCHA、成本（截图 token 贵）、eval（端到端任务成功率 + 单任务成本）

**⑥ 瓶颈**：grounding 可靠性、多步视觉调用成本；扩展靠沙箱浏览器池

🎯 **关键判断**："**Computer Use 现阶段是补充不是主流**——通用但不可靠、贵（视觉调用是 API 的 10-100 倍）。生产落地先做 **Browser Use**（DOM/accessibility 优先，视觉兜底），用于抓取/录入这类窄任务。全 GUI 自主还要等可靠性。"（详见 Day2 Q26 的判断）

---

### 行为题补充：被问"你怎么看 AI Agent 泡沫 / 价值捕获"这类趋势行为题，怎么答？

**① 这类题的本质**：终面/高管面用"趋势题"包装行为题——考你的**成熟度、诚实度、商业判断**，不是技术背诵。

**② 用 STAR+R 框架答（不是纯观点）**：
- **Situation**：绑定你做过的项目（"我们做过 X，发现 Y"）
- **Task**：你面对的判断/决策
- **Action**：你怎么决策（押注了什么、避开了什么）
- **Result**：结果数字
- **Reflection**：你的边界认知（什么适用、什么不适用）

**③ 三条原则**：
1. **分层判断，不一边倒**：别"彻底改变世界"也别"全是泡沫"——给"哪些真实、哪些高估"
2. **绑定实战**：用"所以我们团队开始建 eval 体系/模型路由"体现知行合一
3. **给边界**：什么场景适用、什么不适用——体现成熟度

**④ 示例（答"Agent 泡沫吗"）**：
> "我的判断是分层看（S）：我们做过生产 Agent（T），最难的从来不是搭循环，是让它不闯祸（A）。所以我判断全自主通用 Agent 被高估（还差 3-5 年，可靠性是硬阻塞），但窄域 Agent（编程/客服/数据抽取）在有 eval 和 HITL 下真实可生产（R：我们客服 Agent 可靠性从 70% 到 92%）。所以我把团队精力投在 eval 基础设施和数据飞轮，不是 Agent 框架本身（Reflection：Agent 会商品化，eval+数据+工作流才是护城河）。2026 是证明之年，炒作降温健康。"

⚠️ **踩坑提示**：趋势行为题别只背观点——要绑实战、给数字、给边界，体现"我不仅看，还做了，还反思了"。

---

## 2026 进阶系统设计题（分水岭级）

> 以下 2 道系统设计题是 2026 年大厂终面的高阶题——AI 评测平台和多模态 Agent 客服。考的是"你能不能设计 AI 的质量基础设施和下一代交互系统"。这类题没有标准答案，重点考架构完整性和权衡判断。

### 题 5. 设计一个企业级 AI 评测平台（Golden Set + LLM-as-Judge + Trace 回放）

**题目**："请设计一个企业级 AI 评测平台，支持多个 AI 应用（RAG/Agent/Coding）的持续评估，包含 Golden Set 管理、LLM-as-Judge 自动评分、Trace 回放对比、线上采样评估。日均评估 100 万次。"

**需求澄清**：
- 功能：Golden Set 版本管理、离线评估、在线采样评估、LLM-as-Judge 评分、A/B 对比、Trace 回放、告警
- 非功能：评估本身不能太贵（judge-LLM 成本可控）、评估延迟不影响线上、可扩展多应用
- 约束：支持 RAG/Agent/Coding 三类应用、评估指标可定制、支持人工复核

**架构核心挑战**：

| 挑战 | 方案要点 |
|------|---------|
| Golden Set 管理 | 版本化 + 场景覆盖 + 自动扩充（LLM 反向生成 query） |
| LLM-as-Judge 成本 | 100 万次/天 × judge-LLM 成本 = 不可承受；要采样 + 缓存 + 分级 |
| 评估 vs 线上隔离 | 评估不能影响线上服务；异步 + 独立资源池 |
| 多应用指标差异 | RAG 看 faithfulness、Agent 看轨迹、Coding 看编译——指标引擎要可插拔 |

**架构图**：

```
[线上 AI 应用] → [Trace 采样] → [评估队列]
                                    ↓
                    ┌───────────────┼───────────────┐
                    ↓               ↓               ↓
            [离线评估]        [在线采样评估]    [A/B 对比]
            (Golden Set)     (真实流量采样)    (版本对比)
                    ↓               ↓               ↓
                    └───────────────┼───────────────┘
                                    ↓
                          [LLM-as-Judge 引擎]
                          (分级: 规则→小模型→大模型)
                                    ↓
                          [评估结果存储 + 看板 + 告警]
                                    ↓
                          [Trace 回放 + 人工复核]
```

**关键设计决策**：

1. **Golden Set 管理**：每个应用一个 golden set（100-300 条），版本化（v1/v2），按场景分类（简单/复杂/edge case）。自动扩充：用 LLM 基于文档反向生成 query + 人工校验，降低维护成本
2. **LLM-as-Judge 分级策略**（控成本）：
   - L1 规则判断（格式对不对/长度合规）→ 0 成本，覆盖 30%
   - L2 小模型判断（GPT-4o-mini/Qwen-7B 当 judge）→ 低成本，覆盖 50%
   - L3 大模型判断（GPT-4o/Claude 当 judge）→ 高成本，只对 L2 不确定的 20% 升级
   - 这样 100 万次评估，L3 只跑 20 万次，成本降 80%
3. **在线采样评估**：线上流量采样 5-10% 跑评估（不是全量），用真实流量补充 golden set 的覆盖不足。采样要分层（简单/复杂 query 各采）
4. **Trace 回放对比**：每次应用变更（换模型/改 prompt/改检索）后，回放历史 Trace 对比效果差异。要存"可回放的 Trace"（输入 + 中间步骤 + 输出），不只是结果
5. **告警机制**：评估指标恶化超阈值（faithfulness 掉 5% / 延迟涨 30% / 成本涨 20%）→ 自动告警 + 可触发回滚

**瓶颈与扩展**：
- **瓶颈**：LLM-as-Judge 的算力成本。100 万次/天 × L3 大模型 ≈ $500-2000/天。优化靠分级（L1/L2/L3）+ 缓存（相同 query 的判断结果缓存）+ 采样
- **多应用扩展**：指标引擎可插拔——RAG 注册 faithfulness/recall 指标、Agent 注册轨迹偏离度指标、Coding 注册编译通过率指标。新应用接入只需注册指标 + 接入 Trace

🎯 **面试官追问**：
- "LLM-as-Judge 本身不准怎么办？" → ① 用更强模型当 judge；② 多 judge 投票（3 个 judge 多数表决）；③ 人工校准（采样 5% 人工复核，校准 judge 偏差）；④ 承认有偏差但比纯人工可扩展
- "Golden Set 怎么保持不过时？" → 定期更新（每月加新 case + 淘汰过时 case）+ 从线上 bad case 反哺（用户点踩的 case 加入 golden set）
- "评估平台和 A/B 测试什么关系？" → 评估平台是"质量度量"，A/B 测试是"版本决策"。评估平台提供指标，A/B 测试基于指标做版本对比和灰度决策

⚠️ **踩坑提示**：
- 别忽略"LLM-as-Judge 成本"——100 万次全用大模型当 judge 成本不可承受，必须分级
- "我们用 RAGAS 评估"不够——要讲评估平台化（多应用/在线采样/Trace 回放/告警），不只是跑个工具
- 忽略"在线采样评估"是高频盲点——离线 golden set 覆盖有限，在线采样才能发现真实问题

---

### 题 6. 设计一个多模态 Agent 客服系统（看图 + 对话 + 工具调用）

**题目**："请设计一个多模态智能客服系统，用户可以发文字/截图/语音，Agent 能看图诊断问题、查知识库、调工单系统、给解决方案。日均 50 万次咨询。"

**需求澄清**：
- 功能：多模态输入（文字/截图/语音）→ 理解 + 诊断 + 查知识库 + 调工具 + 多轮对话 + 转人工
- 非功能：P99 < 8s、一次解决率 > 70%、图片识别准确率 > 85%
- 约束：多渠道接入（App/网页/微信）、知识库 10 万文档、要和现有工单系统打通

**架构核心挑战**：

| 挑战 | 方案要点 |
|------|---------|
| 多模态理解 | 文字 + 图片 + 语音统一理解，VLM 看图 + ASR 语音转文字 |
| 图片诊断 | 用户截图质量差/信息不全，VLM 要能"看懂 + 追问" |
| 工具编排 | 查知识库/查订单/建工单/转人工，Agent 动态决策调哪个 |
| 成本控制 | VLM 贵 + 多轮对话 token 多，要控成本 |

**架构图**：

```
用户输入(文字/截图/语音)
        ↓
[多模态预处理] → 语音→ASR→文字 / 图片→裁剪+OCR+VLM描述
        ↓
[意图理解 + 路由] → 简单FAQ / 图片诊断 / 订单查询 / 工单 / 转人工
        ↓                    ↓                ↓
[FAQ: RAG直接答]   [图片诊断: VLM看图+知识库]  [订单: SQL查询]
        ↓                    ↓                ↓
        └────────[Agent编排: 多轮对话 + 工具调用 + 追问]
                          ↓
                [解决方案生成 + 引用溯源]
                          ↓
              [满意度检测] → 解决→结束 / 未解决→追问 or 转人工
```

**关键设计决策**：

1. **多模态预处理降成本**：语音走 ASR 转文字（不走语音 Agent，成本太高）；图片先 OCR + 关键区域裁剪 + VLM 生成结构化描述（"错误码 E1024，位于屏幕中央"），后续推理基于文字描述而非原图——成本降 70%
2. **意图路由是关键**：不是所有咨询都要走 Agent——FAQ 类走 RAG（快、便宜）、订单类走 SQL 查询、图片诊断类走 VLM + Agent、复杂问题才走完整 Agent。路由准确率决定整体效率
3. **图片诊断的"追问"机制**：VLM 看图后如果信息不足（如截图没截全），Agent 主动追问"能再截一张包含 XX 信息的图吗？"——不要在信息不足时硬猜
4. **工具调用与工单系统集成**：Agent 能调"查订单/建工单/查进度/转人工"工具。建工单要 HITL（Agent 草拟工单内容，客服确认后提交）
5. **满意度检测 + 转人工**：每轮对话后检测"问题解决了吗？"——用户表示解决→结束；未解决→追问或转人工。转人工时要带上下文摘要（不让用户重复说）

**瓶颈与扩展**：
- **瓶颈**：VLM 推理延迟（图片诊断 5-10s）。优化：图片预处理降 token + VLM 结果缓存（相同截图命中缓存）+ VLM + 文本模型分工（VLM 只做视觉理解，推理交给文本模型）
- **成本控制**：① 意图路由（70% 走 RAG/SQL 不走 VLM/Agent）；② VLM 结果缓存（常见报错截图命中率 30%+）；③ 多轮对话上下文压缩（只保留关键信息）
- **准确率保障**：① 知识库 golden set 回归（每次知识库更新跑评估）；② 图片诊断人工抽检（5% 抽样人工复核）；③ 用户点踩反馈闭环（点踩的 case 加入 bad case 库）

🎯 **面试官追问**：
- "VLM 看图不准怎么办？" → ① VLM + OCR 互补（VLM 给视觉理解，OCR 给精确文字）；② 低置信度追问（VLM 不确定时让用户补充信息）；③ 人工兜底（高风险场景转人工）
- "多轮对话上下文怎么管？" → 图片只存结构化描述（不存原图 token）；对话历史滑窗 + 摘要；转人工时生成上下文摘要给客服
- "怎么衡量'一次解决率'？" → 用户结束后不再来（同问题 24 小时内不再咨询）= 解决；通过用户反馈"问题解决了吗" + 行为信号（不再来/点击解决方案）综合判断

⚠️ **踩坑提示**：
- 别把多模态客服讲成"VLM + RAG"——要讲意图路由（70% 不走 VLM）和成本控制
- "用户发图就给 VLM 看"是性能灾难——要先预处理（OCR + 裁剪 + 描述），VLM 只看必要部分
- 忽略"追问机制"是高频盲点——信息不足时硬猜是幻觉来源，要主动追问

---

## 今日自测清单

> **升级版自查**：Day13 三主题，行业 + 行为 + 系统设计都要达标。

| 题 | 自查问题 | 达标标准（开口讲） | ✓/✗ |
|----|---------|-------------------|-----|
| 行业场景 | OTA 智能客服架构 | 能讲清意图路由 + 升级判断 + 个性化 | ⬜ |
| 行业场景 | LLM 内容生产 | 能讲清生成 + 审核 + 合规闭环 | ⬜ |
| 行为面 | 为什么离开 10 年公司 | 能讲成"持续成长"不是"待太久" | ⬜ |
| 行为面 | 优势/劣势 | 真实非致命，劣势有改进动作 | ⬜ |
| 反问 | 5 个反问 | 按面试轮次选 2-3 个 | ⬜ |
| 系统设计 | Agent+RAG 融合平台 | 能画完整架构 + 多租户隔离 | ⬜ |
| 系统设计 | 关键决策 | 能讲清模型路由 + 灰度 + 用量计量 | ⬜ |
| 练手面 | 第三场复盘完成 | 共性问题识别 + 改进 | ⬜ |

**达标线**：8 题中 ≥ 6 题达标 → Day13 通过。
**未达标处理**：
- 行业场景答得泛 → 必须结合行业具体业务（OTA 的退改规则/酒店设施结构化）
- "为什么离开"答不好 → 主文件末尾"最后提醒"有金句，背下来
- 系统设计画不全 → 主文件附录 C.3 八步模板 + B.17/B.18 交叉练

**关键提醒**：
- "为什么离开 10 年公司"是必问题——参考主文件末尾"最后提醒"，讲成**持续成长**不是包袱
- 行业场景题要**结合行业具体业务**，不是套通用 RAG 方案
- 练手面 #3 是练手期最后一场，三场横向对比找共性问题
- 主文件附录 **B.5**（行为题）+ **C.3**（系统设计）务必交叉对照
