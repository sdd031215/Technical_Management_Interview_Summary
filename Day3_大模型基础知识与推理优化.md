# Day 3 — 大模型基础知识与推理优化

> 目标：掌握 Transformer 核心原理、模型选型策略、推理优化技术、微调方案，能在面试中展示对大模型底层的深度理解
> 建议时间分配：上午 3h 精读问答 + 下午 2h 重点深挖 + 晚上 1h 自测

---

## 🔗 与主文件《3 周版》的联动（避免重复，先读这里）

本文件是 Day3 的**深化执行手册**。大模型原理在主文件附录：

| 你要做的 | 去哪找 | 本文件补充什么 |
|---------|--------|---------------|
| 查模型选型/推理优化 | 主文件 **附录 A.3**（五个子节） | ❌ 不重复 |
| 查大模型高频面试题 | 主文件 **附录 B.3**（Q13-Q16） | ✅ 补面试官追问 + 踩坑 |
| 查微调决策树 | 主文件 **附录 A.3.4** | ✅ 补"何时微调"现场答法 |
| 查长上下文 vs RAG | 主文件 **附录 B.16** | ✅ 补趋势判断 |
| 做半日时段练习 | 主文件 **第一周 Day 4**（已含本主题半日表） | ✅ 本文件做"题库深化" |

**阅读顺序建议**：先过主文件附录 A.3 建立框架 → 用本文件做面试场景演练。

---

## 今日学习路线

| 时段 | 内容 | 产出 |
|------|------|------|
| 上午 9:00-12:00 | 精读 Q1-Q10（Transformer/模型选型/推理优化/微调） | 技术笔记 |
| 下午 14:00-16:00 | 精读 Q11-Q20（Prompt Engineering/评估/部署/幻觉/长上下文） | 对比表格 |
| 晚上 19:00-20:00 | 闭卷自测 + 薄弱点标记 | 自测记录 |

> **图标约定**：🎯 面试官追问 / ⚠️ 踩坑提示 / 💬 示例答

---

## 核心面试题详解

### Q1. Self-Attention 的计算流程是什么？为什么要除以 sqrt(d_k)？

**答案：**

1. **线性投影**：输入 X 分别通过三个线性变换得到 Q、K、V
2. **注意力分数**：S = Q * K^T，shape 为 [n, n]
3. **缩放**：S_scaled = S / sqrt(d_k)
4. **Softmax**：A = softmax(S_scaled)
5. **加权求和**：Output = A * V

**除以 sqrt(d_k) 的原因**：当 d_k 较大时，点积方差随维度线性增长，导致 softmax 进入梯度极小的饱和区（输出接近 one-hot），梯度几乎为零无法学习。除以 sqrt(d_k) 将方差归一化为 1，保持 softmax 在梯度敏感区间。

**复杂度**：时间和空间均为 O(n^2 * d)，是长上下文的核心瓶颈。

🎯 **面试官追问**：
- "除以 sqrt(d_k) 而不是 d_k，为什么？" → 除 sqrt 把方差归一到 1，除 d_k 会过度缩小；数学上是标准差归一
- "Flash Attention 改变了 attention 的复杂度吗？" → 没改变 O(n²) 计算复杂度，但把显存从 O(n²) 降到 O(n)，靠 IO 优化提速
- "Multi-Query Attention 怎么减少 KV Cache？" → 多个 Q 头共享同一组 K/V，KV 体积降到 1/h
- 详细见主文件附录 **A.3.2**

⚠️ **踩坑提示**：
- 这是送分题也是送命题——答不上 sqrt(d_k) 直接暴露基础不牢
- 别只背流程——要能讲清"为什么这么做"，不是"怎么做"
- 复杂度 O(n²) 要能延伸到"长上下文瓶颈怎么解决"（见 Q15）

---

### Q2. 多头注意力的设计动机？MHA vs GQA vs MQA 的权衡？

**答案：**

**设计动机**：单头注意力只能在一个子空间中建模关系。多头将 Q、K、V 投影到 h 个子空间独立计算，拼接后线性变换。不同头可以关注不同语义维度。

**工程变体**：

| 方案 | KV 头数 | KV Cache 大小 | 推理速度 | 表达力 |
|------|---------|--------------|---------|--------|
| **MHA** | 与 Q 头相同 | 最大 | 基准 | 最强 |
| **GQA** | Q 头数 / 组数（如 8 组） | 缩小到 1/4~1/8 | 更快 | 接近 MHA |
| **MQA** | 1（全部共享） | 缩小到 1/h | 最快 | 略有损失 |

Llama 2 70B、Qwen2 等主流模型均采用 GQA。

---

### Q3. 位置编码有哪些主流方案？RoPE 为什么成为当前主流？

**答案：**

| 方案 | 类型 | 优点 | 缺点 |
|------|------|------|------|
| Sinusoidal | 绝对 | 简单 | 外推能力有限 |
| 可学习 | 绝对 | 灵活 | 完全无法外推 |
| ALiBi | 相对 | 有一定外推性 | 长文本偏置爆炸 |
| **RoPE** | 相对 | 外推性好、数学优雅 | 高维实现需技巧 |

**RoPE 详解**：将位置编码为旋转角度，Q(m) 和 K(n) 的点积自然只与位置差 (m-n) 有关。

**成为主流的原因**：相对位置编码更符合语言建模本质；通过调整 theta 基频可外推（NTK-aware scaling、YaRN）；与注意力机制无缝融合，不引入额外参数。

---

### Q4. 企业级场景中，开源 vs 闭源模型如何选型？

**答案：**

| 维度 | 闭源（GPT-4o/Claude） | 开源（Llama/Qwen/DeepSeek） |
|------|------|------|
| 数据安全 | 数据需上传第三方 | 私有化部署，数据不出域 |
| 定制化 | 只能 Prompt/Few-shot | 支持全量微调、LoRA |
| 成本结构 | 按 Token 计费，高并发线性增长 | 一次性 GPU 投入，边际成本低 |
| 能力上限 | 通常代表最高水平 | 快速追赶，部分场景持平 |

**参数规模选择**：
- **7B 以下**：端侧部署、简单分类/抽取
- **7B-14B**：企业应用主力（RAG、对话、代码生成）
- **30B-72B**：复杂推理、多步规划
- **100B+**：前沿研究，通常 MoE 架构

**关键原则**：垂直领域中，精调过的 7B 往往优于未微调的 70B。

🎯 **面试官追问**：
- "你怎么看开源追上闭源这个趋势？" → 在中文/特定场景已接近或持平，但通用能力和工具调用闭源仍领先；要给判断不是站队
- "数据合规场景必须开源，但开源能力不够，怎么办？" → 开源底座 + 领域 SFT + RAG 补事实 + 工具调用补能力
- "7B-14B 是主力，你怎么调参让它接近 70B 效果？" → 领域 SFT + DPO 对齐 + 强 prompt + RAG，组合拳
- 详细见主文件附录 **A.3.1** 和 **B.3**

⚠️ **踩坑提示**：
- 别绝对化"开源一定追上闭源"或反之——给**场景化判断**
- "精调 7B 胜过未微调 70B"要标前提（垂直领域）
- 别忽视合规/成本/延迟这三条选型硬约束

---

### Q5. 主流开源大模型（Llama、Qwen、GLM、DeepSeek）各自的技术特点？

**答案：**

| 模型 | 技术特点 | 适用场景 |
|------|---------|---------|
| **Llama 3/4** | RMSNorm+RoPE+SwiGLU+GQA；英文极强；生态最完善 | 英文任务首选 |
| **Qwen2.5** | 29 种语言；128K 上下文；代码/数学突出；0.5B-72B 全系列 | 中文场景首选 |
| **GLM-4** | GLM 架构（自回归填空）；中文理解强；工具调用好 | 中文对话和 Agent |
| **DeepSeek-V2/V3** | MoE (236B/21B active)；MLA 压缩 KV；极高性价比 | GPU 有限但需要强推理 |

**架构共性**：RMSNorm + Pre-Norm + SwiGLU（Llama/Qwen），DeepSeek 独创 MLA 进一步压缩 KV Cache。

---

### Q6. 模型量化的主流方案（GPTQ、AWQ、GGUF）有什么异同？

**答案：**

| 维度 | GPTQ | AWQ | GGUF |
|------|------|-----|------|
| 策略 | 二阶信息（Hessian），逐层补偿 | 激活值分布识别重要权重 | 后训练量化，CPU 友好 |
| 校准数据 | 需要 ~128 条 | 需要 ~128 条 | 不需要 |
| 精度 | INT4 优秀 | INT4 略优（保护重要权重） | 略低于前两者 |
| 运行环境 | GPU (CUDA) | GPU (CUDA) | CPU 优先 |
| 推理框架 | vLLM、TGI | vLLM、TGI | llama.cpp、Ollama |

**选型**：服务端 GPU→AWQ/GPTQ + vLLM；本地/边缘→GGUF + llama.cpp；快速实验→GGUF

4-bit 量化通常可将显存需求降低约 75%，推理速度提升 2-4 倍。

---

### Q7. vLLM 和 PagedAttention 的核心原理？

**答案：**

**问题**：传统推理为每个请求按 max_seq_len 预分配连续 KV Cache，平均浪费 60-80% 内存。

**PagedAttention 方案**（借鉴操作系统虚拟内存分页）：
1. KV Cache 划分为固定大小 block（如 16 tokens），不需物理连续
2. block table 映射（类似页表）
3. 按需分配，消除内部碎片
4. Copy-on-Write：共享前缀的请求可共享 KV block

**vLLM 优势**：PagedAttention + Continuous Batching + Tensor Parallelism，相比原生推理通常 2-4 倍吞吐提升。

🎯 **面试官追问**：
- "PagedAttention 的 block size 怎么定？" → 通常 16 tokens，小则元表开销大，大则碎片回升
- "Copy-on-Write 共享前缀，多个请求改了某个 token 怎么办？" → 触发 COW，复制该 block 再修改，其他请求继续共享原 block
- "vLLM 和 SGLang 区别？" → vLLM 通用强；SGLang 的 RadixAttention 对多轮/结构化场景更优
- 详细见主文件附录 **A.3.2** 和 Day9 Q1/Q2

⚠️ **踩坑提示**：
- 别把 PagedAttention（显存）和 Continuous Batching（调度）混为一谈——面试官爱设这个陷阱
- "2-4 倍提升"要标"高并发场景"，单并发无差异
- 别忘了说 vLLM 的局限（如对某些量化支持不全）体现工程判断

---

### Q8. Continuous Batching vs Static Batching 优势在哪？

**答案：**

- **Static Batching**：等 batch 内所有请求完成才开始下一批，短请求等待长请求，GPU 利用率低
- **Continuous Batching**：token 级别动态调度，完成即移出、新请求即插入

**优势**：GPU 利用率从 30-50% 提升到 80%+，在线场景吞吐提升 2-4 倍，P99 延迟更可预测。

**进阶优化**：Chunked Prefill（长 prefill 切分与 decode 交错）、Prefix Caching（共享 system prompt）、Priority Scheduling

---

### Q9. LoRA 和 QLoRA 的原理？关键超参如何选择？

**答案：**

**LoRA**：冻结 W_0，学习两个低秩矩阵 A (d×r) 和 B (r×d)，W = W_0 + B*A。训练参数从 d^2 降到 2dr。

**关键超参**：
| 超参 | 建议值 |
|------|--------|
| rank (r) | 简单任务 4-8，复杂 16-64 |
| alpha | r 的 1-2 倍 |
| target_modules | q_proj, v_proj（基础）；全部线性层（进阶） |
| 学习率 | 1e-4 ~ 3e-4 |

**QLoRA 三项关键技术**：
1. NF4 量化（信息论最优，匹配权重正态分布）
2. 双重量化（量化常数本身再量化）
3. 分页优化器（GPU 显存不足时自动换出到 CPU）

效果：QLoRA 让大模型微调显存门槛大降——单张 80GB 卡（A100/H100）可微调 65B；
4090（24GB）适合 7-13B 模型的 QLoRA 微调（NF4 下 65B 权重约 32.5GB，4090 放不下）。性能损失 <0.5%。

🎯 **面试官追问**：
- "LoRA 的 rank 太小会怎样？太大呢？" → 太小欠拟合（记不住），太大过拟合 + 失去参数高效优势；一般 8-64
- "LoRA 一般加在哪些层？" → 基础只加 q_proj/v_proj，进阶加全部线性层（k/o/gate/up/down）
- "LoRA 和 Adapter 区别？" → Adapter 加额外层增加推理延迟，LoRA 训练时用推理时可合并进 W 不增延迟
- "什么时候该微调？" → **高频题**，背主文件附录 **A.3.4** 微调决策树
- 详细见主文件附录 **A.3.4**

⚠️ **踩坑提示**：
- rank/alpha 不要硬记——要讲清"alpha 一般是 rank 的 1-2 倍，控制更新幅度"
- 别说"LoRA 一定比全量好"——全量上限更高，LoRA 是性价比选择
- 漏说"防灾难性遗忘"是减分项——微调后通用能力可能下降，要留回归集

💬 **示例答**：
> "微调我先过一道决策树：能不能用 prompt + RAG + few-shot 解决？能就别微调。必须微调时，格式风格走 SFT（首选 LoRA），偏好对齐走 DPO，领域知识优先 RAG。LoRA 我一般 rank 用 8-16，alpha 是 rank 的 2 倍，target_modules 加全部线性层。关键是防灾难性遗忘——必须留通用评估集做回归，不然微调完领域好了通用崩了。QLoRA 让我们在单卡 A100 80GB 上微调 65B，4090 跑 7-13B，资源友好。"

---

### Q10. RLHF 和 DPO 的原理和区别？

**答案：**

**RLHF 三步**：SFT → 训练奖励模型 → PPO 策略优化
- 痛点：4 个模型驻留显存、PPO 不稳定、工程复杂

**DPO**：数学证明奖励函数可被最优策略解析解替代，直接用偏好数据优化策略，绕过奖励模型。

| 维度 | RLHF | DPO |
|------|------|-----|
| 复杂度 | 高（4 个模型） | 低（2 个模型） |
| 稳定性 | 差 | 好 |
| 显存 | 极高 | 约 RLHF 的 50% |
| 效果 | 略优 | 接近 RLHF |

**建议**：除非有充分工程能力和资源，否则优先选 DPO。

🎯 **面试官追问**：
- "DPO 为什么更稳定？" → 直接用偏好数据优化策略，解析解，没有 PPO 的 reward model + 4 个模型在显存的复杂度
- "DPO 的偏好数据怎么造？" → 人工标注 chosen/rejected 对、用更强模型生成候选再人工选、从用户反馈挖掘
- "DPO 效果不如 RLHF 怎么办？" → 先 DPO 打底，关键场景叠加 PPO；或用 ORPO 等改进版
- "现在还有比 DPO 更新的对齐方法吗？" → ORPO、KTO、SimPO，要体现前沿敏感度
- 详细见主文件附录 **A.3.4**

⚠️ **踩坑提示**：
- 别说"DPO 完全替代 RLHF"——RLHF 效果上限略高，DPO 是性价比之选
- 偏好数据质量比算法更重要——"garbage in garbage out"
- 别忽视数据构造成本——很多人只讲算法不讲数据

---

### Q11. CoT、Self-Consistency、ToT 分别是什么？如何选用？

**答案：**

- **CoT**：逐步推理，"Let's think step by step"。适合数学/逻辑推理。
- **Self-Consistency**：多次 CoT 采样 + 多数投票。提升 3-8 个百分点。
- **ToT**：树搜索，每步生成多个候选并评估，BFS/DFS 探索。适合需要回溯的复杂任务。

**选用**：简单任务→标准 Prompt → CoT → Self-Consistency → ToT（按需升级）

---

### Q12. 大模型的评估体系包括哪些维度？

**答案：**

**四层架构**：
1. **标准 Benchmark**：MMLU、GSM8K、HumanEval、TruthfulQA、MT-Bench、C-Eval
2. **业务离线评估**：golden test set + BLEU/ROUGE/BERTScore + LLM-as-Judge
3. **在线评估（A/B Test）**：灰度 5-20% 流量，用户满意度、任务完成率、延迟
4. **人工评估**：标准化评分表，标注一致性 Cohen's Kappa > 0.7

---

### Q13. 大模型生产级部署需要考虑哪些因素？

**答案：**

**多 GPU 并行策略**：
- TP（Tensor Parallelism）：单层矩阵切分，降低延迟
- PP（Pipeline Parallelism）：不同层分配到不同 GPU
- DP（Data Parallelism）：完整模型副本分担请求
- EP（Expert Parallelism）：MoE 专用

**成本优化**：量化部署、模型蒸馏、请求分级（路由到小/大模型）、Prefix Caching、Spot 实例、批处理优化

---

### Q14. 大模型幻觉的成因和系统性解决方案？

**答案：**

**三类幻觉**：事实性（与事实不符）、忠实性（偏离输入）、逻辑性（推理错误）

**成因**：训练数据噪声、LM 优化的是"看起来像"而非"是否正确"、过度自信、注意力分散

**多层防御**：
- 训练：RLHF/DPO 对齐
- 推理：RAG 提供事实依据、Self-Verification、多路采样一致性检查
- 后处理：事实核查模块
- UI：展示置信度

---

### Q15. 长上下文处理：RoPE 扩展和 KV Cache 优化？

**答案：**

**RoPE 扩展方案**：线性插值、NTK-aware Scaling、YaRN、Dynamic NTK

**KV Cache 优化**：
- GQA/MQA：减少 KV 头数
- PagedAttention：消除碎片
- KV Cache 量化：FP16→INT8，减少 50% 显存
- Sliding Window / Sparse Attention
- H2O（动态驱逐不重要 KV）
- Ring Attention（分布式注意力）

**工程建议**：
- 4K-32K：原生长度 + GQA + PagedAttention
- 32K-128K：NTK RoPE 扩展 + KV Cache 量化
- 128K+：Ring Attention 多机分布式

🎯 **面试官追问**：
- "长上下文会取代 RAG 吗？" → **高频趋势题**。长上下文缓解部分需求但①成本②延迟③中段衰减（Lost in the Middle）④知识更新成本⑤权限隔离，仍需 RAG；趋势是融合
- "百万 token 上下文，真的能用满吗？" → 实际"大海捞针"测试中段召回率衰减明显，宣称长度 ≠ 有效长度
- "YaRN 和 NTK-aware 区别？" → NTK 调基频做外推，YaRN 在 NTK 基础上分频段不同策略，效果更好
- 详细见主文件附录 **B.16**（长上下文 vs RAG）

⚠️ **踩坑提示**：
- 别说"长上下文淘汰 RAG"——是融合不是替代
- "支持 128K"不等于"128K 都有效"——要讲中段衰减
- RoPE 扩展方案要能区分线性插值 vs NTK vs YaRN 的差别

---

### Q16-Q20. 综合架构题（五段式扩写）

#### Q16 企业级 RAG 设计

**① 原理**：企业级 RAG 不是"塞文档 + GPT"，而是分层架构——接入、解析、索引、检索、生成、评估、横切七层。
**② 量化**：QPS 5-20、P99 < 5s、可用性 99.9%、知识库百万文档、千万 chunk。
**③ 生产经验**：混合检索（BM25+向量+RRF）+ Cross-Encoder Rerank + 引用约束（防幻觉）+ 元数据权限过滤（多租户）。
**④ Tradeoff**：召回 vs 精度（先 recall 后 rerank）、成本 vs 质量（模型路由）、延迟 vs 准确（Top-K 调参）。
**⑤ 示例**：见 Day6 系统设计题 1 完整架构，及 Day1 Q15 排查方法论。

#### Q17 显存估算（生产必背公式）

**① 原理**：推理显存 = 权重 + KV Cache + 激活值 + 框架开销，其中 **KV Cache 是高并发主要瓶颈**，常被低估。

**② 公式**：
```
权重(FP16): 参数量 × 2B           (70B → 140GB)
KV Cache/请求: 2(K+V) × layers × kv_heads × head_dim × seq_len × 2B(FP16)
  例: Llama2-70B, seq=4096: 2×80×8×128×4096×2 ≈ 1.34GB/请求
  （2× 是 K 和 V 各一份，别漏乘；常见错误是只算一份得到 640MB）
激活值 + 框架开销: 权重的 15-20%
```

**③ 生产经验**：高并发场景 KV Cache 总量 > 权重。10 路并发 × 1.34GB = 13.4GB，看似不大，但 batch=64 时就是 86GB——这就是为什么 PagedAttention 关键。
**④ Tradeoff**：FP16 精度好但显存翻倍；INT4 省 75% 但精度损失；FP8（H100）是平衡点。
**⑤ 示例**：
> "部署 Llama 70B FP16：权重 140GB（2×80GB 卡）+ 64 路并发 KV Cache 约 40GB + 激活/开销 20GB ≈ 200GB，需要 3×80GB 卡 TP。换 FP8：权重压到 70GB 单卡 H100，加 KV Cache 用 PagedAttention 管理，单卡扛 32 路并发。这就是为什么 H100 + FP8 + vLLM 是 70B 部署黄金组合。"

#### Q18 Flash Attention（高频底层题）

**① 原理**：标准 Attention 的瓶颈不是计算量，而是**显存读写（IO）**。中间矩阵 S=QK^T（n×n）要反复在 HBM（高带宽内存）和 SRAM（片上缓存）间搬运。Flash Attention 用**分块 + 在线 Softmax**，让计算在 SRAM 内完成，不写回 n×n 中间矩阵。

**② 数学/数据**：
- 标准 Attention：HBM 读写 O(n²)（要读写 n×n 矩阵多次）
- Flash Attention：HBM 读写 O(n²·d/M)，M 是 SRAM 大小，常数小很多
- 显存：O(n²) → O(n)（不存中间矩阵）
- 速度：2-4 倍（主要省 IO），长序列收益更大

**③ 生产经验**：
- 几乎所有现代推理框架（vLLM、SGLang）默认用 Flash Attention 2
- 不改变 Attention 数学结果，纯 IO 优化，可无缝替换
- 长上下文（>8K）收益最显著，短序列收益小

**④ Tradeoff**：
- 通用性：需要特定实现（CUDA/Triton），早期只支持部分模型
- 灵活性：自定义 attention pattern（如稀疏注意力）Flash Attention 不直接支持

**⑤ 示例**：
> "Flash Attention 的核心洞察是'注意力是 memory-bound 不是 compute-bound'。我们测过 Llama 70B 处理 32K 上下文：标准 attention 12 秒，Flash Attention 2 是 4 秒，3 倍提升。关键是它不改数学，纯 IO 优化，所以 vLLM 默认开。面试时很多人答'减少了计算量'，这是错的——计算量没变，是减少了 HBM 读写。"

#### Q19 MoE（Mixture of Experts）

**① 原理**：MoE 把 FFN 层替换为"多个专家 FFN + 门控路由"，每个 token 只激活 top-K 个专家（通常 2/8 或 2/64）。参数量大但**激活参数少**，推理成本低。

**② 量化**：DeepSeek-V3 671B 总参数，每 token 激活 37B——效果接近 671B 稠密模型，成本接近 37B。
**③ 生产经验**：
- 优势：性价比高（效果/成本比），适合推理密集场景
- 挑战：①负载均衡（避免某专家过载）②显存（所有专家都要加载）③通信开销（Expert Parallel 时）
- 部署：单卡放不下所有专家，需 EP（Expert Parallel）跨卡分布

**④ Tradeoff**：
- 显存 vs 计算：MoE 显存大（存所有专家）但计算少（只激活部分），适合算力贵显存富的场景
- 训练 vs 推理：MoE 训练难（负载均衡、稳定性），推理收益大

**⑤ 示例**：
> "我们评估 DeepSeek-V3 替代 Llama 70B：效果相当甚至更好（671B 参数的优势），但激活参数只有 37B，推理 FLOPs 省 40%。代价是显存——所有专家要加载，需要 8 卡 EP 部署。综合算下来，高并发场景性价比优于稠密 70B。挑战是负载均衡：某些热门专家被打爆，需要 auxiliary loss 或专家冗余。"

#### Q20 Speculative Decoding（投机解码）

**① 原理**：小模型（draft model）快速生成 K 个 token，大模型一次性并行验证这 K 个 token 是否接受。接受的前缀直接用，第一个拒绝点之后的重新生成。**无损加速**（结果和纯大模型一致）。

**② 量化**：接受率高时加速 2-3 倍；接受率取决于大小模型分布相似度。
**③ 生产经验**：
- draft 模型选小但同家族（如 Llama 70B + Llama 8B 作 draft），分布相似接受率高
- 适合低延迟场景（在线 chat），不适合已经 batch 满的场景（GPU 已打满无加速空间）
- 变体：Medusa（多头预测）、EAGLE（更准的 draft）

**④ Tradeoff**：
- 延迟 vs 成本：降延迟但总 FLOPs 略增（draft 也要算）；适合延迟敏感型
- 接受率 vs 灵活性：draft 必须和大模型分布相近，换大模型要重选 draft

**⑤ 示例**：
> "我们在线 chat 场景用 Speculative Decoding：Llama 70B + 8B draft，K=4。8B 生成 4 个 token 约 30ms，70B 验证约 80ms（并行），接受率约 75%。平均每轮省 1-2 次大模型 forward，首字延迟降 40%。关键是 GPU 不能太闲——如果 batch 已经打满，speculative 反而增成本。所以我们只在低峰期开。"

---

## 深度问题补充（高频难题 + 前沿趋势）

### Q21. 【底层原理】RoPE 是怎么实现相对位置的？NTK-aware scaling 为什么能外推？

**① 原理**：RoPE（Rotary Position Embedding）把位置信息编码为**旋转角度**——对 Q 和 K 在二维平面上按位置 m、n 旋转，点积结果 Q(m)·K(n) 自然只依赖相对位置 (m-n)，不依赖绝对位置。

**② 数学直觉**：
- 把 d 维向量按两两一组分成 d/2 个二维平面
- 第 k 组旋转角度 θ_k = m · base^(-2k/d)，base 通常 10000
- Q(m)·K(n) = |Q||K|cos(angle_Q - angle_K + (m-n)·θ_k)，只含相对项 (m-n)

**③ NTK-aware scaling 外推原理**：
- 问题：训练时 base=10000 只见过 4K 位置，外推到 32K 时高频维度旋转过快导致泛化差
- NTK 思路：调整 base（如 10000 → 160000），让**低频维度（大尺度）保持外推能力，高频维度（小尺度）插值**，类似 NT K感知频率
- YaRN 进一步：对不同频段用不同混合系数，效果更好

**④ Tradeoff**：
- 线性插值：简单但损失分辨率（所有频率都缩放）
- NTK：保高频分辨率，外推更好，但需调 base
- YaRN：效果最好但实现复杂

**⑤ 示例**：
> "RoPE 的优雅在于把绝对位置编码变成相对——Q 和 K 旋转后点积自动只依赖位置差。我们做长上下文时用 NTK-aware scaling 把 base 从 10000 调到 160000，4K 训练的模型能外推到 32K 且困惑度几乎不涨。关键认知：不同频率维度要区别对待——低频管宏观位置（要外推），高频管局部细节（要插值）。"

---

### Q22. 【生产实战】模型上线后效果衰减怎么发现？怎么治理？

**① 原理**：模型效果衰减来源——①数据漂移（用户 query 分布变了）②概念漂移（业务规则变了，如新产品上线）③上游模型变更（GPT 升级导致 prompt 效果变化）④对抗攻击。

**② 量化监控指标**：
- **业务指标**：解决率、点踩率、人工升级率（最敏感）
- **质量指标**：定期 golden set 回归、faithfulness 采样分
- **分布指标**：query embedding 分布偏移（KL 散度）、新增 query 类型占比
- **告警阈值**：业务指标周环比降 5% 告警，分布 KL > 0.3 告警

**③ 生产经验治理链**：
1. 发现：监控告警 → 人工确认是真衰减还是噪声
2. 定位：分场景拆分指标，找衰减最严重的场景
3. 根因：①知识库过时 → 更新文档 ②prompt 不适配 → 改 prompt ③模型能力不足 → 微调/换模型
4. 验证：golden set + A/B 双重确认
5. 预防：建"知识库 freshness SLA"（如 FAQ 类 7 天更新）

**④ Tradeoff**：
- 监控灵敏度 vs 误报：阈值太低天天告警，太高发现晚；要分场景设阈值
- 自动治理 vs 人审：简单衰减（文档过时）可自动；复杂衰减（概念漂移）需人审

**⑤ 示例**：
> "我们 RAG 上线 3 个月后发现解决率从 78% 滑到 71%。排查：监控显示 query 分布 KL 散度 0.4（显著偏移），人工抽样发现是新产品上线后用户大量问新产品，但知识库还没更新。治理：①建产品知识库同步 SLA（产品上线 24 小时内知识库更新）②加'未知 query'自动聚类告警（每周看新出现的 query 类型）③golden set 每月补充新 case。现在能在一周内发现衰减，两周内修复。"

---

### Q23. 【权衡决策】70B 模型 vs 7B 模型 + 强 RAG，你怎么选？

**① 原理**：这是经典 tradeoff——70B 靠"参数内化的知识 + 推理能力"，7B+RAG 靠"外部知识 + 检索增强"。本质是"知识在哪"和"推理多强"的选择。

**② 量化对比**：

| 维度 | 70B 单独 | 7B + RAG |
|------|---------|----------|
| 推理能力 | 强（多步推理、复杂逻辑） | 弱（依赖 RAG 召回质量） |
| 知识时效 | 差（训练截止） | 好（实时更新） |
| 部署成本 | 高（多卡） | 低（单卡） |
| 延迟 | 高 | 中（多了检索） |
| 知识可控性 | 差（参数内） | 好（知识库可审计） |
| 幻觉 | 较高（自信编造） | 较低（有证据约束） |

**③ 生产经验决策**：
- 需要强推理（数学、代码、复杂规划）→ 70B
- 需要实时知识 + 可控 + 低成本 → 7B + RAG
- 最佳实践：**两者结合**——7B + RAG 做事实类问答，70B 做复杂推理，模型路由分发

**④ Tradeoff**：
- 能力 vs 成本：70B 强但贵 5-10 倍；7B+RAG 便宜但推理弱
- 延迟：70B 慢但单次；7B+RAG 快但多一次检索

**⑤ 示例**：
> "我们客服场景选了 7B（Qwen）+ 强 RAG，不用 70B。理由：①客服 90% 是事实类问题（查政策、查订单），RAG 召回准确就够了，70B 的推理能力用不上 ②知识每周更新，RAG 实时性赢 ③成本：7B 单卡 vs 70B 多卡，年省 200 万 ④幻觉：有 RAG 证据约束，7B 幻觉率反而比 70B 低。10% 复杂投诉才升级到 70B 模型。结论：不是越大越好，是匹配场景。"

---

### Q24. 【前沿趋势】推理模型（o1/R1）会改变 Agent 架构吗？

**① 原理**：推理模型（OpenAI o1/o3、DeepSeek-R1、Qwen-QwQ）通过 RL 训练"慢思考"能力——在回答前生成大量隐藏推理 token（chain of thought），显著提升数学/代码/复杂推理，但延迟高、成本高。

**② 量化**：
- 推理任务：o1 在 AIME 数学竞赛 83%（GPT-4o 13%），提升巨大
- 成本：o1 比 4o 贵 3-6 倍，延迟 10-30 倍（思考 token 几千到几万）
- 适用：复杂推理；不适用：简单 QA、闲聊、格式化输出

**③ 对 Agent 架构的影响判断**：
- **短期**：不改变架构，作为"模型路由"的一个选项——难题走推理模型，简单任务走普通模型
- **中期**：可能简化部分 Agent 设计——原本需要"Plan-and-Execute + 反思"的多步流程，推理模型单次就能搞定
- **长期**：推理模型 + 强工具调用，可能诞生"少步骤但每步深思"的新 Agent 范式

**④ Tradeoff**：
- 能力 vs 成本：推理模型强但贵且慢，不适合高并发实时
- 显式 Agent vs 隐式推理：显式 Agent（ReAct）可控可观测；推理模型黑盒（隐藏 CoT）但能力强

**⑤ 示例**：
> "我的判断：推理模型短期是 Agent 工具箱里的一件武器，不是颠覆者。我们用 o1 处理复杂工单（需要多步推理 + 工具调用的），用 GPT-4o 处理常规问答，模型路由分发。发现 o1 把原本 8 步的 ReAct 流程压缩到 2 步（它自己内部'想'了好几步），成功率提升 15%，但单任务成本翻 4 倍。所以只用在 top 10% 难题上。长期看，推理模型会让 Agent 架构更简洁，但'确定性外壳'（状态机、HITL、guardrail）依然必要。"

---

### Q25. 【底层原理】KV Cache 量化为什么省显存？精度损失可控吗？

**① 原理**：KV Cache 存的是每层 attention 的 K 和 V 矩阵，大小 = 2 × layers × kv_heads × head_dim × seq_len × batch。长上下文 + 高并发时，KV Cache 占显存常超过模型权重。量化（FP16 → INT8/INT4）直接减半或减 75%。

**② 量化方法**：
- **INT8 量化**：显存省 50%，精度损失 < 1%（多数场景可接受）
- **INT4 量化**：省 75%，精度损失 2-5%（需评估）
- **FP8**（H100 原生）：省 50%，精度几乎无损
- 关键：KV Cache 量化和模型权重量化是独立的，可分别选择

**③ 生产经验**：
- 长上下文场景 KV Cache 量化收益最大（KV 占比高）
- 用业务 golden set 评估，不只看困惑度
- 渐进策略：先 INT8（低风险），效果可接受再试 INT4

**④ Tradeoff**：
- 显存 vs 精度：量化越激进省越多但精度掉
- 显存 vs 并发：省下的显存能扛更多并发，吞吐提升

**⑤ 示例**：
> "我们 70B 模型跑 32K 上下文，KV Cache 占了 60% 显存。上 INT8 KV 量化后显存省一半，单卡能多扛 8 路并发，吞吐提升 40%。精度上用业务 golden set 测，faithfulness 几乎没掉（< 0.5%）。关键认知：KV Cache 量化比模型权重量化风险小（KV 是中间态不是核心参数），优先做。"

---

### Q26. 【底层原理·2026高频】为什么 KV 可以缓存而 Q 不能缓存？手写 Attention 时怎么实现？

**① 原理**：自回归生成时，已生成的 token 的 K 和 V 不变（只依赖历史 token），所以可缓存复用；而 Q 每步都变（新 token 的 Q 不同），不能缓存。

**② 数学直觉**：
- Self-Attention：`Attention(Q, K, V) = softmax(QK^T/√d) V`
- 生成第 t 个 token 时：
  - Q_t 只属于新 token（要算新的）
  - K_{1..t-1}、V_{1..t-1} 属于历史 token（不变，可复用）
- 没有 KV Cache：每步重算所有历史 K/V → O(n²) 复杂度
- 有 KV Cache：历史 K/V 复用，每步只算新 token 的 Q/K/V → O(n) 复杂度

**③ 手写实现要点（面试常考）**：
```python
# 伪代码：带 KV Cache 的 Attention
def attention_with_cache(x, kv_cache):
    Q = x @ W_Q              # 新 token 的 Q，shape [1, d]
    K_new = x @ W_K          # 新 token 的 K
    V_new = x @ W_V          # 新 token 的 V
    K = cat([kv_cache.K, K_new])  # 拼接历史 K
    V = cat([kv_cache.V, V_new])  # 拼接历史 V
    kv_cache.K, kv_cache.V = K, V  # 更新缓存
    attn = softmax(Q @ K.T / √d) @ V
    return attn
```
- 第一次调用：kv_cache 为空，正常计算
- 后续调用：复用历史 K/V，只算新 token

**④ 生产经验**：
- **Prefill 阶段**（处理 prompt）：无 cache 可用，计算密集
- **Decode 阶段**（生成 token）：用 cache，访存密集（这就是 PD 分离的依据）
- GQA/MQA 优化：多个 Q 头共享 K/V，进一步减 cache 大小

**⑤ 示例**：
> "面试时被让手写带 KV Cache 的 Attention。关键是理解'Q 每步新、K/V 历史不变'——所以缓存 K/V 矩阵，每步只算新 token 的 Q 并和缓存拼接。代码上维护一个 kv_cache 张量，append 新 K/V。常见追问：为什么 K/V 缓存但 Q 不缓存？答：Q 是当前 token 的查询，每步不同；K/V 是历史 token 的特征表示，已生成后不变。这个原理也解释了为什么 Decode 阶段是访存密集（要读整个 KV Cache）——PD 分离架构的依据。"

---

### Q27. 【生产实战·2026高频】采样参数（Temperature / Top-K / Top-P）怎么设置？不同场景的实战配置？

**① 原理**：采样参数控制生成随机性，直接影响输出质量和多样性。

| 参数 | 作用 | 取值范围 |
|------|------|---------|
| **Temperature** | 控制分布锐度，越低越确定 | 0-2，常用 0-1 |
| **Top-K** | 只从概率最高的 K 个 token 采样 | 整数（如 40） |
| **Top-P**（核采样） | 从累计概率 ≥ P 的最小 token 集采样 | 0-1（如 0.9） |
| **重复惩罚** | 降低已出现 token 概率 | 1.0-1.3 |

**② 不同场景的实战配置**：

| 场景 | Temperature | Top-P | 理由 |
|------|-------------|-------|------|
| 代码生成 | 0-0.2 | 0.9 | 要确定性，不能乱编 |
| 数学/逻辑推理 | 0-0.3 | 0.95 | 要严谨 |
| RAG 事实问答 | 0-0.3 | 0.9 | 基于证据，低幻觉 |
| 客服对话 | 0.3-0.5 | 0.9 | 自然但有边界 |
| 创意写作 | 0.7-1.0 | 0.95 | 要多样性 |
| 头脑风暴 | 0.9-1.2 | 0.97 | 最大化发散 |
| JSON/结构化输出 | 0 | - | 必须可解析 |

**③ 生产经验**：
- **Temperature=0 的陷阱**：完全确定性，但同一 prompt 模型升级后结果可能变（缓存失效）
- **Top-P 比 Top-K 好用**：Top-K 固定数量（长尾分布时不合理），Top-P 自适应（概率分布自适应）
- **重复惩罚慎用**：客服场景会破坏自然语言（如"您好"不能重复但正常）
- **结构化输出强制 Temperature=0** + structured output 模式

**④ Tradeoff**：
- 确定性 vs 多样性：低温度准但僵；高温度多样但可能跑偏
- 可复现 vs 灵活：Temperature=0 可复现但失去多样性

**⑤ 示例**：
> "我们的参数配置矩阵：代码/数学/RAG/结构化输出 Temperature=0（要准要可解析）；客服对话 0.4（自然但不跑偏）；创意文案 0.8。早期踩过坑：客服用 Temperature=0.7，模型偶尔'发挥'编内容；调到 0.4 + Top-P 0.9 后稳定。重复惩罚我们关了——客服场景'您好'重复是正常的，开了反而生硬。关键认知：参数要分场景配置，不是一套用所有接口。"

---

### Q28. 【生产实战·2026高频】微调导致"灾难性遗忘"（旧能力丢失）怎么办？怎么预防？

**① 原理**：微调时模型在领域数据上更新参数，可能"覆盖"原有通用能力——如微调后客服风格好了，但代码能力崩了。这是参数更新的副作用。

**② 量化检测**：
- 微调前后跑**通用基准**（MMLU/C-Eval/代码/数学）+ 业务评估集
- 通用能力下降 > 5% 就是灾难性遗忘信号
- 必须**留通用回归集**，不只看领域指标

**③ 五种预防/缓解方案**：

| 方案 | 原理 | 效果 |
|------|------|------|
| **LoRA/QLoRA** | 只更新低秩 adapter，原参数冻结 | 遗忘最轻，首选 |
| **混合训练数据** | 领域数据混 20-30% 通用数据 | 缓解遗忘 |
| **正则化（L2/EWC）** | 限制重要参数变化幅度 | 中等效果 |
| **回放（Replay）** | 领域训练时混入旧任务样本 | 有效但需旧数据 |
| **多 adapter 路由** | 领域 adapter + 通用 adapter 动态切换 | 最彻底（LoRA 优势） |

**④ 生产经验**：
- **首选 LoRA**：原参数不动，遗忘最轻；adapter 可热插拔（领域任务挂领域 adapter，通用任务挂通用 adapter）
- **混合数据是性价比解**：训练集混 30% 通用数据（如开源 instruction 数据），显著缓解
- **评估闭环必建**：微调后通用基准 + 业务基准双跑，通用掉了就回退或混数据重训
- **小学习率**：微调 lr 比 pretrain 低 1 个数量级（1e-5 vs 1e-4），减少参数漂移

**⑤ 示例**：
> "我们客服场景微调踩过坑：领域准确率从 70% 到 88%，但通用 MMLU 掉了 12%（模型变'客服脑'，问其他全懵）。解决方案：①改用 LoRA（原参数不动）②训练数据混 30% 通用 instruction ③微调 lr 从 2e-4 降到 5e-5 ④建通用回归集每次微调后跑。调整后领域 85%（略降）但通用只掉 2%，可接受。关键认知：微调不是只看领域指标，必须保通用能力——否则模型变成单任务工具，灵活性丢失。"

---

## 2026 新增高频题（推理模型与基础深化）

> 推理模型（o 系列 / R1 / GPT-5 thinking）是 2026 最大的范式变化，也是 Day3 最大缺口。以下 7 题补齐这块。MoE（Q19）、Speculative Decoding（Q20）、Flash Attention 2（Q18）已覆盖，这里补它们的 2026 升级版（FlashAttention v3、PD 分离后的 spec decoding）和全新题。

### Q29. 【2026 最高频】推理模型（o1/o3 / DeepSeek-R1 / GPT-5 thinking）到底是怎么"学会推理"的？

> 注：Q24 已讲推理模型对 Agent 架构的影响判断；本题补**底层机制**（RL 后训练 / test-time compute），是 2026 必答的原理题。

**① 原理**：推理模型不是靠堆参数，而是靠 **RL 后训练 + test-time compute** 让模型"想得更久更准"。
- **Test-time compute scaling**：推理时花更多 token 思考（CoT）→ 准确率提升，这是新的 scaling law（区别于"模型越大越强"的参数 scaling）
- **RL 后训练**：DeepSeek-R1 用 **GRPO**（Group Relative Policy Optimization，组相对策略优化）做 RL；**PRM（过程奖励模型）** 监督中间推理步骤对错（区别于只看最终结果的 ORM）
- 冷启动 SFT（用高质量长 CoT 数据）+ RL → 涌现出超长思考链能力

**② 关键概念**：
- **Internal CoT**（推理模型自带的隐藏思考链）vs **Prompted CoT**（用户引导的）——前者更强但消耗 thinking tokens，部分模型不返回过程（黑盒）
- **Thinking budget / reasoning effort** 参数：可设上限控制思考预算（low/medium/high），权衡延迟和准确率

**③ 量化参考**：
- DeepSeek-R1 训练成本远低于 o3（业界估算 R1 约 o3 的几个百分点），性价比惊人
- 推理成本：R1 等推理模型比同体量普通模型贵 5-10 倍（思考 token 多）

**④ Tradeoff**：
- 准确率 vs 延迟/成本：数学/代码/复杂推理显著提升，但慢且贵——只适合"难题"，日常简单任务用普通模型更快更省
- 可见性：internal CoT 黑盒，调试和合规审计难

**⑤ 示例**：
> "我们对推理模型的态度是'特种兵不是主力'。日常对话/分类用普通模型，遇到复杂投研分析、SQL 生成、数学建模才调 DeepSeek-R1。reasoning effort 按任务设——简单子任务 medium，关键决策 high。判断：推理模型的价值在'让中等模型解决难题'，但它的延迟和成本决定了不能全链路用。RL 后训练（GRPO/PRM）是它和普通模型的本质区别——不是 prompt 技巧，是真训出来的。"

🎯 **面试官追问**：
- "PRM 和 ORM 什么区别？" → PRM 监督每步推理对错（过程奖励），ORM 只看最终答案（结果奖励）；PRM 更细但贵
- "test-time compute 会一直涨吗？" → 边际收益递减，存在最优思考长度；过想反而出错（overthinking）
- 详见 Day15 趋势题（推理模型会取代 RAG 吗）

⚠️ **踩坑提示**：别说"推理模型就是加了 let's think step by step"——internal CoT 是 RL 训出来的内在能力，不是 prompt 技巧。

---

### Q30. 【蒸馏】推理模型的能力怎么蒸馏到小模型？为什么重要？

**① 原理**：推理模型（R1）强但贵，**蒸馏**把 R1 的思考链（thinking trace）当训练数据 SFT 小模型（Qwen/Llama 7B-32B），让小模型学到类似的推理模式——DeepSeek-R1-Distill 系列是代表作。

**② 为什么 2026 重要**：
- 推理模型贵慢，但蒸馏后的小模型在推理任务上接近大模型，成本低 10-100 倍
- 这是"把推理能力下沉到 SLM/端侧"的关键路径

**③ 生产经验**：
- 蒸馏 ≠ 完全继承：小模型学到的是"推理模式"，能力天花板仍低于教师模型
- 蒸馏数据质量决定效果——要用 R1 在高质量难题上生成的思考链

**④ Tradeoff**：
- 成本 vs 能力：蒸馏小模型便宜但上限低；关键场景仍要直接用教师模型

**⑤ 示例**：
> "我们数学辅导场景用 DeepSeek-R1-Distill-Qwen-32B 替代直接调 R1：推理准确率掉 8%，但单次成本降 20 倍、延迟降 5 倍。对高频 C 端场景（学生一天问几十次），这个 tradeoff 值。判断：蒸馏是让推理能力'平民化'的核心技术，端侧推理模型基本都靠蒸馏。"

---

### Q31. 【FlashAttention v3】FlashAttention 3 相比 v2 改进了什么？

> 注：Q18 已讲 FA2 原理（IO-aware，显存 O(n²)→O(n)）；本题补 FA3 的 2026 升级。

**① 原理**：FA3（针对 Hopper/H100 架构）用**异步**把 softmax/matmul 和 WGMMA（warp-group 矩阵乘）重叠执行，减少 stall；并支持 FP8 低精度 attention。

**② 量化数据**：
- H100 上达 75% GPU 利用率，比 FA2 快 1.5-2 倍
- BF16 约 800 TFLOPs/s，FP8 约 1.3 PFLOPs/s

**③ 生产经验**：
- 目前主要在 Together Inference Engine、vLLM(Ascend) 等集成，生态还在铺开
- FA2 没改 attention 数学，FA3 也是纯 IO/硬件优化，可无缝替换

**④ Tradeoff**：
- 硬件绑定：FA3 针对 Hopper，老卡（A100）用不了，得用 FA2
- 灵活性：自定义 attention pattern 支持仍有限

⚠️ **踩坑提示**：被问"Flash Attention 改了 attention 复杂度吗"——FA2/FA3 都**没改 O(n²) 计算复杂度**，是把显存从 O(n²) 降到 O(n)、靠 IO 优化提速。别答"降了计算量"。

---

### Q32. 【KV Cache 优化全景 · 2026 热点】长上下文瓶颈是 KV Cache，2026 有哪些体系化优化手段？

> 📎 **本题与 Q34 主题相同，已合并**。完整权威答案（含 KV Cache 大小计算公式、三层成本、六条优化路线全景表：PagedAttention/复用/量化/淘汰/分布式/压缩、生产经验 + 面试官追问）见 **[Q34](#q34-kv-cache-深度--2026-高频kv-cache-为什么是长上下文的真正瓶颈有哪些优化路线)**。
>
> **核心结论**：长上下文的真正瓶颈不是"计算量"而是 **KV Cache 的显存占用**（随 seq_len 线性增长，长上下文场景下 KV Cache 是权重的 1-2 倍）。2026 体系化优化六条路线：① **PagedAttention**（消除碎片，已成标配）② **Prefix Cache / RadixAttention**（复用共享前缀 KV）③ **KV 量化**（FP8 甜点位，省 50%）④ **Sliding Window / StreamingLLM**（固定窗口突破上限）⑤ **分布式 KV**（PD 分离基础）⑥ **KV 压缩/蒸馏**（保留重要 token）。
>
> **起步组合**：PagedAttention + Prefix Cache（system prompt 固定场景立竿见影）；2026 甜点位是 FP8 KV 量化（Blackwell 原生支持 + 精度好，长上下文衰减 < 2%）。

---

### Q33. 【SLM / 端侧】小语言模型（Phi-4 / Gemma / Qwen-small）为什么 2026 爆发？

**① 原理**：SLM（sub-7B）在成本、延迟、隐私、离线上有压倒性优势。Gartner 预测 SLM 使用量 2027 年超过 LLM。代表：Phi-4-mini(3.8B)、Gemma 3/4 E4B、Qwen-small、DeepSeek-R1-Distill。

**② 为什么强**：
- **蒸馏赋予推理**：DeepSeek-R1-Distill 把推理能力传给小模型，Phi-4-mini 在 MMLU/GPQA/MATH 上逼近 7B-9B
- 数据质量驱动：Phi 系列靠"教科书级"合成数据，小参数高质量数据 > 大参数低质数据

**③ 适用场景**：
- 端侧助手（手机/IoT/嵌入式）——毫秒级响应、零网络、隐私
- 高频分类/路由——第一级流量用 SLM 兜底，省 90% 成本
- 端云协同：常规端侧，复杂上云

**④ Tradeoff**：
- 能力天花板：SLM 够用于大多数任务，但复杂推理仍要上云调大模型
- 端云路由：什么时候端侧、什么时候上云是新的设计决策

**⑤ 示例**：
> "我们 App 内助手改成端云协同：80% 简单查询（天气、提醒、FAQ）走端侧 Qwen-small，毫秒级响应、无网络依赖、隐私不出端；20% 复杂问题上云调 Qwen-Max。整体延迟降 70%，云端推理成本降 80%。判断：SLM 不是替代 LLM，是形成'端云分层'——SLM 让 AI 功能可以离线、隐私、实时，这改变了产品形态（以前 AI 必须联网，现在不必）。"

🎯 **面试官追问**：
- "SLM 能力不够怎么办？" → 蒸馏 + 领域微调补强；真难题路由上云
- "端侧怎么更新模型？" → OTA 灰度推模型，注意端侧硬件异构

---

## 2026 进阶高频题（分水岭级）

> 以下 3 题面向 Infra / 推理优化方向深问，考的是"你是否理解 2026 年大模型推理的真正瓶颈和工程难点"。长上下文的 KV Cache、推理模型的部署差异、MoE 的 Expert Parallel，都是大厂 Infra 面的高频深题。

### Q34. 【KV Cache 深度 · 2026 高频】KV Cache 为什么是长上下文的真正瓶颈？有哪些优化路线？

**① 原理**：很多人以为长上下文的瓶颈是"计算量"，实际上瓶颈是 **KV Cache 的显存占用**。自回归生成时，每个 token 的 Key/Value 都要缓存供后续 token attend，KV Cache 大小 = `2 × n_layers × n_heads × head_dim × seq_len × batch × dtype_bytes`。以 Llama-3-70B 为例，128K 上下文单条请求的 KV Cache 就达 **~40GB**，比模型权重（~140GB FP16）的 1/3 还大——**长上下文场景下 KV Cache 是权重的 1-2 倍**，这才是显存杀手。

**② KV Cache 的三层成本**：
- **显存占用**：随 seq_len 线性增长，长上下文直接 OOM
- **带宽消耗**：每生成一个 token，要把整个 KV Cache 从 HBM 读一遍（decode 是 memory-bound），KV Cache 越大单 token 延迟越高
- **Prefill 计算**：长 prompt 的 prefill 是 compute-bound，百万 token prefill 可能要 10-30 秒

**③ 优化路线全景**：

| 路线 | 技术 | 效果 | 代价 |
|------|------|------|------|
| 分页管理 | **PagedAttention**（vLLM） | 消除显存碎片，吞吐 ×2-4 | 无明显代价，已成标配 |
| 复用 | **Prefix Cache / RadixAttention**（SGLang） | system prompt 的 KV 复用，省 30-60% prefill | 命中率依赖 prompt 前缀一致性 |
| 量化 | **KV Cache 量化**（FP8/INT8/INT4） | 显存降 50-75% | 精度损失，长上下文衰减加剧 |
| 淘汰 | **Sliding Window / StreamingLLM** | 固定窗口只保留最近 KV，突破显存上限 | 丢失早期上下文，不适合需全量回忆的任务 |
| 分布式 | **分布式 KV Cache**（Mooncake/DeepSeek 3FS） | KV 跨节点存储，PD 分离的基础 | 网络传输延迟，架构复杂 |
| 压缩 | **KV 压缩 / 蒸馏**（H2O/Heavy-Hitter） | 保留重要 token 的 KV，丢弃冗余 | 压缩策略影响质量 |

**④ 生产经验**：
- **PagedAttention + Prefix Cache 是起步组合**：vLLM 默认开 PagedAttention，Prefix Cache 需显式启用（`--enable-prefix-caching`），对 system prompt 固定的场景立竿见影
- **KV 量化要评估长上下文衰减**：INT8 量化在短上下文几乎无损，但 64K+ 长上下文的中段召回率可能掉 10-15%；FP8 是 2026 的甜点位（Blackwell 原生支持 + 精度好）
- **Sliding Window 只适合"最近 N 轮"场景**：客服/对话场景用 StreamingLLM（保留 attention sink + 最近窗口）能突破显存上限，但"全量回忆"场景（长文档 QA）不能用

**⑤ 示例**：
> "我们做长文档 RAG，128K 上下文单请求 KV Cache 40GB，A100 80G 单卡只能跑 1 条并发。优化三步：① 上 PagedAttention + Prefix Cache（RAG 的 system prompt 固定，prefix 命中率 80%，省 prefill 算力 50%）；② KV Cache 量化到 FP8（显存降 50%，单卡并发 1→3，长上下文 faithfulness 掉 < 2% 可接受）；③ 超长文档（> 128K）用 Sliding Window + 关键段落保留（attention sink + 检索召回的 chunk 永不淘汰）。三步组合后单卡并发 1→4，单 query 成本降 60%。教训：长上下文的瓶颈不是算力是显存，KV Cache 优化是性价比最高的杠杆。"

🎯 **面试官追问**：
- "PagedAttention 具体怎么解决碎片？" → 借鉴 OS 虚拟内存，KV Cache 按固定大小 block 存储（非连续），用 block table 映射逻辑→物理，消除外部碎片 + 内部碎片从 60%+ 降到 < 4%
- "Prefix Cache 命中率低怎么办？" → 前缀不一致时命中率低；解法是 prompt 结构化（system prompt 固定前置）+ 共享 prefix 抽取
- "KV 量化和模型权重量化能叠加吗？" → 能，但要分别评估精度损失，叠加后长上下文衰减可能叠加放大

⚠️ **踩坑提示**：
- 别只说"长上下文贵"——要讲清楚贵在 KV Cache 显存，不是计算
- "我们用了 vLLM"不够——vLLM 默认只开 PagedAttention，要讲你额外开了什么（prefix cache / KV 量化）
- 忽略"长上下文中段衰减"是常见盲点——KV 量化 + 长上下文会叠加放大 Lost in the Middle

---

### Q35. 【推理模型部署 · 2026 高频】推理模型（o3 / R1 / GPT-5 thinking）部署和普通模型有什么不同？thinking budget 怎么调？

**① 原理**：推理模型靠 **test-time compute**（"想得更久"）提升能力，和普通模型的部署差异在三点：① **internal CoT**——思考链是隐藏的（不输出给用户），但占用输出 token；② **thinking budget / reasoning effort**——可控制思考长度，是成本/质量的旋钮；③ **延迟分布完全不同**——普通模型首 token 快、后续均匀；推理模型"思考阶段"可能 10-60 秒无输出，然后才吐答案。

**② 推理模型 vs 普通模型的部署差异**：

| 维度 | 普通模型 | 推理模型 |
|------|---------|---------|
| 输出 token | 用户可见 | 思考 token 隐藏 + 答案 token 可见（思考占 60-90% token） |
| 延迟 | 首 token 200-500ms | "思考期" 10-60s 无输出，需流式吐思考过程缓解体感 |
| 成本 | 按输出 token 计 | 思考 token 也计费，单次成本是普通模型 5-20 倍 |
| 超时 | 10-30s | 需 120-300s（难题可能想几分钟） |
| 评估 | 端态答案 | 还要评"思考质量"（推理链是否合理） |

**③ 生产部署要点**：
- **thinking budget 分级**：按任务难度设不同 budget——简单任务 low（省成本）、难题 high（保质量）。o3 的 reasoning_effort 分 low/medium/high，R1 可通过 max_thinking_tokens 控制
- **流式输出思考过程**：推理模型"黑箱思考几十秒"用户体验极差，必须流式输出思考 token（哪怕只吐摘要）让用户知道"在干活"
- **超时与降级**：推理模型超时风险高（难题想太久），要有"超时降级到普通模型"的 fallback 机制
- **成本路由**：80% 简单 query 走普通模型，20% 难题走推理模型（见 Day3 Q20 混合路由），否则成本爆炸

**④ Tradeoff**：
- 思考预算 vs 成本/延迟：budget 越高准确率越好但成本线性增长、延迟线性增长；要在 golden set 上找"边际收益骤降"的拐点
- 流式思考 vs 安全：思考过程可能泄漏内部推理（含敏感信息），要过滤后再输出给用户
- 推理模型 vs Agentic RAG：推理模型擅长"给定证据做多步综合"，Agentic RAG 擅长"动态检索"，两者可组合但延迟和成本叠加

**⑤ 示例**：
> "我们投研系统从 GPT-4o 切到混合路由——80% 简单事实查询走 GPT-4o（3s、$0.01/次），20% 多跳分析走 DeepSeek-R1。R1 部署踩了三个坑：① 延迟——R1 思考期平均 25s 无输出，用户以为卡死，改流式输出思考摘要（每 5s 吐一句'正在分析 XX'）才解决；② 成本——R1 思考 token 占总 token 75%，单次成本是 GPT-4o 的 8 倍，必须靠分类器路由控制流量；③ 超时——难题 R1 可能想 3 分钟，网关超时设 180s + 超时降级到 GPT-4o 兜底。thinking budget 我们分三档：简单比较 low（思考 ~500 token）、标准分析 medium（~2000）、深度投研 high（~8000）。准确率从 72% 提到 89%，但只贵在 20% 流量上。判断：推理模型是特种兵不是主力，靠路由控制使用面。"

🎯 **面试官追问**：
- "thinking budget 怎么找拐点？" → 在 golden set 上跑 budget = 500/1000/2000/4000/8000，画准确率-budget 曲线，找"再增 budget 准确率提升 < 2%"的点作为默认
- "推理模型的思考过程要不要存？" → 要存（用于评估和 debug），但不输出给用户（可能含敏感推理）；存储成本要算进总成本
- "推理模型 + RAG 怎么组合？" → RAG 提供证据，推理模型做多步综合；注意推理模型的"思考"里要包含"证据评估"环节（判断召回够不够），不只是直接推理

⚠️ **踩坑提示**：
- 别说"推理模型更准所以全用它"——成本和延迟决定了它只能用于关键子任务
- 忽略"流式输出思考过程"是高频遗漏——这是推理模型用户体验的关键
- "thinking budget 越大越好"是误区——有边际递减，要找拐点不是拉满

---

### Q36. 【MoE 部署 · 2026 高频】MoE 模型部署为什么比 Dense 模型难？Expert Parallel 怎么做？

**① 原理**：MoE（Mixture of Experts）把 FFN 层换成"多专家 + 门控路由"，每 token 只激活 top-K 专家。代表：DeepSeek-V3（671B 总参数 / 37B 激活）、Qwen3-MoE、Llama 4。MoE 的部署难点在于：**总参数大（所有专家都要驻显存）但激活参数小（每 token 只用几个）**——"大模型质量 + 小模型成本"的代价是部署复杂度上升。

**② MoE vs Dense 部署差异**：

| 维度 | Dense 模型 | MoE 模型 |
|------|-----------|---------|
| 显存需求 | 权重 = 总参数 | 权重 = 总参数（所有专家）+ 路由开销 |
| 推理计算 | 每 token 用全部参数 | 每 token 只算激活的 top-K 专家 |
| 单卡部署 | 参数放得下就行 | 所有专家都要驻显存，单卡更难放下 |
| 负载均衡 | 天然均衡 | 专家激活不均（热门专家过载、冷门专家闲置） |
| 通信开销 | 张量并行即可 | 需 Expert Parallel + all-to-all 通信 |

**③ Expert Parallel（EP）核心**：
- **为什么需要 EP**：671B 的 MoE 单卡放不下所有专家，必须把专家分散到多卡。EP = 每张卡放一部分专家，token 路由到对应卡的专家计算
- **all-to-all 通信**：每层 MoE 都要把 token 发到"有目标专家的卡"算完再收回，通信量比 Dense 的张量并行大
- **负载均衡是关键**：如果 80% 的 token 都路由到同一个专家（热门专家），那张卡成瓶颈，其他卡闲置。训练时用 auxiliary loss 平衡，推理时用动态专家分配缓解

**④ 生产经验**：
- **active params 决定推理成本，total params 决定显存需求**：DeepSeek-V3 671B 总/37B 激活——推理算力按 37B 算（比 70B Dense 便宜），但显存按 671B 算（需要 8+ 张 H100）。这是 MoE 的"显存贵、算力省"特性
- **EP + TP 组合**：大规模 MoE 通常 EP（专家分卡）+ TP（每个专家内部张量并行）组合用。EP 跨卡、TP 卡内，通信模式要匹配硬件拓扑
- **Batch 对 MoE 更重要**：MoE 的专家激活稀疏，小 batch 时 GPU 利用率低；大 batch 能让多个 token 填满不同专家，利用率高。Continuous Batching 对 MoE 收益更大

**⑤ 示例**：
> "我们部署 DeepSeek-V3（671B/37B 激活），8×H100 节点。踩的坑：① 显存——总参数 671B FP8 也要 ~340GB，8 卡 H100（640GB）刚够，KV Cache 空间紧张，必须 KV 量化；② 负载不均——某些专家被频繁激活（热门），对应 GPU 利用率 95% 而其他卡只有 40%，吞吐被短板卡限制。用 DeepEP（DeepSeek 开源的 EP 通信库）+ 动态专家重分配缓解，利用率从 40% 均到 70%；③ 通信——EP 的 all-to-all 通信占总延迟 20-30%，要调 batch size 让计算/通信比合理。相比 Dense 70B，DeepSeek-V3 质量更好（激活 37B 接近 70B Dense 效果）、单 token 算力成本低 40%，但显存贵 3 倍 + 部署复杂度高。判断：MoE 适合质量优先 + 有多卡资源的场景，单卡/小集群还是 Dense 更省心。"

🎯 **面试官追问**：
- "MoE 为什么省算力但不省显存？" → 每 token 只算 top-K 专家（省算力），但所有专家都要驻显存（不省显存）。这是 MoE 的根本 tradeoff
- "负载不均怎么解决？" → 训练时 auxiliary loss 平衡（让 token 均匀路由）；推理时动态专家分配（冷门专家迁移到热卡）+ 大 batch 填满冷门专家
- "MoE 和推理模型的 KV Cache 有关系吗？" → 有。MoE 的 FFN 层换成了专家，但 Attention 层（KV Cache）不变，KV Cache 优化策略通用

⚠️ **踩坑提示**：
- 别把"激活参数"和"总参数"搞混——推理成本看激活，显存看总参数
- "MoE 一定比 Dense 便宜"是错的——算力便宜但显存贵，小规模部署可能更贵
- 忽略"负载均衡"是高频盲点——这是 MoE 生产部署的核心难点

---

### Q40. 为什么基于 Qwen2.5 架构？基于 Llama3 / GLM4 不行吗？

**答案：**

选 Qwen2.5-72B 不是因为它是国产，是四个工程现实算出来的最优解——中文+代码双强、128K 长上下文、Apache 2.0 无限制、迭代速度最快。

| 维度 | Qwen2.5-72B | Llama3-70B | GLM-4-9B | 结论 |
|---|---|---|---|---|
| 架构 | Decoder-only + GQA + RoPE + SwiGLU + 128K 原生长上下文 | 同架构，原生 8K | GLM 架构（自回归填空）| **Qwen 长上下文碾压** |
| 中英文+代码 | 中英双语 + 代码/数学全第一梯队 | 英文/代码强，中文弱 | 中文最强，代码一般 | **Qwen 最均衡** |
| 工具链支持 | vLLM / SGLang / TensorRT-LLM 一等公民 | 一等公民 | 支持一般 | 平手 |
| 微调生态 | LLaMA-Factory / UnSloth / Axolotl 全适配，中文社区案例最多 | 全适配 | 一般 | **Qwen 中文场景最全** |
| License 商用 | **Apache 2.0（无任何限制）** | Community License（< 7 亿 MAU） | 免费商用 | **Qwen 最宽松** |
| 迭代速度 | 2.0→2.5→3 半年一代 | 3→4 一年一代 | 慢 | **Qwen 最快** |

**选 Qwen2.5 的核心理由**：
1. **中英文双语 + 代码能力最均衡**：Coding Agent 要处理中文注释、中文需求文档、中英混合 Issue，Qwen2.5 的双语能力是 Llama3 的硬伤补不上来的；代码能力在 72B 档位也是第一梯队，不输 Llama3
2. **128K 原生长上下文**：Coding Agent 经常要把整个代码库/几十个文件塞进 prompt，Qwen2.5 原生 128K（Llama3 原生只有 8K，扩长要外推微调）；长上下文上 Qwen 在 Needle-in-a-Haystack 测试表现稳
3. **Apache 2.0 零限制商用**：Llama3 的 Community License 有 7 亿 MAU 限制和"使用 Llama 商标"条款，企业法务会卡；Qwen 完全开源无任何附加条款，法务零摩擦
4. **工具链和微调生态成熟**：vLLM、SGLang 一等公民支持；LLaMA-Factory、UnSloth、Axolotl 都有现成 Qwen 微调 recipe；中文社区案例多，出问题能搜到答案
5. **迭代速度**：Qwen 系列半年迭代一代（2.0→2.5→3），能力快速逼近闭源；Llama 一年一代——迭代慢意味着你的微调 recipe 半年就得重做

**为什么不用 Llama3**：只有"纯英文代码 + 英文文档"场景 Llama3 才更优；Coding Agent 涉及中文需求/注释/Issue 时 Llama3 的中文理解明显掉档。

**为什么不用 Llama4 / Qwen3-MoE**：MoE 架构部署复杂度高（专家并行 + all-to-all 通信），72B 级别 Dense 单机 8×H100 即可，同级别 MoE 显存占用更大单机放不下，ROI 不如 Dense。

**一句话版**：中文+代码双强 + 128K 长上下文 + Apache 2.0 零限制 + 迭代最快，四票投给 Qwen2.5-72B Dense。纯英文场景才考虑 Llama3。

🎯 **面试官追问**：
- "7B 还是 72B？" → Coding Agent 主推理用 72B（代码生成质量差距大，72B 能搞定复杂重构，7B 只够补全）；7B 留给路由/分类/记忆决策这类轻任务降本
- "Qwen3 出了为什么还用 2.5？" → Qwen3 主推 MoE 架构，部署复杂度高 + 显存占用大，单机放不下同级别 MoE；2.5-72B Dense 在 vLLM 上单机 8×H100 即可，ROI 更优
- "128K 上下文用满吗？" → 跨文件代码库检索/长会话续接时会到 64-96K；超过 100K 体验会衰减，关键任务还是配合 RAG 不全靠塞
- "Qwen 的工具调用能力够吗？" → Qwen2.5 原生 function calling 支持完整；我们 SFT 时重点补了"多工具链式调用 + 错误恢复"的训练数据

⚠️ **踩坑提示**：
- 别只答"开源"——要落到"双语能力 + 长上下文 + License + 迭代速度"四点
- 漏掉"为什么不用 Qwen3 MoE"——面试官 2026 必追这个
- 不讲"72B vs 7B 分工"——显得没做过成本优化
- 别说"因为是国产所以选"——这是立场先行不是工程判断，会被认为不专业

---

### Q41. 所选数据集大小是多少？怎么保证质量？

**答案：**

**数据集规模**：

| 阶段 | 数据量 | 数据构成 | 训练配置 |
|---|---|---|---|
| SFT | ~10 万条指令对 | 公开数据 30% + 合成数据 50% + 真实日志 20% | LoRA rank=64，3 epoch，~5 亿 token |
| 评估集 | 2000 条 holdout | 工具调用轨迹 + 代码任务 | 不参与训练，只做评测 |

**数据构成详解**：
- **公开数据（30%）**：OpenOrca、CodeAlpaca、Evol-Instruct 的代码子集——打基础能力底座
- **合成数据（50%）**：用 GPT-4o + Claude + DeepSeek 三 teacher 生成（多 teacher 防 model collapse）——覆盖工具调用、代码重构、Debug 三类场景
- **真实日志（20%）**：脱敏后的真实使用日志（工具调用序列 + 用户反馈）——最贵但最有价值，对齐真实分布

**质量管控（质量 > 数量）**：
1. **LLM-as-judge 三轮筛选**：每条数据用 GPT-4o 打分（正确性 + 清晰度 + 难度），低于 0.7 的丢弃（砍掉约 30%）
2. **人工抽检 5%**：随机抽样人工复核，主要抓 LLM-judge 漏网的"看起来对但逻辑错"案例
3. **难度分层**：简单（单工具）/ 中等（2-3 工具链）/ 困难（5+ 工具 + 错误恢复），按 4:4:2 配比，避免模型只学会简单任务
4. **去重 + 毒性过滤**：MinHash 去重（相似度 > 0.9 留一条）+ 安全过滤器剔除有害内容

**为什么不做 DPO/RLHF（v1 阶段）**：SFT 已能解决 80% 问题；DPO 需要偏好数据对（好/差答案对），标注成本翻倍，留作 v2。

🎯 **面试官追问**：
- "10 万条怎么够？" → LIMA 论文证明 1K 条高质量 > 5 万条低质量；关键是质量 + 难度分布，不是堆量
- "合成数据 50% 会不会 model collapse？" → 用多 teacher（3 个不同模型）+ 加噪声 + 真实数据混入（20%）缓解；定期用真实分布评估集检测 collapse
- "数据集会持续更新吗？" → 会，数据飞轮——线上失败 case 进评估集，每月迭代一轮数据

⚠️ **踩坑提示**：
- 别只给数字——要讲数据构成和质量管控（面试官真正考的是"你懂不懂数据工程"）
- 漏讲"评估集"——只讲训练不讲评估 = 没做过项目
- 不提"难度分层"——全是简单任务训练出来的模型一上线就崩

---

## 今日自测清单

> **升级版自查**：不是"能不能背下来"，而是"能不能 3 分钟脱稿讲清楚，并接住追问"。

| 题 | 自查问题 | 达标标准（开口讲） | ✓/✗ |
|----|---------|-------------------|-----|
| Q1 | Self-Attention + sqrt(d_k) | 能讲清"除 sqrt 不是除 d_k"的原因 | ⬜ |
| Q2 | MHA vs GQA vs MQA | 能讲清为什么 GQA 成主流 | ⬜ |
| Q3 | RoPE 成为主流的原因 | 能讲清相对位置 + 外推性好 | ⬜ |
| Q4 | 开源 vs 闭源选型 | 能给场景化判断，不绝对化 | ⬜ |
| Q5 | Llama/Qwen/GLM/DeepSeek 特点 | 能讲清 DeepSeek MLA 和 MoE 优势 | ⬜ |
| Q6 | GPTQ/AWQ/GGUF 选型 | 能讲清"服务端 vs 边缘"选型逻辑 | ⬜ |
| Q7 | vLLM/PagedAttention | 能区分显存优化 vs 调度优化 | ⬜ |
| Q9 | LoRA/QLoRA + 何时微调 | 能背微调决策树 + rank 怎么选 | ⬜ |
| Q10 | RLHF vs DPO | 能讲清 DPO 为什么更稳定 | ⬜ |
| Q15 | 长上下文 + KV 优化 | 能答"长上下文会取代 RAG 吗" | ⬜ |
| Q18 | Flash Attention | 能讲清 IO-aware + 显存 O(n²)→O(n) | ⬜ |
| Q29 | 推理模型机制（GRPO/PRM/test-time compute） | 能讲清"RL 训出来不是 prompt 技巧" | ⬜ |
| Q32 | KV Cache 优化全景 | 能讲清 PagedAttention/复用/量化三层 | ⬜ |
| Q33 | SLM 端侧 + 端云协同 | 能讲清"SLM 不替代 LLM 是分层" | ⬜ |

**达标线**：14 题中 ≥ 11 题能脱稿讲 + 接住至少 1 个追问 → Day3 通过。
**未达标处理**：薄弱题回到主文件附录 A.3 重读原理，再用本文件示例答练嘴 3 遍。

## 参考资源

- [2026大模型面试题库](https://zhuanlan.zhihu.com/p/1981387722473116577)
- [大模型面经汇总 - 代码随想录](https://notes.kamacoder.com/interview/llm/)
- [LLM训练流程与微调技术](https://www.cnblogs.com/moonout/p/19705026)
- [主流开源大模型对比与选型指南](https://jimmysong.io/zh/book/ai-handbook/llm/model-overview/)
