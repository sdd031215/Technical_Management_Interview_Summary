# Day 6 — 系统设计专项练习

> 目标：练习 LLM 方向的系统设计面试题，能在白板上 30 分钟内画出完整架构图并讲清每个模块的选型理由
> 产出：3 道系统设计题的完整解答（含架构图）

---

## 🔗 与主文件《3 周版》的联动（避免重复，先读这里）

本文件是 Day6 的**深化执行手册**。系统设计的答题模板和高频题在主文件：

| 你要做的 | 去哪找 | 本文件补充什么 |
|---------|--------|---------------|
| 查系统设计 8 步模板 | 主文件 **附录 C.3**（白板通用模板） | ✅ 本文件做 3 道完整实战题 |
| 查 RAG 平台设计要点 | 主文件 **附录 B.17** | ✅ 补完整架构图 + 追问弹药 |
| 查 Agent 平台设计 | 主文件 **附录 B.18** | ✅ 补状态机 + MCP 设计 |
| 查 LLM 网关/模型路由 | 主文件 **附录 B.19** | ✅ 补成本优化题 |
| 做半日时段练习 | 主文件 **第一周 Day 7**（已含本主题半日表） | ✅ 本文件做"实战深化" |

**阅读顺序建议**：先记熟主文件附录 C.3 八步模板 → 用本文件做 3 道完整实战演练。

---

## 今日安排

| 时段 | 内容 | 产出 |
|------|------|------|
| 上午 9:00-10:30 | 系统设计题 1：企业级 RAG 平台 | 架构图 + 讲解稿 |
| 上午 10:30-12:00 | 系统设计题 2：多 Agent 协作系统 | 架构图 + 讲解稿 |
| 下午 14:00-15:30 | 系统设计题 3：大模型服务成本优化方案 | 架构图 + 讲解稿 |
| 下午 15:30-17:00 | 回顾 Day 1-3 面试题薄弱点 | 补强笔记 |
| 晚上 19:00-20:00 | 每道题限时 15 分钟重讲一遍 | 计时记录 |

> **图标约定**：🎯 面试官追问 / ⚠️ 踩坑提示 / 💬 示例答

---

## 系统设计面试方法论

### 标准流程（30 分钟）

```
Step 1: 需求澄清（3 分钟）
  - 功能需求：系统要做什么？
  - 非功能需求：QPS、延迟 SLA、数据规模、可用性要求
  - 约束条件：预算、团队规模、时间窗口

Step 2: 高层架构设计（5 分钟）
  - 画出核心模块和数据流
  - 不要一上来就深入细节

Step 3: 深入关键模块（15 分钟）
  - 面试官最关心的 2-3 个核心模块
  - 每个模块讲清：做什么、怎么选、为什么

Step 4: 扩展性讨论（5 分钟）
  - 如果流量增长 10 倍怎么办？
  - 瓶颈在哪里？
  - 有什么可以优化的地方？

Step 5: 总结（2 分钟）
  - 30 秒概括整体架构
  - 强调关键决策点
```

🎯 **面试官追问**（方法论层面的连环题）：
- "你说先澄清需求，但面试官只给一句话题目，问什么不显得菜？" → 问规模/SLA/约束，体现工程感；别问"用什么语言"这种
- "高层架构 5 分钟够吗？要不要直接深入？" → 先画 4-6 个主模块框图，让面试官看到全局再深入；直接深入会显得没架构感
- "如果面试官打断你'别画了直接说重点'怎么办？" → 适应风格，切换到讲关键决策点

⚠️ **踩坑提示**：
- **需求澄清是最容易被跳过、最能加分的一步**——直接开画的候选人暴露经验不足
- 别陷入"完美主义"——白板题不求覆盖所有 corner case，讲清主干 + 关键决策即可
- 8 步模板要练成肌肉记忆，对应主文件附录 **C.3**

💬 **示例答**（开场 1 分钟）：
> "我先确认几个关键约束：日均 10 万查询、P99 延迟要求、数据规模、预算——这几个决定架构走向。基于这些我画一个顶层架构，4-5 个主模块，先讲数据流再讲控制流，然后我们挑你最关心的模块深入。这样 OK 吗？"（征得同意再开画，体现沟通感）

---

## 系统设计题 1：设计一个企业级 RAG 平台

### 题目
"请设计一个企业级 RAG 问答平台，支持多部门接入，日均 10 万次查询，知识库规模 100 万文档。"

### 需求澄清

```
功能需求：
- 多部门知识库管理（每个部门独立知识空间）
- 支持多种文档格式（PDF/Word/网页/数据库）
- 支持多轮对话和引用溯源
- 支持权限控制（不同角色看到不同内容）

非功能需求：
- QPS: ~5（峰值 ~20）
- P99 延迟: <5 秒
- 可用性: 99.9%
- 数据规模: 100 万文档，约 5000 万 Chunks

约束条件：
- 团队 5 人
- 月预算 ¥5 万（含 LLM API + 基础设施）
```

### 架构图

```
                        ┌─────────────────────────┐
                        │     API Gateway          │
                        │  (鉴权/限流/租户路由)     │
                        └────────────┬────────────┘
                                     ↓
┌──────────────────────────────────────────────────────────┐
│                    查询处理层                              │
│                                                          │
│  [会话管理] → [Query改写] → [意图路由]                    │
│   (Redis)    (LLM改写)    (RAG/直接回答/工具调用)          │
└──────────────────────────────────────────────────────────┘
                     ↓ (RAG路径)
┌──────────────────────────────────────────────────────────┐
│                    检索引擎层                              │
│                                                          │
│  [混合检索]          →  [RRF融合]  →  [Rerank精排]        │
│  ├─ 向量检索(Qdrant)                  (BGE-Reranker)      │
│  └─ BM25检索(ElasticSearch)                               │
└──────────────────────────────────────────────────────────┘
                     ↓
┌──────────────────────────────────────────────────────────┐
│                    生成层                                  │
│                                                          │
│  [上下文组装] → [Prompt构建] → [LLM生成] → [后处理]      │
│  (Token预算)    (引用约束)    (流式输出)   (幻觉检测)      │
└──────────────────────────────────────────────────────────┘
                     ↓
┌──────────────────────────────────────────────────────────┐
│                    离线索引层                              │
│                                                          │
│  [文档解析] → [分块] → [Embedding] → [入库]              │
│  (多格式)    (语义)   (BGE-M3)     (异步队列)             │
│                                                          │
│  [增量更新] + [全量重建(周)] + [版本管理]                  │
└──────────────────────────────────────────────────────────┘
                     ↓
┌──────────────────────────────────────────────────────────┐
│                    可观测性层                              │
│  Langfuse(Trace) + Prometheus(指标) + ELK(日志)          │
│  评估Pipeline(每日自动) + 告警                             │
└──────────────────────────────────────────────────────────┘
```

### 关键技术决策讲解

**1. 向量数据库选 Qdrant 而非 Milvus**
"100 万文档级别 Qdrant 单机即可承载，Rust 编写性能优秀，支持 payload 过滤（用于权限控制），运维复杂度低于 Milvus。如果后续增长到千万级，再考虑迁移 Milvus 分布式。"

**2. 混合检索 + RRF**
"我们的业务知识中大量包含专有名词（酒店名称、城市编码等），纯向量检索的精确匹配能力不足。BM25 补充精确匹配，RRF 融合消除量纲差异。离线评估显示混合检索相比纯向量，Recall@10 提升了 15%。"

**3. 多租户权限控制**
"在检索层通过元数据过滤实现：每个 Chunk 入库时打上 `tenant_id` 和 `access_level` 标签，查询时自动注入过滤条件。**绝不能依赖 Prompt 约束权限**。"

**4. 成本控制**
"使用模型路由——简单 FAQ 问题（占比 60%）路由到 GPT-4o-mini（¥0.001/次），复杂问题路由到 GPT-4o（¥0.03/次）。月 LLM 成本控制在 ¥3 万以内。"

**5. 流量增长 10 倍的扩展方案**
"向量库分片 + 副本、ElasticSearch 水平扩展、LLM 层引入自部署开源模型（Qwen2.5-14B）减少 API 依赖。"

---

## 系统设计题 2：设计一个多 Agent 协作系统

### 题目
"请设计一个多 Agent 协作系统，支持复杂任务的自动分解和执行，如'帮我调研竞品并生成分析报告'。"

### 架构图

```
用户任务
    ↓
┌─────────────────────────────────────────────┐
│              Task Router（任务路由器）         │
│  - 意图分类：简单问答 / 单步任务 / 复杂任务    │
│  - 复杂度评估：决定 Agent 数量和策略           │
└─────────────────────────────────────────────┘
    ↓ (复杂任务)
┌─────────────────────────────────────────────┐
│              Planner（规划器）                 │
│  - 任务分解为子任务序列                       │
│  - 确定子任务之间的依赖关系                   │
│  - 为每个子任务分配 Agent 角色                 │
└─────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────┐
│         Agent Execution Layer（LangGraph）    │
│                                              │
│  [Research Agent] → [Analysis Agent] → [Writer Agent]│
│  (信息收集)          (数据分析)         (报告生成)     │
│                                              │
│  每个 Agent：                                 │
│  - 独立的 System Prompt                       │
│  - 独立的工具集（MCP Tools）                   │
│  - 独立的 Memory（短期+长期）                  │
└─────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────┐
│              Review & Quality Gate            │
│  - Critic Agent 评审输出质量                  │
│  - 质量不达标 → 触发 Re-planning              │
│  - 人工介入点（可选）                          │
└─────────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────────┐
│              State & Memory Service           │
│  - LangGraph Checkpoint（状态持久化）          │
│  - Redis（短期上下文）                         │
│  - 向量DB（长期记忆，历史任务轨迹）             │
└─────────────────────────────────────────────┘
```

### 关键技术决策

**1. 为什么用 LangGraph 而不是 CrewAI？**
"需要精确控制执行流（条件分支、Re-planning、断点恢复），且需要生产级的状态持久化。LangGraph 的 StateGraph 天然支持这些需求，虽然开发成本高，但生产可靠性更好。"

**2. 如何处理 Agent 间的信息传递？**
"通过 LangGraph 的共享 State 传递，每个 Agent 读取 State 中的上游结果，写入自己的输出。State 是类型安全的（TypedDict），避免信息丢失或格式不一致。"

**3. 如何防止 Agent 进入死循环？**
"三重保障：max_steps 全局限制（如 20 步）、单 Agent 步数限制（如 5 步）、token 预算限制。超出任一限制触发降级（返回已完成部分的结果）。"

---

## 系统设计题 3：大模型服务成本优化方案

### 题目
"公司每月 LLM API 支出 ¥30 万，请设计一套成本优化方案，在不显著影响质量的前提下将成本降低 50%。"

### 架构图

```
用户请求
    ↓
┌──────────────────────────────────────┐
│           请求预处理层                 │
│                                      │
│  [语义缓存]  → 命中？→ 直接返回       │
│  (Redis + Embedding 相似度匹配)      │
│                                      │
│  [难度评估]  → 简单/中等/困难         │
│  (轻量分类模型，<5ms)                 │
└──────────────────────────────────────┘
    ↓
┌──────────────────────────────────────┐
│           智能路由层                   │
│                                      │
│  简单(60%) → 小模型（Qwen2.5-7B 自部署）│
│  中等(30%) → 中模型（GPT-4o-mini）     │
│  困难(10%) → 大模型（GPT-4o/Claude）   │
│                                      │
│  + Prefix Caching（共享 system prompt）│
│  + 上下文压缩（检索结果摘要）           │
└──────────────────────────────────────┘
    ↓
┌──────────────────────────────────────┐
│           后处理与反馈层               │
│                                      │
│  [质量检查] → 不达标？→ 升级到大模型   │
│  [用户反馈] → 点赞/点踩 → 路由策略调优 │
│  [写入缓存] → 高质量回答入语义缓存     │
└──────────────────────────────────────┘
```

### 成本优化六大策略

| 策略 | 预期节省 | 实施难度 | 风险 |
|------|---------|---------|------|
| **模型路由** | 40-50% | 中 | 路由误判影响质量 |
| **语义缓存** | 15-25% | 低 | 缓存过时 |
| **自部署开源模型** | 30-50%（简单任务） | 高 | 需要 GPU 和运维 |
| **上下文压缩** | 10-20% | 低 | 压缩可能丢信息 |
| **Prefix Caching** | 15-30% | 低 | 仅对共享前缀有效 |
| **量化部署** | 50-75%（显存） | 中 | 精度略有损失 |

### 成本估算

```
优化前：¥30万/月（全部 GPT-4o）

优化后：
- 简单任务 60% → 自部署 Qwen2.5-7B：GPU 成本 ¥3万/月
- 中等任务 30% → GPT-4o-mini：¥2.7万/月
- 困难任务 10% → GPT-4o：¥3万/月
- 语义缓存命中 15%：节省 ¥4.5万/月

总成本：约 ¥8.7万/月（节省约 71%）
```

---

## 系统设计通用"追问弹药"

无论哪道题，面试官都可能追问以下问题：

1. **"瓶颈在哪里？"** → 提前想好每个架构的瓶颈点
2. **"流量增长 10 倍怎么办？"** → 水平扩展方案
3. **"如果这个组件挂了怎么办？"** → 降级和灾备
4. **"你怎么做灰度发布？"** → A/B 测试策略
5. **"成本怎么估算的？"** → 展示商业意识

---

## 深度追问 Q&A（面试官三板斧）

以下追问按三道设计题分别展开。系统设计面试中，**架构图只占 30% 的分，追问应对占 70%。**

### 题 1 追问：企业级 RAG 平台

#### 追问 1.1："Qdrant 和 Milvus 你到底怎么选的？如果现在数据量翻了 10 倍，你的选型还成立吗？"

> "100 万文档级别选 Qdrant 的核心理由有三：第一，单机即可承载（Rust 实现，内存效率高），运维成本远低于 Milvus 分布式部署；第二，Qdrant 原生支持 payload 过滤，我们的多租户权限过滤可以直接在检索时完成，不需要额外加一层过滤逻辑；第三，Qdrant 的 HNSW 索引参数调优比 Milvus 更直观——我们团队没有专门的向量库运维，这一点很关键。
>
> **数据量 10 倍增长后**：1000 万文档 ≈ 5 亿 Chunks，Qdrant 单机大概率扛不住。这时我的迁移路径是：Qdrant → Milvus 分布式（Coordinator + Proxy + Worker Node）。但迁移成本很高——索引重建 + 数据迁移 + 检索逻辑重写。所以我会在架构中提前做一层 VectorStore Adapter，把向量库的具体实现封装起来，上层只调 search() 和 upsert()，下层可以随时换引擎。这个 Adapter 的代价很小（不到 200 行代码），但给未来的扩展留了无限空间。"

#### 追问 1.2："你为什么用 BM25 + 向量混合检索，而不是纯向量 + 更大 Embedding 维度？"

> "纯向量检索有两个硬伤。第一，专有名词匹配：向量模型没见过你的业务术语（比如公司内部的 SKU 编码、项目代号），这些词在向量空间中会被拆成子词，搜索精度急剧下降。用 1024 维还是 4096 维都解决不了这个问题，因为根因不在维度而在训练数据覆盖。第二，长尾查询：用户问'2024 年 Q3 华东区实体门店的毛利率报表'——这类问题包含精确过滤条件，向量检索很难同时兼顾语义相似和条件过滤。
>
> BM25 的优势恰好互补：专有名词精确命中、结构化条件天然支持。两者的融合选 RRF（Reciprocal Rank Fusion）而不是线性加权，因为 BM25 和向量相似度的量纲完全不同，RRF 通过排名融合绕过了归一化问题。"

#### 追问 1.3："多租户权限控制，你怎么确保不会越权？有没有什么极端情况？"

> "权限控制有两个层次。第一层，检索时过滤：每个 Chunk 入库时打上 tenant_id 和 access_level，查询时 Qdrant 做 payload filter，只返回当前租户有权看到的内容。这一层是硬保证，数据层面做到了隔离。第二层，生成时兜底：即使检索层漏了（比如 payload filter 有 bug），生成层的 Prompt 中也明确了'如果检索结果包含不在当前租户权限内的内容，不要输出'。但这一层是软保证——LLM 可能不遵守。所以绝不能用 Prompt 替代数据层过滤。
>
> **极端情况**：跨部门文档分享。比如财务部把一份预算报告分享给市场部，市场部的用户搜到了，但他只能看到被分享的那份，看不到财务部的其他文档。这个场景下，权限模型从'按租户'升级为'按文档 + 按租户'，每份文档维护一个 authorized_tenants 列表。复杂度上升一个量级，所以在需求澄清阶段就要问'有没有跨部门文档分享'。"

#### 追问 1.4："检索召回了 20 篇文档但都不相关，你这个系统怎么办？"

> "这是 RAG 系统最常见的 Bad Case——召回失败比生成失败更难处理，因为生成层的输入就已经有问题了。我的处理是三层兜底：
>
> 1. **检索层兜底**：如果 Rerank 后 Top-3 的 Relevancy Score 都低于阈值（比如 < 0.3），触发 Query Rewrite。用一个更轻量的模型（比如 GPT-4o-mini）把用户问题换个方式重新表达，再搜一次。
> 2. **重试后仍不满意**：直接走'知识库外回复'——告诉用户'这个问题在我们的知识库中没有找到相关材料，建议你换个方式提问，或联系 XX 部门获取帮助'。坦率承认不知道，比硬编一个答案好一万倍。
> 3. **离线优化闭环**：这类低召回率的 Query 自动进 Bad Case 池，标注团队每周分析一批，找到根因（缺文档？分块策略不对？Embedding 模型对某些领域不敏感？）然后定向修复。"

#### 追问 1.5："你的系统有没有单点故障？"

> "有，最明显的单点是 LLM API。如果 GPT-4 API 挂了，整个生成链路停摆。我的灾备方案是：
> - LLM 层：主模型 GPT-4o → 备模型 Claude 3.5 Sonnet，通过 LiteLLM 做自动 fallback。LiteLLM 检测到主模型连续 3 次 timeout 或 5xx，自动切备模型。
> - 向量库：Qdrant 做主从复制，主节点挂了从节点自动接管。
> - ElasticSearch：集群自带高可用，3 节点即可保证。
> - 语义缓存 Redis：AOF 持久化 + Sentinel 哨兵模式。
>
> 真正防不住的是全机房断电——但对于日均 10 万次查询的系统，99.9% 可用性（月宕机 43 分钟）是合理目标。"

---

### 题 2 追问：多 Agent 协作系统

#### 追问 2.1："你为什么用 LangGraph 而不是 CrewAI？不能因为 LangGraph 更火就选它。"

> "不是因为 LangGraph 更火，是因为我需要三个能力，CrewAI 做不到：
>
> 1. **条件分支**：比如 Research Agent 搜完竞品后，如果发现竞品数量 > 10，走'聚合摘要'分支（用 Analysis Agent 做聚类），否则直接走 Writer Agent。CrewAI 的任务编排是线性的，只能按预先定义的顺序执行，做不到这种运行时条件分支。
> 2. **Re-planning**：Critic Agent 评审 Writer 的报告后，如果质量分 < 0.7，需要触发重新规划——比如补搜某个维度、重写某个段落。LangGraph 的 StateGraph 支持从任意节点跳回 Planner 节点重新执行。CrewAI 只能在单任务内重试，不能全局重规划。
> 3. **断点恢复**：一个复杂任务可能跑 10 分钟，中间某步因为 API 限流中断了，你能从断点继续而不是从头再来。LangGraph 的 Checkpoint 机制天然支持，CrewAI 不支持持久化中间状态。
>
> 但 CrewAI 有一个场景我非常推荐：3 人以内的快速原型。CrewAI 的配置式开发几分钟就能搭一个串行 Agent 流水线，不需要写状态图。所以我的态度是：原型用 CrewAI，生产用 LangGraph。"

#### 追问 2.2："Agent 之间怎么通信？如果 Research Agent 搜错了，下游怎么知道？"

> "通过 LangGraph 的共享 State 传递。State 是一个 TypedDict 定义的严格类型结构，不是随便丢一个 JSON。每个 Agent 在 State 中有自己的字段，Research Agent 写入 research_results，Analysis Agent 读取这个字段。
>
> 防错机制：Analysis Agent 不是无条件接受 Research Agent 的输出。它会做一层验证——'这些搜索结果是否覆盖了目标竞品的核心维度（定价、市场份额、产品特性）？'如果覆盖不全，它可以请求 Research Agent 补搜——State 中多一个 retry_requests 字段，流程跳回 Research 节点。更重要的是，每个 Agent 写入 State 时都附带 confidence 字段，下游能看到'这个结果的上游自己都不太自信'。"

#### 追问 2.3："怎么防止 Agent 死循环？"

> "三重保障：1. Token 预算（软限制）：整个任务 50K Token 预算，用完还没出结果 → 降级返回。2. 步数限制（中限制）：全局 20 步、单 Agent 5 步，超过触发 Supervisor 介入。3. 循环检测（硬限制）：State 在最后 3 步没变化 → 强制中断 + 人工介入。关键是 Token 预算是防止 AI 失控烧钱的第一道防线，不是阻止失控本身。"

#### 追问 2.4："Critic Agent 自己也是 LLM，怎么能保证评得准？"

> "LLM-as-Judge 有自评偏差。我的应对：1. 用不同模型做评审（Writer 用 GPT-4o，Critic 用 Claude）。2. 结构化评审维度（覆盖率、准确性、逻辑性、可读性、格式各打分，不是笼统的'质量分'）。3. 人工校准集：定期抽 50 个样本让人工和 Critic 分别打分，差距 > 15% 说明评估标准需要调。4. 如果连续 3 轮 Re-planning 质量分没有提升，不再纠结，直接输出 + 标注'可能需补充'。"

#### 追问 2.5："5 个 Agent = 5 倍成本，怎么说服老板？"

> "不是 5 倍。Research 和 Analysis 用 GPT-4o-mini，只有 Writer 用 GPT-4o。实际成本约 1.5 倍。但价值：一个人写竞品分析报告需 4 小时（查 2h + 分析 1h + 写 1h），Agent 5-8 分钟完成。按人力 ¥100/h，每次省 ¥400。每天 10 份，月省 ¥12 万。Agent 多出的那部分月成本不到 ¥5000。ROI ≈ 24x。老板不需要理解架构，只需要知道：每月多花 ¥5000 API 费，省了一个全职分析师。"

---

### 题 3 追问：大模型服务成本优化

#### 追问 3.1："语义缓存如果命中了相似但不完全一样的问题，直接返回旧答案不怕出错吗？"

> "语义缓存不是简单字符串匹配。三层闸门：1. 相似度阈值：Embedding 余弦相似度 > 0.95 才命中。'查北京天气'和'北京天气怎么样'能命中，'北京什么天气适合旅游'命不中——意图不同。2. 时效性检查：天气/股价 TTL=5 分钟，知识问答 TTL=7 天。3. 用户反馈信号：命中后被点'踩'的缓存立即标记为待审核。三层之下，缓存导致的错误率 < 0.3%，但能省 15-25% API 调用。"

#### 追问 3.2："模型路由怎么防止'简单判成困难'导致成本反向升高？"

> "路由误判成本是单向的：简单判成困难 = 多花钱；困难判成简单 = 质量下降。策略是宁可保守：分类器只识别'确定简单'的任务（FAQ 匹配、单句翻译、格式转换），用 BERT-small 微调 < 5ms。不确定的默认走大模型。关键指标不是'路由准确率'，而是'用户满意度 + 总成本'——满意度不变、成本在降，路由就是有效的。"

#### 追问 3.3："自部署开源模型听起来很美，但实际运维成本怎么算？"

> "开源模型'免费'是假的。隐性成本：GPU 服务器 ¥3万/月 + 运维人力 ¥1万/月 + 电费机房 ¥0.3万/月 + 框架维护 ¥0.5万 = ¥4.8万/月。所以 ROI 依赖调用量：日均 < 5 万次用 API 更划算，> 20 万次自部署优势大。我们的场景日均 6 万次，简单任务 3.6 万次从 API 切到自部署，月省约 2 万。面试中亮出这组数字比说'开源省钱'强 10 倍。"

#### 追问 3.4："成本降了 70%，质量有没有偷偷下降？怎么证明？"

> "评测体系建设决定了你能不能回答这个问题：1. Golden Set 200 条覆盖各类难度，每次模型切换自动跑对比质量分。2. 质量分下降 > 3% 阻断上线。3. 线上 A/B 10% 流量对比点踩率/人工转接率，跑一周统计显著才全量。我们实际优化：质量分 98.2 vs 97.9（差距 < 1%），成本降 71%。能拿出这组数据，比说'我觉得没降'通过率高 3 倍。"

#### 追问 3.5："上下文压缩会不会丢掉关键信息？"

> "不是简单截断，是有方向的压缩：1. 检索结果压缩：Reranker 从 20 篇筛 Top-5 + 每篇提取 2-3 句关键句，8000 token → 2000 token，丢冗余不丢关键。2. 对话历史压缩：小模型定期生成摘要，3 轮后就做，不是 10 轮爆了才做。3. 压缩粒度按场景：格式转换任务不需要压缩，开放性问答才需要。底线：压缩后人再读能不能还原原意？偏了 = 策略需调整。"

---

---

## 补充：Agent 系统的 CI/CD 与部署流水线

系统设计面试中，面试官常追问："你的系统怎么上线？怎么保证上线不出事？"以下是 Agent 系统特有的 CI/CD 考量。

### Q.新增："Agent 系统的 CI/CD 有什么特殊性？"

> "Agent 的 CI/CD 比传统微服务多三个维度：
>
> 1. **Prompt 变更也是一种代码变更**：改了 Prompt 就要走 CI——跑评测集 → 质量分对比 → 不达标阻断合并。Prompty（Prompt as Code）是这个理念的实践——Prompt 和代码存在同一仓库，用同一套 CI。
>
> 2. **Tool 变更的回归测试爆炸**：Agent 有 10 个工具，改一个工具的返回格式，可能影响 Agent 对它的调用方式。CI 必须跑全量 Agent 轨迹回归——不是测工具本身对不对，是测 Agent 用了这个工具后的表现对不对。
>
> 3. **Agent 行为的不可精确断言**：传统 API 测试你 assert `response.status == 200`。Agent 你只能 assert '任务完成率 > 85%'。这意味着 Agent CI 依赖 LLM-as-Judge 评分，而评分模型本身也要做校准。
>
> **实际 Pipeline 设计**：
> ```
> PR 提交 → 静态检查（Prompt 格式、Tool Schema 校验）
>         → 单元测试（单个 Tool 正确性）
>         → Agent 集成测试（50 条 Golden Case 跑全流程）
>         → LLM-as-Judge 评分（每 Case 多维度评分）
>         → 评分对比（新 vs 旧，差距 > 5% 阻断）
>         → 人类审批（如涉及高风险 Tool 变更）
>         → 灰度发布（5% → 20% → 100%，每步观察 1 小时）
> ```

### Q.新增追问："Agent 的灰度发布和普通服务有什么不同？"

> "三个不同：
> 1. **灰度维度更多**：不只是'新版本 vs 旧版本'，还需要灰度 '新 Prompt vs 旧 Prompt' '新模型 vs 旧模型' '新 Tool 配置 vs 旧 Tool 配置'。建议用 Feature Flag 来做多维度组合实验。
> 2. **实验归因困难**：Agent 的一次失败可能是路由错了、检索漏了、Tool 坏了、生成偏了。A/B 只看最终成功率不够，需要按 Agent 步骤做指标下钻——'新版本在哪一步显著差了'。
> 3. **观察窗口更长**：Agent 的 Bad Case 是低频长尾的——可能跑了 1000 次才有一次严重错误。灰度观察窗口至少 24 小时，且需要有 Bad Case 自动聚类告警。"

---

## 深度系统设计题补充（高频大题）

### 题4：设计一个支持千万级文档的企业搜索系统（混合检索 + RAG + 权限）

**① 需求澄清**：
- 规模：1000 万文档，日均 100 万查询，峰值 QPS 50
- 功能：关键词 + 语义 + 过滤 + 权限 + 摘要生成
- SLA：P99 < 1s（检索）+ < 5s（含生成），可用性 99.9%
- 多租户：500 部门，跨部门隔离 + 部分共享

**② 顶层架构**：
```
Gateway → Query 处理（改写/意图）→ 并行检索
  ├─ BM25（ES 集群，分片）
  ├─ 向量检索（Milvus 分片 + partition_key）
  └─ 结构化过滤（PostgreSQL）
→ RRF 融合 → Rerank（GPU 集群）→ 权限过滤 → 生成
```

**③ 核心模块深挖**：
- **索引层**：1000 万 chunk，按租户 partition，HNSW（M=32, ef=128），增量更新 + 定期全量重建
- **检索层**：三路并行（BM25 + 向量 + 结构化），RRF 融合，top-50 → rerank top-10
- **权限层**：检索后过滤（性能好）+ ACL 标签（安全），双重保险
- **生成层**：模型路由（FAQ 走小模型），引用溯源

**④ 瓶颈与扩展（必答追问）**：
- 瓶颈：向量库内存、rerank GPU、生成延迟
- 10x 流量：向量库分片（10 shard）+ rerank 多副本 + 生成走模型路由降本 + 读写分离

**⑤ Tradeoff**：
- 实时性 vs 性能：增量更新慢（分钟级）但性能好；实时同步快但写放大
- 召回 vs 精度：三路并行提召回，rerank 提精度

---

### 题5：设计一个 LLM 推理服务平台（多租户 + 计费 + 模型管理）

**① 需求澄清**：
- 多租户：1000 企业客户，独立配额/计费/数据隔离
- 模型：支持 10+ 模型（GPT/Claude/开源），客户可选或路由
- 功能：API 调用 + 流式 + 批处理 + 微调托管
- SLA：99.95% 可用性，按租户分级 SLA

**② 顶层架构**：
```
租户 Gateway（鉴权/配额/计费）
  → 模型路由（按成本/质量/SLA/合规）
    ├─ 自建推理集群（开源模型）
    ├─ 上游 API Gateway（闭源，多 provider fallback）
    └─ 微调模型托管（LoRA 热加载）
  → 缓存层（精确 + 语义，按租户隔离）
  → 计费/可观测（per-tenant token 统计）
```

**③ 核心模块深挖**：
- **多租户隔离**：配额（令牌桶 per tenant）+ 数据（缓存/日志 namespace 隔离）+ 计费（独立账单）
- **模型路由**：按租户 SLA（高级走顶级模型）+ 成本（默认走性价比）+ 合规（金融客户只走私有模型）
- **微调托管**：LoRA adapter 热加载（base 模型共享 + adapter 切换），省 GPU
- **计费**：精确 token 计数 + 实时账单 + 防滥用

**④ 瓶颈与扩展**：
- 瓶颈：GPU 资源调度、热点租户（某大客户打满）、上游 API 限流
- 扩展：GPU 池化 + 优先级调度 + Spot 实例弹性 + 多区域多活

**⑤ Tradeoff**：
- 共享 vs 隔离：全共享省成本但 noisy neighbor；全隔离贵但稳。分级（VIP 独立 + 普通 shared）
- 缓存 vs 隐私：缓存提性能但可能跨租户泄漏，必须 namespace 严格隔离

---

### 题6：设计一个 AI 内容安全审核系统（实时 + 多模态）

**① 需求澄清**：
- 内容类型：文本 + 图片 + 视频（UGC 场景）
- 时延：文本 < 500ms，图片 < 2s，视频离线 + 实时关键帧
- 标签：色情/暴力/政治/广告/隐私 等 20+ 类
- 规模：日均 1 亿条 UGC

**② 顶层架构**：
```
UGC 上传 → 分流（文本/图/视频）
  → 多级审核
    L1: 规则 + 关键词（毫秒级，过滤 70%）
    L2: 小模型分类器（百毫秒级，过滤 25%）
    L3: 大模型/VLM（秒级，处理复杂 5%）
  → 人工审核队列（低置信度 case）
  → 决策（通过/拒绝/人工）
```

**③ 核心模块深挖**：
- **分层过滤**：L1 规则最快过滤大头，L2 小模型性价比高，L3 大模型处理疑难——漏斗省成本
- **置信度路由**：高置信度自动决策，低置信度转人工（约 3-5%）
- **多模态**：文本用 LLM、图片用 VLM、视频抽关键帧串行审
- **对抗防御**：防变体绕过（同音字/拆字/图片加噪），需对抗训练 + 持续更新

**④ 瓶颈与扩展**：
- 瓶颈：L3 大模型 GPU、视频处理、人工审核积压
- 扩展：L1/L2 水平扩、L3 GPU 池化、视频离线批、人工审核 SaaS 弹性

**⑤ Tradeoff**：
- 准确率 vs 召回率：审核场景宁可误杀（召回优先），漏过代价大
- 自动 vs 人工：全自动快但可能错杀合规内容；加人工兜底准但慢

---

## 2026 新增系统设计大题（AI-aware 设计）

> 2026 系统设计面试已从"传统 box-and-arrow"转向"AI-aware 设计"——考成本/延迟权衡、推理模型路由、Agent 工程化。以下 5 道是 2026 高频新题（OpenAI/Google 真题、美团/字节面经均出现）。用附录 C.3 八步模板答，重点在"关键决策"而非画图。

### 题6：设计一个 AI 编程助手（Cursor / Claude Code 类）架构

**① 需求澄清**：
- 三层能力：行内补全（FIM，<50ms TTFT）、对话式 Edit（带代码库上下文）、Agentic 模式（多文件改+跑命令+自验证）
- 规模：千级开发者、百万级文件代码库、低延迟在线

**② 顶层架构（三层）**：
```
1. 快速补全层：小模型(Codestral/GLM-Coder) + speculative decoding + FIM
2. Chat/Edit 层：代码库检索 + 上下文组装 + 中模型
3. Agentic 层：规划→tool-use循环→多文件改→跑测试→自验证
```

**③ 核心深挖（护城河在这）**：
- **代码库检索（context engineering 是 moat，不是模型）**：repo 索引（embedding + 符号 ranking）→ 检索增强 prompt 组装 → 上下文窗口打包
- **服务端优化**：语义缓存（重复补全）、Prompt/Prefix KV 复用（共享文件上下文）、流式、请求合并
- **Agentic 循环**：context-as-managed-resource（压缩、sub-agent、文件式记忆）

**④ 横切**：评测（Terminal-Bench/SWE-bench 类）、成本（补全高频要省钱）、安全（生成的代码沙箱执行、HITL review）

**⑤ 瓶颈与扩展**：
- 瓶颈：补全 TTFT（speculative + 小模型解）、代码库检索精度（symbol ranking）
- 扩展：补全层独立扩容、KV 复用提吞吐、后台云 Agent 异步

🎯 **关键判断**："**护城河不在模型（大家都能调 GPT/Claude），在代码库上下文工程 + 评测闭环。**" 2026 Karpathy 宣布 "vibe coding 已死"，转向 "agentic engineering"——AI 拥有任务，人做意图/spec/review。

---

### 题7：设计一个实时语音 Agent（端到端 <500ms）

**① 需求澄清**：实时语音对话（GPT-4o Realtime / Gemini Live 类），首音频 <500ms，支持打断（barge-in）

**② 延迟预算拆解（核心考点）**：
```
wake/VAD → 流式 STT(~100ms,分块) → LLM(流式 token) → 流式 TTS(首音频<250ms)
                                                     总目标 <500ms TTF Audio
```

**③ 架构选型（关键 tradeoff）**：
- **分离管线 ASR→LLM→TTS**：灵活可控，但延迟叠加多
- **原生 speech-to-speech / Realtime API**：延迟低（端到端），但可控性差、贵
- **传输**：WebSocket/SIP/WebRTC；turn-taking 检测（用户开口即打断）

**④ 延迟杠杆**：全链路流式、部分结果前传、小快模型、边缘推理、speculative response（预测用户说完前先起算）

**⑤ 横切**：打断处理、静音幻觉抑制、口音/噪声鲁棒、会话记忆、成本（实时音频按秒计费，贵）

**⑥ 瓶颈**：首音频延迟（TTS 流式 + LLM 首 token）、打断响应；扩展用边缘节点 + 模型分级

⚠️ **踩坑提示**：别只画 ASR→LLM→TTS 三框——要讲清**延迟预算拆解**和**分离管线 vs Realtime API 的选型 tradeoff**，这是 2026 语音 Agent 的核心考点。

---

### 题8：设计一个企业 MCP Gateway / Agent 网关

**① 需求澄清**：企业内统一管理 Agent ↔ 工具（MCP）、Agent ↔ Agent（A2A）的入口，多租户、安全、可观测

**② 定位（背这句）**：**"从 API Gateway 到 Agent Gateway"**——MCP = agent↔工具（直连、细粒度可见），A2A = agent↔agent（可扩展、粗粒度协作）。网关同时讲两套协议。

**③ 网关职责（类比 Kong Agent Gateway / Linux Foundation Agentgateway）**：
- **发现与注册**：Agent/工具注册中心，能力发现
- **鉴权授权**：OAuth、租户隔离、权限分级
- **限流策略**：按租户/工具/Agent 限流
- **可观测**：全链路 trace（每个 tool call / agent 通信）、审计日志
- **密钥与安全**：密钥管理、工具输出消毒、沙箱

**④ 参考架构**：AWS Bedrock AgentCore（MCP 管单 agent 工具，A2A 管多 agent）、MuleSoft A2A

**⑤ 横切**：多租户、治理合规、Tool Poisoning 防御（见 Day2 Q34）、A2A 跨厂商信任

**⑥ 瓶颈与扩展**：网关本身无状态可水平扩；瓶颈在审计日志存储和 trace 采样

🎯 **关键判断**："**Agent 网关是 2026 的 API 网关**——MCP/A2A 普及后，企业需要一个统一治理入口，这是平台层的新机会。"

---

### 题9：设计一个 AI 数据飞轮（持续改进闭环）

**① 需求澄清**：让 AI 系统越用越好——从生产数据中学习，反哺模型/prompt/知识库

**② 闭环（MAPE-K 框架）**：
```
Monitor(生产日志/trace) → Analyze(识别失败/低置信)
  → Plan(改什么) → Execute(重训/微调/加数据/DPO) → 知识库更新
                                          ↑________反馈回 Monitor_______|
```

**③ 数据来源（"数据排气"data exhaust）**：prompt/response 日志、隐式反馈（thumbs/retry）、显式反馈、用户纠正、Agent trace

**④ Pipeline**：在线日志 → 采样过滤 → 人工标注（或 LLM-as-judge）→ eval 回归集 → 微调/SFT/DPO → shadow 部署 → A/B → 全量

**⑤ 关键工程**：反馈捕获 UX、去重、PII 脱敏、CI/CD 里的 eval 门禁（eval-driven development，见 Day16）、防漂移/回归

**⑥ 横切**：合规（用户数据用于训练要授权）、数据质量、冷启动（没有反馈数据时）

🎯 **关键判断**："**飞轮是 AI 的真正护城河**——模型会商品化，但'用户反馈→eval→改进'的闭环越转越快，是别人抄不走的。设计飞轮比设计单次模型更重要。"

---

### 题10：设计一个 LLMOps 可观测平台（五大支柱）

**① 需求澄清**：企业级 LLM 应用的可观测 + 评测平台（Langfuse / Braintrust / Datadog Agent Observability 类）

**② 五大支柱（背）**：
1. **连续输出评估**：在线采样跑 LLM-as-judge，不只看延迟
2. **分布式 trace**：每个 LLM 调用/tool call/检索作为 span，parent/child（agent→tool→LLM）
3. **Prompt/版本优化**：Prompt 版本管理 + A/B
4. **RAG 监控**：检索质量、上下文相关性
5. **LLMOps 集成**：CI/CD 门禁、eval 回归

**③ Trace 设计**：OpenTelemetry 式 span，捕获 input/output/token/延迟/成本；trace 完整 agent 轨迹不只模型调用

**④ 关键指标（语义指标 > infra 指标）**：任务成功率、tool 调用准确率、检索 precision、幻觉率、cost/task、p95 延迟

**⑤ 横切**：采样策略（全量贵）、隐私脱敏、告警（质量回归不只延迟）

🎯 **关键判断**："**传统 APM（看 CPU/延迟）对 LLM 不够**——LLM 的'质量'是语义指标（幻觉率/任务成功率），需要新的可观测栈。这是 2026 平台团队的核心基建。"

---

### 补充追问：RAG vs Agent vs 微调，系统级"何时选哪个"？

**一句话框架**（2026 最高频判断题）：
> "**微调扩展'能力/行为'；RAG 扩展'知识'；Agent 扩展'工作流'。互补不互斥。**"

**决策标准**：
- 需求是"模型知道什么"（实时事实/引用）→ RAG
- 是"模型怎么表现"（风格/格式/低延迟）→ 微调
- 是"模型做什么"（多步动作/工具）→ Agent
- 数据变化频率、预算、延迟容忍、是否要引用/审计

**2026 nuance**：便宜的微调 + 强推理模型在模糊边界；默认 RAG+prompt，只按需加 FT/Agent。失败模式 = 过度工程化上 Agent（简单任务硬套 Agent 框架）。

---

## 2026 进阶系统设计大题（分水岭级）

> 以下 2 道系统设计题是 2026 年大厂面试的新热点——实时语音 Agent 和 AI Coding 平台。考的是"你能不能把 AI 能力放进实时交互和工程生产力场景"。这类题没有标准答案，重点考架构判断和权衡。

### 题 11. 设计一个实时语音 Agent（打断/低延迟/状态机）

**题目**："请设计一个实时语音对话 Agent，支持用户随时打断、延迟 < 1.5 秒、多轮对话记忆、可调用工具（查天气/设闹钟/控制智能家居）。日均 100 万次对话。"

**需求澄清**：
- 功能：实时语音输入 → 语音输出 + 工具调用 + 多轮记忆 + 打断
- 非功能：端到端延迟 < 1.5s（用户说话结束到 Agent 开始说话）、支持打断（用户说话时 Agent 停止输出）、日均 100 万对话、并发峰值 1 万
- 约束：移动端为主、网络不稳定、隐私合规（语音数据不长期存储）

**架构核心挑战**：

| 挑战 | 传统方案 | 实时语音 Agent 要求 |
|------|---------|-------------------|
| 语音识别 | 录完再 ASR | 流式 ASR（边说边识别） |
| 对话管理 | 轮次分明 | 打断检测 + 状态机（听/想/说/被打断） |
| 语音合成 | 生成完再 TTS | 流式 TTS（边生成边合成边播） |
| 延迟 | 3-8s | < 1.5s（每环节都要压） |

**架构图**：

```
用户语音 → [VAD 语音活动检测] → [流式 ASR] → [打断检测]
                                              ↓ (检测到打断)
                                        [状态机: 中止当前输出]
                                              ↓ (未打断)
                              [LLM 推理 + 工具调用] ← [记忆管理]
                                              ↓
                              [流式 TTS] → [音频流播] → 用户
                                              ↑
                                        [打断检测: 用户开始说话→中止 TTS]
```

**关键设计决策**：

1. **VAD（语音活动检测）是第一道关**：用轻量模型（WebRTC VAD / Silero VAD）实时检测"用户开始说话"和"用户停止说话"，延迟 < 50ms。这是打断检测的基础
2. **流式 ASR**：边说边识别，用流式 ASR（Whisper-streaming / FunASR streaming），每 200ms 输出一次中间结果。用户说完即出最终文本，不等"录完再识别"
3. **状态机设计**：四状态——`listening`（听用户说）→ `thinking`（LLM 推理）→ `speaking`（TTS 播放）→ `interrupted`（被打断，回到 listening）。状态转换由 VAD + ASR 驱动
4. **流式 TTS**：LLM 输出按句/按词流式送 TTS，TTS 边合成边播。不要等 LLM 生成完再 TTS——那是 3-5s 延迟。用"首句快出 + 后续流式"策略，首句 < 500ms 出声
5. **打断处理**：`speaking` 状态下 VAD 检测到用户说话 → 立即中止 TTS 播放 + 清空音频缓冲 + 状态切回 `listening`。LLM 推理中被打断要能取消（abort 请求）
6. **延迟拆解预算**（1.5s 内）：
   - VAD 检测说话结束：50ms
   - 流式 ASR 最终结果：200ms
   - LLM 首 token：400-600ms
   - TTS 首句合成：300-500ms
   - 网络传输：100-200ms
   - 合计：1.05-1.55s（卡在边缘，要每环节优化）

**工具调用与记忆**：
- 工具调用：LLM 决策要调工具时，先语音告知用户"正在查询…"再调（避免沉默），工具返回后继续语音输出
- 记忆：短期（当前对话上下文，滑窗 + 摘要）+ 长期（用户画像/偏好，跨会话，mem0 管理）
- 隐私：语音数据识别后即丢（不存原始音频），只存文本摘要用于记忆

**瓶颈与扩展**：
- **瓶颈**：LLM 首 token 延迟（400-600ms）是最大头。优化：用小模型（8B 级）做语音对话 + 大模型做复杂任务路由；Prefix Cache 复用 system prompt
- **10x 流量**：流式 ASR/TTS/LLM 都要独立扩缩容；GPU 池按组件分（ASR 用 GPU、TTS 用 GPU、LLM 用 GPU，各自弹性）
- **网络不稳定**：移动端弱网要处理——音频包丢失重传 + ASR 容错 + 客户端本地 VAD 兜底（网络断了也能检测打断）

🎯 **面试官追问**：
- "打断后 LLM 还在推理怎么办？" → 要能 abort LLM 请求（推理引擎支持 cancel）；已生成的部分结果丢弃；不能让"用户打断后 Agent 还在说"
- "多轮对话上下文怎么管？" → 语音对话上下文膨胀快（每轮几百 token），用滑窗 + 摘要（超过 N 轮摘要历史）；长期记忆走外部存储
- "延迟 1.5s 够吗？" → 对话场景 1.5s 是底线（再慢用户觉得卡）；理想是 < 1s。用端侧小模型（SLM）做简单对话能压到 800ms

⚠️ **踩坑提示**：
- 别忽略"打断"——这是语音 Agent 区别于文本 Agent 的核心交互，没有打断检测的语音 Agent 体验极差
- "流式"是关键词——ASR/TTS/LLM 都要流式，任何环节"等完再传"都会让延迟超标
- 延迟要拆解到每环节——笼统说"我们优化了延迟"不够，要讲每环节多少 ms

---

### 题 12. 设计一个 AI Coding 助手平台（代码补全 / Review / Agent）

**题目**："请设计一个企业级 AI Coding 助手平台，支持代码补全、PR Review、自然语言生成代码、多语言（Python/Java/Go/TS）。要接入企业私有代码库做上下文，日均 50 万次调用，代码不能外泄。"

**需求澄清**：
- 功能：IDE 内代码补全 + PR 自动 Review + 自然语言→代码 + 代码库问答
- 非功能：补全延迟 < 300ms、Review 准确率 > 85%、代码不出企业（隐私）
- 约束：多语言、接入 GitLab/GitHub 私有库、支持 VSCode/JetBrains IDE

**架构核心挑战**：

| 挑战 | 方案要点 |
|------|---------|
| 代码隐私 | 私有化部署 Code 模型（DeepSeek-Coder/Qwen-Coder），不走外部 API |
| 补全低延迟 | < 300ms：小模型 + Prefix Cache + 代码上下文预取 |
| 代码库上下文 | 代码 RAG：把企业代码库建索引，补全时召回相关代码作为上下文 |
| Review 准确率 | 多维度 Review（安全/性能/规范/逻辑）+ 误报控制 |

**架构图**：

```
IDE 插件 → [代码补全请求] → [上下文组装: 当前文件 + 相关代码 RAG]
                                    ↓
                        [补全模型(小): DeepSeek-Coder-1.3B] → 补全建议 → IDE
                        [Review 模型(大): Qwen-Coder-32B]   → Review 报告 → PR 评论

代码库同步 → [Git webhook] → [代码解析/分块] → [代码 Embedding] → [向量库]
                                                                    ↑
                                                          [补全/Review 时召回]
```

**关键设计决策**：

1. **补全用小模型，Review 用大模型**：补全要快（< 300ms）用 1.3B-7B 级模型 + Prefix Cache（当前文件作为 prefix，KV 复用）；Review 要准用 32B+ 模型 + 完整上下文，延迟可接受几秒
2. **代码 RAG 是核心**：企业代码库建向量索引（按函数/类/文件分块），补全时召回"当前编辑文件相关的代码"作为上下文（如调用的 API 定义、同类实现）。这让补全"懂企业代码规范"而不只是通用代码
3. **代码分块策略**：按 AST 切（函数/类为单位），保留 import/类型定义作为元数据。代码 chunk 要保留"调用关系"图（A 调 B，补全 A 时召回 B 的定义）
4. **Review 多维度**：拆成安全（漏洞检测）、性能（N+1/死循环）、规范（lint）、逻辑（空指针/边界）四个维度，每个维度独立 prompt + 模型判断，减少误报
5. **隐私保障**：模型私有化部署 + 代码不上传（IDE 插件只发"补全请求 + 上下文片段"，不发整个文件）+ 审计日志（谁的什么代码被模型看了）

**瓶颈与扩展**：
- **瓶颈**：补全的 300ms 延迟——模型推理 + 网络往返。优化：IDE 插件本地跑 1.3B 模型（端侧补全）+ 云端 7B 做复杂补全；Prefix Cache 复用当前文件 KV
- **代码库索引更新**：代码库频繁变更，索引要增量更新（Git webhook 触发，只索引变更文件）；全量重建只在周末低峰跑
- **10x 流量**：补全是高频低延迟（模型要多副本 + GPU 弹性）；Review 是低频高算力（可排队异步处理）

🎯 **面试官追问**：
- "代码补全怎么做到 300ms？" → 小模型（1.3B 端侧或 7B 云端）+ Prefix Cache（当前文件 KV 复用）+ 流式输出（首 token < 100ms 就可显示）+ 代码上下文预取（用户打字时预取相关代码）
- "代码 RAG 和普通 RAG 有什么区别？" → 代码分块按 AST（不是固定 token）+ 保留调用关系图 + 代码专用 embedding（如 jina-embeddings-v2-code）+ 召回的是"API 定义/类型声明"等结构性代码
- "Review 误报多怎么办？" → 分维度 Review（安全/性能/规范/逻辑各自独立判断）+ 置信度分级（高置信直接标，低置信给建议）+ 用户反馈闭环（点踩的规则降权）

⚠️ **踩坑提示**：
- 别忽略"代码隐私"——企业级 AI Coding 的第一要求是代码不出企业，用外部 API 直接被毙
- "补全用大模型"是性能灾难——补全是高频低延迟场景，必须小模型 + 缓存
- 代码 RAG 用通用 embedding 效果差——要用代码专用 embedding + AST 分块

---

## 今日产出检查

> **升级版验收**：系统设计题的标准不是"画完图"，而是"限时讲完 + 接住追问"。

| 产出 | 达标标准 | 验收方式 | ✓/✗ |
|------|---------|---------|-----|
| 题1：RAG 平台架构 | 七层完整 + 每模块选型理由 | 限时 15 分钟讲完 | ⬜ |
| 题2：Agent 协作架构 | 状态机 + 错误恢复 + 可观测 | 限时 15 分钟讲完 | ⬜ |
| 题3：成本优化方案 | 路由 + 缓存 + 降级 + 量化 | 限时 15 分钟讲完 | ⬜ |
| 题6-10（2026新题） | 至少练 1 道（语音Agent/MCP网关/数据飞轮任选） | 能讲清延迟预算或闭环设计 | ⬜ |
| 瓶颈分析 | 每题能指 1-2 个瓶颈 | 面试官追问接住 | ⬜ |
| 10x 扩展 | 每题能讲分片/读写分离 | 面试官追问接住 | ⬜ |
| 追问弹药 | 每题 ≥ 3 个追问有预案 | 朋友/AI 抽问 | ⬜ |
| RAG/Agent/微调选型 | 能背"能力/知识/工作流"框架 | 被问能脱稿答 | ⬜ |

**达标线**：7 项中 ≥ 6 项达标 → Day6 通过。
**未达标处理**：
- 画不完 → 主文件附录 C.3 八步模板没练熟，重画 3 道限时
- 讲超时 → 砍细节，只讲主干 + 2-3 个关键决策
- 接不住追问 → 把追问归类（瓶颈/扩展/选型/容错），逐类准备弹药

**关键提醒**：
- 系统设计题**最高频减分项是"直接开画不澄清需求"**——必须先花 2-3 分钟问约束
- 架构图 4-6 个主模块足够，别画成密集蜘蛛网——面试官看不清等于没画
- 主动暴露关键决策点引导追问："这里我选 X 不选 Y，原因是……" 把面试官引到你准备好的地方
- 主文件附录 **B.17-B.19** 是这三道题的答题框架，务必交叉对照
