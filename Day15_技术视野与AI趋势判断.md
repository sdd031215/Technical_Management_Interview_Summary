# Day 15（第三周 Day 1）：技术视野与 AI 趋势判断

> **目标岗位**：AI 方向技术负责人 / Tech Lead
> **今日主题**：终面如何展现技术视野 —— 对 MCP、多模态、长上下文、Agent 等趋势形成自己的判断框架
> **核心原则**：不要说"我看好 XX"，要说"XX 正在改变 YY，我的判断是 ZZ，因为……"

---

## 🔗 与主文件《3 周版》的联动（避免重复，先读这里）

本文件是 Day15 的**深化执行手册**。技术视野是终面高频维度：

| 你要做的 | 去哪找 | 本文件补充什么 |
|---------|--------|---------------|
| 查趋势题答题框架 | 主文件 **附录 B.4**（系统设计趋势题）+ **B.12**（MCP）+ **B.16**（长上下文） | ✅ 补 6 大趋势的判断框架 |
| 查半日时段练习 | 主文件 **第三周 Day 1**（终面技术视野半日表） | ✅ 本文件做"观点深化" |
| 查技术深度 | 主文件 **附录 A**（MCP/Agent/推理部署原理） | ❌ 不重复 |

**阅读顺序建议**：本文件的核心价值是"形成你的判断"，主文件是事实依据——两者结合，避免"复述新闻"。

---

## 一、今日面试场景定位

终面（VP/CTO 面）的核心考察维度之一就是「技术视野」。面试官要回答一个问题：**这个人能不能在我还没看清的时候，帮我看清方向？**

考察形式通常是开放式追问，比如：
- "你怎么看当前 AI 技术的发展趋势？"
- "MCP 协议的出现意味着什么？"
- "你觉得下一个技术突破会在哪里？"

**扣分回答**：背诵新闻、罗列名词、没有自己的判断。
**加分回答**：有框架、有因果链、有"为什么这么判断"的推理、有边界意识（知道什么适用、什么不适用）。

---

## 二、核心话题拆解：你需要形成观点的 6 个方向

### 2.1 MCP 协议（Model Context Protocol）

#### 面试高频问题

**Q1：MCP 是什么？为什么重要？**

> MCP（Model Context Protocol）是 Anthropic 于 2024 年 11 月推出的开放协议，目标是建立 AI 模型与外部工具/数据源之间的标准化交互层。可以理解为「AI 界的 USB-C 接口」—— 让任何 AI 应用都能通过统一协议接入任何工具，而不是每个模型、每个工具各写一套对接代码。

**Q2：MCP 和 Function Calling 的本质区别是什么？**

| 维度 | Function Calling | MCP |
|------|-----------------|-----|
| 协议性质 | 厂商私有（OpenAI/Anthropic 各自定义） | 开放标准协议 |
| 工具注册 | 在请求中内联传入工具定义 | 通过 MCP Server 独立注册和发现 |
| 调用模式 | 单次请求-响应 | 多轮调用 + 状态感知 + 自主协作 |
| 权限控制 | 无标准方案 | 通过 Server 层做统一权限治理 |
| 生态扩展 | 每个模型需要单独适配 | 一次编写，多模型共用 |

**面试观点框架**（可以这样讲）：

> "MCP 解决了 AI Agent 落地的核心瓶颈：工具接入缺乏标准。过去每个 Agent 框架都有自己的一套 Tool 定义，Function Calling 也是各家各玩各的。MCP 的出现意味着工具层从'私有协议'走向'开放标准'，这对生态发展是巨大的杠杆。但我认为 MCP 的挑战不在协议本身，而在于生产环境的安全治理——当 Agent 可以调用任意 MCP Server 时，如何做权限隔离、审计追踪、调用限流？这些问题 2025-2026 年正在成为新的工程焦点。"

#### Q3：生产级 MCP Server 怎么做安全治理？

核心要点：
1. **权限最小化**：每个 MCP Server 声明需要的权限范围（文件读写、网络访问、数据库操作），Host 端强制执行
2. **调用审计**：全量记录 Tool Call，包括参数、返回、耗时、Token 消耗
3. **人机协同**：高风险操作（删除数据、发送邮件、转账）必须人工确认
4. **沙箱隔离**：MCP Server 运行在容器/沙箱中，限制系统级访问

**参考资源**：
- [JavaGuide - 万字拆解 MCP 协议](https://javaguide.cn/ai/agent/mcp.html)
- [Anthropic 官方 MCP 规范](https://modelcontextprotocol.io/)

---

### 2.2 AI Agent：从工具到数字员工

#### Q4：2025-2026 年 Agent 为什么突然爆发？

不是突然爆发，而是三个先决条件同时成熟：
1. **基础模型能力**：GPT-4o/Claude 3.5+ 级别的模型在推理、规划、工具使用上达到可用阈值
2. **工具生态标准化**：MCP 协议让工具接入不再是一次性工程
3. **工程框架成熟**：LangGraph、CrewAI、AutoGen 等让 Agent 开发从"手写状态机"变成"声明式编排"

**你的判断**（形成自己的观点）：

> "Agent 正在经历和 2014 年微服务类似的范式转变。2014 年之前大家也在拆服务，但 Docker/K8s 标准化后才真正爆发。Agent 也一样——之前也能做，但 MCP + LangGraph + 评测体系标准化后，才进入可规模化复制的阶段。"

#### Q5：Multi-Agent 在生产环境中如何保证可控性？

回答框架（四层保障）：
1. **编排层**：不依赖 Agent 自由协商，而是用 LangGraph/编排引擎定义流程骨架，Agent 在约束内发挥
2. **状态管理**：所有 Agent 的状态集中在 State Machine 中，可追踪、可回放、可中断
3. **人机协同**：关键决策点设置 Human-in-the-Loop（HITL），Agent 出问题自动挂起等人工介入
4. **评测闭环**：每个 Agent 的输出都有自动化评测，连续不达标自动降级或摘除

---

### 2.3 多模态：感知能力的全面升级

#### Q6：多模态的工程落地，当前到了什么阶段？

分层判断（不要一刀切说"成熟了"或"不成熟"）：

| 能力层 | 成熟度 | 判断 |
|--------|--------|------|
| 图文理解（OCR + 图表） | ✅ 生产可用 | 文档解析场景已广泛落地 |
| 语音交互 | ✅ 生产可用 | 延迟降至 320ms，接近人类对话 |
| 视频理解 | ⚠️ 部分可用 | 短视频分析 OK，长视频一致性差 |
| 视频/3D 生成 | 🔬 探索阶段 | 质量和一致性不足以替代专业工具 |
| 跨模态推理 | 🔬 早期 | 图文关联的深度推理仍需提升 |

**你的判断**：

> "多模态的最大价值不在'生成好看的图'，而在'让 AI 理解真实世界'。企业场景中，最有价值的多模态应用是文档理解（合同、发票、报告）+ 视觉质检。这两块的 ROI 非常明确，2025 年已经是标配能力。"

---

### 2.4 长上下文：窗口越大越好吗？

#### Q7：上下文窗口从 4K 到 1M+ token，对应用开发意味着什么？

**不是越大越好，而是要回答"什么时候用大窗口、什么时候不该用"**：

- **大窗口的优势**：减少分块、提升跨文档关联能力、简化 RAG 架构
- **大窗口的代价**：推理成本非线性增长（1M context 的成本可能是 128K 的 8-10 倍）、延迟显著增加、注意力稀释（模型对长文本中间部分的"记忆"弱于开头和结尾——Lost in the Middle 问题依然存在）

**你的判断**：

> "长上下文简化了 RAG 的工程复杂度，但不能替代 RAG。两者的关系应该是'长上下文做精读，RAG 做初筛'。我的实践是：用向量检索把候选从 10000 篇缩减到 20 篇，再用长上下文模型对这 20 篇做全量理解。这比'塞 10000 篇进去让模型自己找'成本低、效果好。"

---

### 2.5 推理成本的下降趋势

#### Q8：推理成本以每年 10x 的速度下降，这意味着什么？

这是你面试中必须能展开讲的趋势，因为直接影响技术决策：

1. **过去不可行的方案现在可行了**：比如实时语音 Agent、全量文档理解、暴力 Rerank
2. **成本不再是瓶颈，变成了需要管理的资源**：模型路由（简单问题用小模型，复杂问题用大模型）成为必选项
3. **商业模式重构**：当推理成本趋近于零，AI 产品的护城河从"我有模型"变成"我有数据 + 场景 + 用户反馈闭环"

**量化参考（2025-2026）**：
- GPT-4 级别模型 API 成本：约 ¥2-5/1M tokens（较 2023 年下降 ~100x）
- 开源模型自部署（vLLM + A100）：约 ¥0.5-1/1M tokens
- 端侧模型：近乎零边际成本

---

### 2.6 端侧模型与离线 AI

#### Q9：端侧模型对 AI 应用架构有什么影响？

> "端侧模型的战略价值在隐私和实时性。不是替代云端模型，而是形成'端云协同'：常规任务端侧处理（毫秒级响应、零网络依赖），复杂推理上云。这个架构下，AI 应用的产品形态会发生根本变化——以前'AI 功能'必须是联网的，以后不是了。"

---

### 2.7 Agent 核心循环：从 ReAct 到隐式状态（2026 最新趋势）

> 来源：[Agent Loop 的收敛](https://mp.weixin.qq.com/s/T9jeKkiMUGcKi417Anofcg) — 刀客，2026.04

#### Q10：ReAct 模式为什么正在被抛弃？

> "ReAct（Reasoning + Acting）曾是 Agent 开发的事实标准——让模型交替输出 Thought 和 Action，runtime 解析文本执行工具。但现在主流框架（OpenClaw、LangChain、DeerFlow 2.0）都在逐步放弃显式 ReAct，转向**隐式状态的 Agent Loop**。
>
> 原因三个：
> 1. **强耦合 Prompt**：ReAct 依赖解析模型输出文本中的标记（如'Thought:'、'Action:'），一旦模型输出格式稍有偏差，整个 loop 就崩了——这是生产环境不可接受的脆弱性。
> 2. **结构化 Tool Calling 成为原生能力**：GPT-4/Claude 都原生支持 `tool_calls` 字段，模型是否要调用工具不再需要通过解析自然语言来判断，直接读结构化的 `tool_calls` 即可。
> 3. **模型能力提升**：模型已经能在连续对话中自行维持规划和上下文，不需要 runtime 显式为它维护 ReAct 的状态机。
>
> 一句话总结：ReAct 把智能编码在 prompt 里，新的隐式状态 Loop 把智能留在了模型里，runtime 只做机械的状态推进。"

#### Q11：隐式状态 Agent Loop 的核心逻辑是什么？

> "可以压缩成不到 30 行伪代码：
>
> ```
> messages = [UserMessage(input)]
> while True:
>     ai_msg = model.invoke(messages, tools=TOOLS)
>     messages.append(ai_msg)
>     if not ai_msg.tool_calls:      # 模型没调工具 = 任务完成
>         return ai_msg.content
>     for call in ai_msg.tool_calls: # 有工具调用 → 执行 → 结果喂回去
>         result = execute(call)
>         messages.append(ToolMessage(result))
> ```
>
> 整个循环只有一个判断：**模型是否产生了 tool_calls**。如果有 = 继续循环，如果没有 = 退出。Thought → Action → Observation 的因果链没有消失，但被内化为消息历史的自然演化——模型通过多轮上下文自行理解'已经做了什么、接下来该做什么'。
>
> 面试中可以强调的认知：**'Agent = 模型决策 + Harness 执行 + 消息历史传递'**。不需要 Planner、不需要 Memory、不需要 Orchestrator——这些都是在这三件事上叠加的能力。"

#### Q12：OpenAI 格式和 Anthropic 格式的循环判断有什么区别？

> "本质信号完全一样，只是字段名不同：
>
> | 概念 | Anthropic | OpenAI |
> |------|-----------|--------|
> | 判断是否调工具 | `response.stop_reason == 'tool_use'` | `response.choices[0].message.tool_calls` 非空 |
> | 工具调用 ID | `tool_use_id` | `tool_call_id` |
> | 工具结果 role | `user`（嵌在 user 消息里） | `tool`（独立 role） |
>
> 面试中可以这样讲：'不管用哪家模型，Agent Loop 的决策树就一个分支——模型有没有举手要工具。协议差异只是字段名不同。这也解释了为什么 MCP 协议的价值这么大——它在上层统一了这种差异性。'"

#### Q13：30 行代码的 Demo Loop 和 Claude Code 1729 行的差距在哪？

> "Claude Code 的核心循环和我们写的 30 行 Demo 逻辑完全相同。但剩下的 1700 行全是**保护、恢复和优化**：
>
> | 机制 | Demo | Claude Code |
> |------|------|-------------|
> | 循环退出判断 | `tool_calls` 非空 | `needsFollowUp` 流式标志位（流式场景下更可靠） |
> | 退出路径 | 1 条 | 8+ 条（maxTurns、context 超长、token 截断恢复、用户中断、Stop Hook 拦截…） |
> | Token 截断恢复 | 无 | 最多重试 3 次，自动检测截断 |
> | Context 管理 | 无 | 用 Haiku 后台生成工具历史摘要、自动压缩 |
> | 并行工具执行 | 串行 | `StreamingToolExecutor` 边收边跑，安全工具并行 |
> | 状态管理 | 1 个 `messages` 列表 | 10 字段 State 对象 |
>
> **面试金句**：'30 行代码写一个能跑的 Agent 很简单，让它在生产环境不崩溃才是工程的核心。这也是为什么 Harness Engineering 正在成为独立的技术方向——模型在变强，但如何把模型'驯住'，工程上刚刚起步。'"

---

### 2.8 Agent 落地实战：敬畏大模型，别当成万能解药

> 来源：[Agent 落地踩坑实录](https://mp.weixin.qq.com/s/OasTgsTG8TbLFNHrYW7sjA) — "机智的叉烧" CS的陋室，2026.06

#### Q14：Agent 落地中最容易犯的错误是什么？

> "最根本的错误是：**对大模型过于信任**。具体表现四个层面：
>
> 1. **忽视效果评价**：改一行 Prompt 或 Skill，自认为'小改动没问题'就不测。实际 Agent 的脆弱性远超想象——蝴蝶效应非常严重。任何改动都必须跑一遍评测集。
>
> 2. **无视不确定性**：大模型终究是概率模型。对那些高确定性、错误零容忍的任务（如加法运算、精确数据统计），应该果断交给代码或计算器，而不是'让大模型试试'。
>
> 3. **不认模型边界**：数值计算密集、表格解析、风格硬朗的文本生成（大模型偏软）、超长文本处理——这些是大模型的天生弱项。知道模型'什么做不好'，比知道'它会什么'更体现工程成熟度。
>
> 4. **盲目堆砌 Harness 模块**：Memory、Loop、Multi-Agent 不是越多越好。每个模块都要回答：**触发信号是什么？处理方法是什么？** 如果回答不了，就别加。"

#### Q15：Agent 系统的评测体系怎么搭建？

> "这是 Agent 落地最容易被跳过但最关键的一步。核心四条：
>
> 1. **建测试集**：不需要海量，但要覆盖在线的多种场景，特别是边界 Case 和异常输入。有标准答案的直接标注，没标准答案的用 LLM-as-a-Judge 评分。
>
> 2. **定指标**：准确率、评分、异常率都行，重要的是能让你对系统现状有扎实的量化认知，而不是靠感觉。
>
> 3. **随业务演进更新测试集**：新任务要加入，稳定部分可适当缩减。精力聚焦在变化最大的模块上。
>
> 4. **尽可能接近真实环境**：测试集要包含 Memory、Skills 等运行上下文，不能只在'干净环境'下测。
>
> > '一套严格的评价体系，对于一个 Agent 系统而言非常困难，但是建立后大家绝对会发现，有这么一套体系保驾护航，系统的稳定性和可靠度会非常高，绝对是值得的。'"

---

#### Q15.5（新增）："Agent 评测框架这么多（AgentBench、GAIA、SWE-bench、WebArena、τ-bench），面试时怎么讲？"

> "不能只是罗列名字。面试中要展示的是'我知道每个评测测什么、不测什么'：
>
> | 评测框架 | 测什么 | 不测什么 | 适用展示场景 |
> |---------|--------|---------|------------|
> | **AgentBench** | 8 个环境中的基础 Agent 能力（OS/DB/Web/游戏等） | 真实业务场景的端到端效果 | "我们选基础模型时参考了 AgentBench 排名" |
> | **GAIA** | 多步推理 + 工具使用（需要搜索/浏览/编码） | 复杂业务逻辑的正确性 | "GAIA 的高分模型在我们场景下工具调用更稳定" |
> | **SWE-bench** | 真实 GitHub Issue → 代码修复 | Agent 的规划和记忆能力 | "评估代码 Agent 的事实标准，但只测工程师场景" |
> | **WebArena** | Web 任务完成（购物/社交/内容管理） | 离线场景、企业内部系统 | "测 Web Agent，但我们的 Agent 不面向公开 Web" |
> | **τ-bench** | 真实世界任务完成率（含部分成功的细粒度评分） | 中文场景效果 | "τ-bench 的评分粒度比 SWE-bench 更精细，区分'完全完成'和'方向对但没做完'" |
> | **自建 Evaluation** | 你的业务场景 | — | "最终决策一定看自建评测集，通用 Benchmark 只是初筛" |
>
> **面试话术**：'通用 Benchmark 的价值是帮你快速筛掉明显不行的方案，但不能帮你做最终决策。就像买车看碰撞测试，5 星的不一定适合你，但 2 星的一定不能买。我们选模型时会参考 AgentBench 和 GAIA，但最终是否上线，看的是自建 Golden Set 上的表现。'"

### 2.9 Agent 可观测性深度（工程化必备）

> 来源：[Agent 可观测性](https://opentelemetry.io/) — OpenTelemetry + Agent 专用方案

#### Q.新增："Agent 的可观测性为什么比普通微服务复杂？"

> "普通微服务的可观测性是'请求 → 服务 A → 服务 B → 服务 C → 响应'，这是一条确定性的链路。Agent 的可观测性是'请求 → LLM 推理 → 工具调用 A → LLM 推理 → 工具调用 B → ...（N 步后）→ 响应'。三个根本区别：
>
> 1. **步数不确定**：一个 Agent 任务可能 2 步完成也可能 20 步，trace 长度差异巨大。传统的采样策略（1/100）在 Agent 场景不适用——你可能刚好跳过了那个 15 步的异常任务。
>
> 2. **非确定性**：同样的输入两次，Agent 可能走完全不同的路径。所以 trace 对比没有意义——你不能说'上次的 trace 长这样，这次不一样所以出 bug 了'。
>
> 3. **语义层观测**：微服务观测的是延迟、错误率、QPS。Agent 还需要观测'Agent 选对工具了吗''Agent 在第几步开始偏离正确路径''这次失败是因为检索漏了还是生成偏了'——这是语义层的问题，不是指标层的问题。
>
> **观测四层架构**：
> - **L1 Token 层**：每次 LLM 调用的 token 消耗、延迟、模型名称（成本归因）
> - **L2 Step 层**：每步的 tool call 名称、参数、返回值摘要、耗时（过程追踪）
> - **L3 Task 层**：端到端任务完成状态、总耗时、总 token、用户反馈（业务指标）
> - **L4 Session 层**：跨任务的用户行为、偏好演化、记忆使用情况（长期分析）
>
> **工具推荐**：
> - **Langfuse**（首选）：专门为 LLM/Agent 设计，支持 step-level trace、Prompt 版本管理、人工标注
> - **OpenTelemetry + 自研语义Span**：把 L1-L4 的信息编码为 OTel Span Attributes，接入公司统一的观测平台"

---

### 2.9 Loop Engineering：提示词工程之后的下一个杠杆（原）

> 来源：[Loop Engineering 实操手册](https://mp.weixin.qq.com/s/kICrdEkPCYAiyOiwI-Gt1Q) — Datawhale, 2026.06

#### Q16：什么是 Loop Engineering？和 Prompt Engineering 的关系是什么？

> "Loop Engineering 是让 AI 系统在反复运行中持续改进的一套工程方法论。如果 Prompt Engineering 解决的是'一次对话怎么调好'，Loop Engineering 解决的是'N 次运行怎么越来越好'。
>
> 五个核心构件：
> - **Automations**：loop 的心跳，按节奏触发（定时/事件），设定硬停止条件
> - **Worktrees**：多个 Agent 并行时用 Git worktree 隔离工作区，互不干扰
> - **Skills**：把项目背景、约定、踩坑经验写成持久化资产，Agent 每轮直接读
> - **Connectors**：通过 MCP 接上 GitHub、Jira、Slack、Sentry 等真实工具链
> - **Sub-agents**：写的和验的分开，第二个 Agent 用不同指令验收，避免'自我说服'"

#### Q17：Loop Engineering 什么时候该用、什么时候不该用？

> "Loop 不是免费的——它烧 Token、有时间成本、产出不一定好。四个前置检查：
>
> 1. **任务是重复的吗？** 一次性任务一个好 prompt 更快更省
> 2. **有自动判定'干砸了'的机制吗？** 测试、类型检查、linter 至少一个，否则你就是在盲跑
> 3. **Token 预算扛得住浪费吗？** Loop 会反复读上下文、重试、试探，不产出也烧钱
> 4. **Agent 能跑自己写的代码吗？** 需要日志、可复现、能定位崩溃点
>
> 还有一条底线，比上面四个都重要：**你打算 review 它产出的代码吗？不打算，就别建 Loop。**"

#### Q18：Loop 跑起来后有哪些翻车模式？

> "三种经典翻车：
>
> 1. **假装干完了（Ralph Wiggum 循环）**：Agent 提前发'完成'信号，活干一半就退。根因是没有硬闸门——测试/构建不过就自动拒。
>
> 2. **理解债务**：Loop 交付越快，'仓库里有什么'和'你理解什么'的差距越大。总有一天你要 Debug 一个团队里没人读过的系统。
>
> 3. **认知投降**：你慢慢不再自己判断，Loop 返回啥就收啥。所以即使有了 Loop，也要读 diff、抽查闸门、不让 Loop 碰架构。"

---

## 三、技术趋势判断框架（面试时这样讲）

给你一个可复用的表达框架：

```
1. 趋势是什么（一句话定义）
2. 为什么是现在（触发因素：成本/技术/生态/需求）
3. 对应用层的影响（我们做系统的人该关注什么）
4. 我的判断（机会在哪、坑在哪）
5. 边界（什么场景适用、什么不适用）
```

**示例 — 完整表达**：

> "我判断 2025-2026 年 AI 应用有两个核心趋势。第一是 Agent 从 Demo 到生产的工程化，这需要解决可观测、评测、安全三个核心挑战，MCP 协议的出现恰好解决了工具标准化的问题。第二是推理成本断崖式下降，这会让很多'过去太贵做不了'的事变得可行，但也意味着 AI 产品的护城河会从'我有模型'变成'我有数据和场景理解'。我自己在做的准备是，把团队的技术栈从'调 API'升级到'搭平台'——建模型路由、建评测体系、建 Prompt 版本管理，这些才是可持续的竞争力。"

---

## 四、今日行动计划

- [ ] 针对上面 6 个方向，每个方向用「五分钟表达」练习一遍（计时）
- [ ] 整理 3 个你亲身经历中能体现"技术判断力"的案例
- [ ] 阅读：[JavaGuide AI 专栏 - 万字拆解 MCP](https://javaguide.cn/ai/agent/mcp.html)
- [ ] 阅读：[腾讯云 - 2025 AI 技术前沿展望](https://cloud.tencent.com/developer/article/2587122)

---

## 五、易踩的坑

1. **不要当复读机**：背诵行业新闻是扣分项，面试官要的是你的判断
2. **不要只说"我看好"**：要说"我为什么看好"、"我看好什么具体方向"、"我不看好什么"
3. **不要回避不确定性**：说"这个问题现在还没有定论，我的判断是 X，但需要关注 Y 的进展"比硬给答案强
4. **不要脱离自身定位**：你是技术负责人，不是研究员。多讲工程落地判断，少讲学术前沿猜想

---

## 六、深度趋势判断补充（加分题）

> 以下两题是"高段位趋势判断"——答得好能体现你不只是追热点，而是有独立思考。

### 趋势题1：你怎么看 Scaling Law 放缓？后 Scaling 时代押注哪些方向？

**五段式答**：

**① 现状判断**：Scaling Law（模型越大越强）确实在放缓——GPT-4 到 GPT-4o 到 o1 的提升，靠的不只是堆参数，更多是训练数据质量、RL、推理时计算（test-time compute）。绝对意义的"参数 scaling"边际收益递减。

**② 为什么是现在**：①高质量语料快耗尽 ②训练成本指数增长 ③小模型能力已够用很多场景

**③ 后 Scaling 四大方向（我的排序）**：
1. **推理时计算**（o1/R1 路线）：给模型更多"思考时间"换能力，性价比高
2. **数据质量与合成数据**：高质量数据 > 海量噪音，合成数据是新蓝海
3. **架构创新**：MoE、SSM（Mamba）、线性注意力——降推理成本
4. **Agent 与工具增强**：能力不够工具凑，模型 + 工具 > 单纯大模型

**④ 我的押注**：作为应用层 Tech Lead，我押"推理时计算 + Agent 工程化"——前者让中等模型解决难题，后者让模型能力倍增。架构上要为这两个做准备（LLM Gateway 支持推理模型、Agent 平台）。

**⑤ 边界**：纯学术方向（如世界模型、AGI）我不押，作为关注项；应用层聚焦"已成熟可落地"的。

---

### 趋势题2：开源会追上闭源吗？你的团队该 all-in 哪边？

**① 现状**：开源（Llama/Qwen/DeepSeek）和闭源（GPT/Claude）差距在缩小——中文场景 Qwen 已接近或持平 GPT-4o；通用能力闭源仍领先 6-12 月。

**② 我的判断（不是非此即彼）**：
- **通用能力天花板**：闭源长期领先（OpenAI/Anthropic 投入大），但差距会稳定在"可用范围内"（开源够用）
- **特定场景**：开源 + 领域微调常超闭源通用模型（领域专家 vs 通才）
- **趋势**：开源是"基础设施民主化"，会让 AI 应用层爆发（类似 Linux 之于互联网）

**③ 团队策略（混合）**：
- 核心高频流量走**开源 + 微调**（成本低、可控、合规）
- 长尾难题走**闭源 API**（能力顶、省事）
- 抽象层（LLM Gateway）保证两边平滑切换

**④ Tradeoff**：
- 能力 vs 可控：闭源强但黑盒；开源可控但需自维护
- 成本 vs 省心：开源便宜但运维重；闭源贵但省心

**⑤ 示例**：
> "我的判断是'开源追到可用，闭源保持领先'，团队应该混合而非 all-in。我们策略：核心客服流量走 Qwen 微调（领域内超 GPT-4o 通用，成本低 10 倍），长尾复杂 case 走 Claude API（能力兜底）。LLM Gateway 抽象层让我们随时调整配比——开源出新模型就评估替换。这样既不被闭源锁定，也不冒险 all-in 开源。判断依据：应用层不需要'最强模型'，需要'够用 + 可控 + 可持续'。"

---

### 趋势题3：Vibe Coding / AI 编程（Cursor / Claude Code）会怎么改变开发？你怎么看？

**① 现状**：2025-2026 AI 编程爆发——Cursor、Claude Code、GitHub Copilot Workspace、Windsurf，开发者用自然语言描述需求，AI 生成/修改代码。这是"Vibe Coding"（凭感觉编程）——开发者不写代码，只描述意图 + review AI 输出。

**② 为什么是现在**：①模型够强（Claude 3.5 Sonnet 代码能力质变）②工具成熟（Cursor 的 composer、Claude Code 的 agentic 能力）③范式转变（从补全到 agent 自主完成多文件任务）。

**③ 对开发的影响（我的判断）**：
1. **生产力 10 倍提升**：常规 CRUD、测试、文档、重构 AI 接管，开发者专注架构和业务逻辑
2. **角色转变**：开发者从"写代码"变"审代码 + 设计 + 沟通"——更像 Tech Lead
3. **技能重构**：自然语言精确表达（spec writing）比语法记忆更重要
4. **风险**：①代码质量参差（需强 review）②安全漏洞（AI 不懂上下文）③过度依赖（开发者基本功退化）

**④ 生产团队怎么用**：
- **全员配 AI 编程工具**（Cursor/Copilot），生产力立即提升
- **建 review 文化**：AI 生成的代码必须人审，不能盲合
- **spec 驱动开发**：先写清楚 spec，AI 实现，人审 + 测
- **不碰核心逻辑**：架构设计、关键算法、安全相关人写

**⑤ Tradeoff**：
- 效率 vs 质量：AI 快但质量参差，需 review 平衡
- 依赖 vs 能力：过度依赖 AI 导致基本功退化，需保持手感

**⑥ 示例**：
> "我们团队全员上 Cursor + Claude Code，生产力提 3-5 倍——常规需求、测试、文档基本 AI 完成，开发者专注 review 和架构。但立了规矩：①AI 生成代码必须人审 ②核心算法/安全相关人写 ③每月做'无 AI 编程日'保持手感。判断：Vibe Coding 不会取代开发者，但会取代'不会用 AI 的开发者'。未来开发者核心能力是'精确表达需求 + 严谨 review'，不是语法记忆。作为 Tech Lead，我推动团队转型——从写代码的人，变成指挥 AI 写代码的人。"

---

### 趋势题4：从 Claude Code / Cursor 的设计，能学到什么 Agent 工程经验？

**① 原理**：Claude Code、Cursor 是当前最成功的生产级 Agent 产品，它们的设计是 Agent 工程的"教科书"。拆解其设计能学到实战经验。

**② 关键设计拆解**：

| 设计 | Claude Code / Cursor 的做法 | 工程启示 |
|------|----------------------------|---------|
| **上下文管理** | 不塞全部代码，按需检索（grep/语义检索） | Agent 上下文要"精"不要"多" |
| **工具组合** | 文件读写 + 终端 + 搜索 + 编辑，组合完成复杂任务 | 工具要原子化 + 可组合 |
| **CLAUDE.md / .cursorrules** | 项目级"记忆文件"，告诉 Agent 项目约定 | Agent 需要可配置的项目记忆 |
| **流式 + 可中断** | 边做边展示 + 用户随时打断 | Agent 要可观测 + 可控 |
| **sandbox 执行** | 命令在 sandbox 跑，危险操作确认 | 高风险动作必须 HITL |
| **diff-based 编辑** | 不重写文件，用 diff 修改 | 工具要可逆 + 可审计 |
| **grep 优于 RAG** | 代码场景 grep 精确，不用向量检索 | 工具选择要匹配场景 |

**③ 核心启示（Agent 工程通用）**：
1. **上下文精炼 > 上下文堆砌**：长上下文模型虽强，但精准上下文（检索/过滤）仍关键
2. **工具原子化 + 可组合**：少而精的工具组合 > 多而杂的专用工具
3. **记忆文件机制**：让用户配置 Agent 行为（如项目规范），比微调更灵活
4. **diff/可逆设计**：所有改动可审计 + 可回滚，生产 Agent 必备
5. **工具选择匹配场景**：不要无脑上 RAG（代码场景 grep 更准）

**④ Tradeoff**：
- 通用 vs 专用：通用 Agent（Claude Code）灵活；专用 Agent（客服）更可控。按场景选
- 自动 vs HITL：全自动快但风险高；HITL 慢但安全。危险动作必须 HITL

**⑤ 示例**：
> "从 Claude Code 学到几个 Agent 工程原则，应用到自己系统：①上下文精炼——我们 RAG 不塞 top-20，而是 rerank 后只塞 top-5 最相关 ②工具原子化——把'查订单'拆成'查订单 + 改订单 + 退款'原子工具，组合完成复杂流程 ③记忆文件——做'客服规范.md'让运营配置 Agent 话术 ④diff 设计——所有改订单操作记 diff 可回滚 ⑤工具匹配场景——结构化查询用 SQL 不用向量检索。这些原则让我们的客服 Agent 可控性和 Claude Code 一个量级。"

---

### 趋势题5：推理模型（o3/R1/GPT-5 thinking）会取代 RAG 和微调吗？

**① 现状**：推理模型靠 test-time compute（想得更久）大幅提升闭卷能力（o1 在 AIME 数学 83% vs GPT-4o 13%），让"是不是还需要 RAG/微调"成 2026 最热的判断题。

**② 为什么不取代（四个理由）**：
1. **知识冻结**：推理模型知识止于训练截止日，不解决"实时事实"
2. **无引用/溯源**：合规、客服、投研场景必须可溯源，推理模型给不出处
3. **私有/长尾数据幻觉**：训练没见过的内部数据，推理模型照样编
4. **贵且慢**：思考 token 多，成本是普通模型 5-10 倍、延迟 10-30 倍

**③ 重新划定边界（不是取代是重新分工）**：
- 推理模型减少了"为补推理能力而做的微调"
- 但 RAG 仍是"实时知识 + 引用"的唯一解
- **胜式组合：推理模型 + RAG + 蒸馏小模型**——推理模型攻坚、RAG 补实时知识、蒸馏把推理下沉到便宜模型（详见 Day3 Q29-Q30）

**④ Tradeoff**：
- 能力 vs 成本：推理模型强但贵慢，只适合难题
- 微调的"能力"角色被削弱，但"风格/延迟/蒸馏"角色仍在

**⑤ 我的判断（这样说）**：
> "推理模型不取代 RAG 和微调，而是重新分工。它把'为补推理而微调'的需求削弱了，但 RAG 解决的'实时事实 + 引用 + 私有数据'它碰不到——知识冻结、无溯源、贵慢是硬伤。我的实战配置：推理模型只用在 top 10% 难题（投研分析、复杂 SQL），RAG 管实时知识，蒸馏把推理能力下沉到便宜模型跑日常。一句话：**推理模型是攻坚的特种兵，不是替代主力的通用解**。"

⚠️ **踩坑提示**：别说"有了推理模型就不用 RAG/微调"——2026 典型过度简化，会被追问"实时知识怎么办、合规溯源怎么办"打脸。

---

### 趋势题6：AI Agent 是泡沫还是现实？2026 你的诚实判断？

**① 现状**：Forrester 2026 预测"AI 从炒作转向干脏活的'安全帽年'"；Elaine Zelby 等 KOL 说"Agent 是最被高估的"。这是终面必问的"诚实度"题——面试官想听你不带水份的判断。

**② 我的分层判断（不一边倒）**：
- **高估的**：全自主通用 Agent、"AI 员工"、"10x 开发者"——这些还差 3-5 年，可靠性是硬阻塞
- **真实且复利的**：窄域、有清晰 eval 的 Agent（编程、客服、数据抽取）、工具调用、人在回路的场景

**③ 2026 是"证明之年"**：
- 炒作降温是健康的——把资源从"炫技 demo"导向"能上生产的窄域 Agent"
- **真正的护城河不在 Agent 本身**，在 eval + 数据 + 工作流集成（别人抄不走）

**④ Tradeoff / 边界**：
- 通用 vs 可靠：通用 Agent 灵活但不可靠；窄域 Agent 可靠但场景受限
- 自主 vs HITL：全自主炫但危险；HITL 稳但慢

**⑤ 我的判断（这样说）**：
> "我的诚实判断：Agent 整体被高估，但窄域 Agent 真实且复利。全自主通用 Agent、'AI 员工'这些还早 3-5 年，可靠性是硬伤——我做过生产 Agent，最难的从来不是'搭起循环'，是让它不闯祸。但窄域 Agent（编程、客服、数据抽取）在有清晰 eval 和 HITL 的前提下，2026 已经能稳定上生产。我的押注：把精力投在 eval 基础设施、数据飞轮、工作流集成上——这是护城河，Agent 本身会商品化。2026 是'证明之年'，炒作降温是好事。"

⚠️ **踩坑提示**：别一边倒（"Agent 彻底改变世界"或"Agent 全是泡沫"）——分层判断 + 给边界 + 绑定你的实战，才是终面想听的成熟度。

---

### 趋势题7：AI 的价值最终在哪里被捕获？（基础设施 vs 应用 vs 模型）

**① 现状**：Sam Altman / a16z 的论点——价值主要在应用层；模型在商品化（OpenAI 开放权重、中国开源）；基础设施是万亿 capex 话题。这是考察"商业判断力"的高管题。

**② 三层价值流向（我的判断）**：
- **模型层**：商品化中（开源 + 推理降价），难成持久护城河
- **基础设施层**：资本密集的公用事业（NVIDIA/云厂赢，但利润率被压缩）
- **应用/工作流层**：**可防御的价值 + 收入聚集在这**——因为这是专有数据、分发、工作流锁定所在

**③ 但要警惕（边界）**：
- 应用层也是创业坟场——"薄包装"（thin wrapper）会被模型厂商一键抹平
- 真正能守住的应用：深度工作流集成 + 数据飞轮 + 用户切换成本

**④ Tradeoff**：
- 模型 vs 应用：做模型烧钱且易被开源；做应用轻但易被抹平
- 基础设施：稳但重资本、低利润率

**⑤ 我的判断（这样说）**：
> "价值最终在应用/工作流层被捕获，但前提是'深'不是'薄'。模型在商品化（开源 + 推理降价），基础设施是资本密集的公用事业（NVIDIA 和云厂赢，但利润率被压）。真正的价值在深度工作流集成 + 专有数据飞轮 + 用户切换成本——薄 wrapper 会被模型厂商一键抹平。所以我的策略：不赌模型（用最好的），不碰基础设施（太重），深耕一个垂直工作流，用数据飞轮建护城河。这也是为什么我强调 eval 和数据飞轮——它们是应用层护城河的基石。"

---

### 趋势题8：A2A 协议 + "Agent 互联网"——会先发生在围墙花园还是开放网络？

**① 现状**：A2A（Google，100+ 合作伙伴）让 Agent 互相发现和协作，是"Agent 互联网"的基础。但跨厂商协作的信任/认证/结算是硬障碍。

**② 分时间线判断**：
- **近期（1-2 年）**：A2A 主要在"单厂商内多 Agent"和"企业内 Agent 网络"落地——围墙花园内
- **中期（3-5 年）**：跨厂商的 B2B Agent 协作（你的订票 Agent + 我的支付 Agent）成真，但仍在可信联盟内
- **远期（5+ 年）**：开放 Agent 经济（含结算/支付）——但信任基建要先就位

**③ 阻塞点**：
- 信任/认证：怎么确认对方 Agent 可信、不恶意
- 幂等/结算：Agent 间任务幂等、支付结算模型
- 安全沙箱：防恶意 Agent 利用协作链路攻击
- 死锁/环路检测：Agent 链里互相调用形成死循环

**④ Tradeoff**：
- 开放 vs 可控：开放网络创新但风险大；围墙花园稳但碎片化
- 标准化 vs 灵活：协议统一降集成成本但限制定制

**⑤ 我的判断（这样说）**：
> "A2A 我判断会先在围墙花园和企业内成真，开放 Agent 经济还要 5 年以上。跨厂商协作的信任、认证、结算是硬阻塞——不是协议能解决的，要身份基建 + 经济模型先就位。所以我们当前不做跨厂商 Agent 协作，先把企业内 Agent 网络（订票 Agent + 审批 Agent + 报销 Agent 互通）做好。A2A 协议本身设计合理（Agent Card / Tasks / 流式），但它的'HTTP 时刻'要等信任基建——就像 HTTP 也要等 SSL/支付才催生电商。"

---

### 趋势题9：硬件（Blackwell/Rubin、Groq、Cerebras）怎么重塑 AI 应用设计？

**① 现状**：GTC 2026 发布 Rubin（后 Blackwell，宣称推理再降 10×）；Groq 3 LPU（每兆瓦吞吐 35×）已集成进 Vera Rubin；Cerebras wafer-scale。硬件在重塑软件。

**② 对应用设计的影响**：
- **延迟预算放宽**：硬件快了，可以"多扔点推理"——更长 agent 循环、更多 rerank、实时多模态变得经济
- **成本曲线**：推理成本继续塌缩（硬件 + 开源 + SLM 三重），之前不可行的场景（每用户个性化、always-on agent）变可行
- **新交互形态**：Groq/Cerebras 的几百 token/s 让"实时思考展示""长推理链可视化"成可能

**③ 但要警惕（边界）**：
- **锁定 vs 灵活**：Groq/Cerebras 快但锁定；通用 GPU 灵活
- **benchmark vs 真实**：厂商 benchmark 和真实 workload 吞吐有差距，必须自测
- **Jevons 悖论**：成本降 → 用量涨 → 总花费可能不降反升（要做 GPU FinOps）

**④ Tradeoff**：
- 极致延迟 vs 通用性：专用硬件（Groq）极致快但锁定；通用 GPU 灵活
- 成本 vs 复杂度：多硬件栈降本但运维复杂

**⑤ 我的判断（这样说）**：
> "硬件降本正在重塑应用设计，核心是'延迟预算放宽 + 成本塌缩'。Groq/Cerebras 的几百 token/s 让实时语音、长推理链可视化这些之前不经济的交互变可行。但我的判断是别急着 all-in 专用硬件——锁定风险大，且厂商 benchmark 和真实 workload 有差距。务实做法：通用 GPU 起步，对极致延迟场景（实时语音）定点用 Groq 类硬件。还要警惕 Jevons 悖论——成本降了用量会涨，必须上 GPU FinOps 把推理当资源治理。一句话：硬件红利是真的，但要落到'解锁了什么新场景'，不是单纯'省钱'。"

---

## 2026 进阶趋势判断题（加分项）

> 以下 2 题是 2026 下半年新增的高阶趋势判断题——模型 commoditization 和 Agent 经济。考的是"你能不能看到模型层竞争的终局，以及判断 Agent 互联的节奏"。这类题没有标准答案，重点考因果推理和边界判断。

### 趋势题 10：模型 commoditization（商品化）——当模型能力趋同，竞争壁垒在哪？

**① 现状**：2026 年开源模型（Qwen3/DeepSeek-V3/Llama4）和闭源模型（GPT/Claude/Gemini）的能力差距持续缩小——很多 benchmark 上开源已追平甚至超越闭源。模型正在"commoditize"（商品化）——能力不再是稀缺资源，价格持续塌缩。当模型本身不再是壁垒，竞争转移到哪里？

**② 为什么现在**：
- 开源追赶速度加快（DeepSeek-V3 用 1/10 训练成本逼近 GPT-4o）
- 模型 API 价格持续降（GPT-4o 从 $30/百万 token 降到 $2.5，降 12 倍）
- 蒸馏/微调让小模型在窄域逼近大模型——"够用"的模型越来越便宜

**③ 壁垒转移的三个方向**：

| 方向 | 为什么成壁垒 | 谁在布局 |
|------|------------|---------|
| **数据飞轮** | 专有数据 + 用户反馈闭环 → 模型持续变好，别人抄不走 | 有用户/场景的公司 |
| **工作流整合** | AI 深度嵌入业务流程（不是"加个 chat"），切换成本高 | 垂直 SaaS / 企业应用 |
| **Eval + 治理基础设施** | 评估体系 + 模型治理 + 安全合规 → 能稳定上线 AI，这是工程能力 | 大厂 / 成熟 AI 团队 |

**④ 但要警惕（边界）**：
- **模型 commoditization 不是"模型不重要"**：底层模型仍在快速进步，"用 commoditize 的模型"不等于"不关注模型进展"。要持续评估新模型是否解锁新能力
- **"数据飞轮"被高估**：很多公司的"数据"其实是公开数据/低质量数据，构不成壁垒；真正有壁垒的是"用户行为数据 + 标注反馈"
- **工作流整合的窗口期有限**：先发优势能占 2-3 年，但竞品也会整合 AI，最终还是要回到"产品体验 + 数据"

**⑤ 我的判断（这样说）**：
> "模型 commoditization 是 2026 最确定的趋势——开源追平闭源、API 价格塌缩、模型能力不再是稀缺资源。但'模型商品化'不等于'AI 没壁垒'，壁垒在往上层转移：① 数据飞轮——有用户行为数据和反馈闭环的公司，模型持续变好，别人抄不走；② 工作流整合——AI 深度嵌入业务流程，切换成本高；③ Eval + 治理基础设施——能稳定上线 AI 的工程能力，这是大部分公司缺的。我的判断是：纯做'模型 wrapper'的应用活不过 2027（模型一升级 wrapper 就被卷），但'数据 + 工作流 + 治理'三位一体的应用有护城河。所以我让团队把精力投在 eval 基础设施和数据飞轮，不是追模型——模型会 commoditize，这些不会。"

🎯 **面试官追问**：
- "那 OpenAI/Anthropic 这些模型公司怎么办？" → 它们的壁垒在"前沿能力 + 品牌 + 生态（API/工具）"，但前沿能力的领先在缩小；长期看模型公司要么往"基础设施"走（云化），要么往"应用"走（自己做产品）
- "创业公司还有机会吗？" → 有，但不在"做个更好的模型"，在"用 commoditize 的模型 + 专有数据/工作流解决特定场景"。垂直场景 + 深整合 > 通用 + 浅 wrapper
- "数据飞轮怎么启动？" → 先有用户用（哪怕免费）→ 收集使用数据 + 反馈 → 标注 + 训练/微调 → 模型变好 → 更多人用。冷启动靠"人工标注 + 合成数据" bootstrap

⚠️ **踩坑提示**：
- 别说"模型不重要了"——commoditize 不等于不重要，底层模型仍需持续关注
- "我们的壁垒是数据"如果讲不出"什么数据、为什么别人没有"，是空话
- 忽略"工作流整合"是高频盲点——这才是大部分企业 AI 应用的真正护城河

---

### 趋势题 11：Agent 经济——A2A 协议什么时候真正到来？"Agent 的 HTTP 时刻"会发生在哪？

**① 现状**：2026 年 Google 推 A2A（Agent-to-Agent）协议，被称为"Agent 的 HTTP"——让不同厂商的 Agent 能互相发现、协商、协作。理想很美：你的 Agent 找航空公司的 Agent 订票、找酒店 Agent 订房、找支付 Agent 付款，全自动。但现实是——A2A 目前只在"围墙花园"（企业内/生态内）落地，开放 Agent 经济还远。

**② 为什么现在**：
- MCP（Agent ↔ 工具）已成事实标准，Agent ↔ Agent 是下一层
- 多 Agent 协作的需求真实存在（跨系统任务编排）
- Google/Anthropic 都在推标准协议，抢占"Agent 互联网"的入口

**③ A2A 落地的三个前提**（缺一不可）：
1. **信任机制**：Agent 之间怎么建立信任？（我的 Agent 凭什么信你的 Agent 不会骗它）——需要身份认证 + 信誉系统 + 问责机制
2. **经济模型**：Agent 协作怎么计费/结算？（我的 Agent 调你的 Agent 要付钱，微支付怎么搞）——需要 Agent 原生的支付层
3. **互操作标准**：Agent 的能力描述/协商协议/数据格式要统一——A2A 协议本身要成熟 + 被广泛采纳

**④ 分阶段落地判断**：

| 阶段 | 时间 | 场景 | 前提 |
|------|------|------|------|
| 企业内 A2A | 2026-2027 | 同一公司内多 Agent 协作（客服 Agent → 工单 Agent → 风控 Agent） | 信任=内部可信，经济=内部结算，标准=自定 |
| 生态内 A2A | 2027-2028 | 平台生态内（如 Google 生态 / 微信生态内 Agent 协作） | 信任=平台担保，经济=平台计费，标准=平台定 |
| 开放 A2A | 2030+ | 跨厂商跨平台 Agent 自由协作 | 信任/经济/标准三前提全齐，还需法律/监管框架 |

**⑤ 我的判断（这样说）**：
> "A2A 是方向，但'Agent 的 HTTP 时刻'不会很快到来。我的判断是分三阶段：① 2026-2027 企业内 A2A 先落地——同一公司内客服 Agent 调工单 Agent 调风控 Agent，信任靠内部、经济靠内部结算、标准自定，这块现在就能做；② 2027-2028 生态内 A2A——Google/微信/钉钉这种大平台内 Agent 协作，信任靠平台担保、经济靠平台计费；③ 开放 A2A 要 2030+——跨厂商自由协作需要信任机制（身份+信誉）、经济模型（微支付）、互操作标准三前提全齐，还要法律/监管框架（Agent 闯祸谁负责）。所以我的判断是：企业内和生态内 A2A 是 2026-2028 的真实机会，开放 Agent 经济是 5 年+ 的事。务实做法：先做企业内多 Agent 协作（价值真实、可控），别等开放 A2A 标准。"

🎯 **面试官追问**：
- "A2A 和 MCP 什么关系？" → 分层不竞争：MCP（Agent ↔ 工具/数据）是底层，A2A（Agent ↔ Agent）是上层。MCP 先成熟，A2A 在 MCP 之上
- "Agent 闯祸谁负责？" → 这是开放 A2A 的最大障碍。企业内好办（公司担责），开放场景需要法律框架——目前没有答案，这也是为什么开放 A2A 要很久
- "你们公司做 A2A 了吗？" → 如果做过：讲企业内多 Agent 协作（Supervisor 模式 + 内部消息总线）；如果没做过：讲"我们在评估，先做企业内的，开放的等标准成熟"

⚠️ **踩坑提示**：
- 别说"A2A 马上就来了"——开放 Agent 经济要 5 年+，企业内/生态内才是近期机会
- "A2A 就是 Agent 互相调 API"是 oversimplify——信任/经济/问责是真正的难点
- 忽略"Agent 闯祸谁负责"是高频盲点——这是开放 A2A 的法律障碍

---

## 七、今日自测清单

## 七、今日自测清单

> **升级版自查**：技术视野的标准不是"知道多少趋势"，而是"每个趋势能不能给带因果链的判断"。

| 趋势方向 | 自查问题 | 达标标准（5 分钟表达） | ✓/✗ |
|---------|---------|----------------------|-----|
| MCP | 是什么 + 为什么现在 + 我的判断 | 能讲清"协议层 + 安全治理挑战" | ⬜ |
| Agent 工程化 | 三个先决条件 + 生产挑战 | 能用"微服务 2014 类比"讲透 | ⬜ |
| 长上下文 vs RAG | 现状 + 趋势 + 边界 | 能讲清"融合不是替代" + 5 个限制 | ⬜ |
| 多模态融合 | VLM 现状 + 跨模态检索 | 能讲清工程落地挑战 | ⬜ |
| 推理模型（o1/R1）| 适用 + 不适用场景 | 能讲清"慢思考贵且适用范围有限" | ⬜ |
| 后 Scaling 时代 | 四大方向 + 你的押注 | 能给判断不只是复述 | ⬜ |
| 推理模型取代 RAG?（趋势题5） | 四个不取代理由 + 重新分工 | 能讲清"不取代是重新分工" | ⬜ |
| Agent 泡沫 vs 现实（趋势题6） | 分层判断 + 护城河 | 能给"不一边倒"的诚实判断 | ⬜ |
| 价值捕获（趋势题7） | 三层流向 + 薄 wrapper 警惕 | 能讲清"应用层深才有护城河" | ⬜ |

**达标线**：9 个方向中 ≥ 7 个能用"五步框架"（趋势/为什么现在/影响/我的判断/边界）讲完 → Day15 通过。
**未达标处理**：
- 答案像复述新闻 → 强制每条加"我的判断"和"边界"两段
- 没有观点 → 用"如果让我押注 1 个方向，我押 X，因为 Y"句式逼自己表态
- 脱离 Tech Lead 定位 → 删掉学术细节，多讲"工程上该准备什么"

**关键提醒**：
- 终面技术视野题的**杀手锏是"判断 + 因果 + 边界"三件套**，缺一不可
- 别怕说错——面试官更看重你**怎么思考**，不是结论对不对
- 把"我的判断"绑定到**你正在做的事**（"所以我让团队开始建评测体系/模型路由"），体现知行合一
- 详细答题框架见主文件附录 **B.12**（MCP）和 **B.16**（长上下文）
